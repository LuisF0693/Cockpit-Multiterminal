# Story 3.1: Dashboard master com visão agregada

## Status

InReview

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"

## Story

**As a** desenvolvedor multi-agente,
**I want** um painel master com todos os terminais/agentes, status e tempos,
**so that** eu governe a operação inteira de um único lugar.

## Acceptance Criteria

1. Dashboard lista todos os terminais ativos: nome, adapter, status, tempo no status atual, tarefa vinculada (quando houver).
2. Atualização em tempo real a partir dos eventos dos adapters.
3. Clicar em um agente navega para o terminal correspondente (e atalho de volta ao master).
4. Dashboard é a tela inicial padrão do app.

## Quality Gate Tasks

- [x] Pre-Commit (@dev) — verify 80 testes PASS
- [x] QA Gate (@qa) — **CONCERNS (aprovado)**: percepção visual pendente do fundador
- [ ] Pre-PR (@devops): quando houver remote

## Tasks / Subtasks

- [x] Task 1 — Tempo no status (AC: 1) — `lastStatusChangeAt` no record; registry atualiza em create/status/exited; `formatDuration` + teste
- [x] Task 2 — MasterDashboard (AC: 1, 2) — tabela completa (dot/nome/adapter/status/tempo/tarefa—/ação) com tick 1s; eventos push = tempo real
- [x] Task 3 — Navegação (AC: 3, 4) — view master|canvas (master inicial); nav no header; clicar/sidebar → canvas+foco; Ctrl+M alterna; canvas fica montado escondido (xterm/portas vivos)
- [x] Task 4 — Testes + verify — 80 testes PASS

## Dev Notes

Terminais continuam MONTADOS no canvas quando o master está ativo (esconder com CSS, não desmontar — desmontar mataria xterm/portas). Tarefa vinculada: coluna presente com "—" até o E5. FR: dashboard é a materialização do conductor (visão ponto 2).

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-14 | 0.1/0.2 | Draft + Validação PO GO 10/10 — Approved | River/Pax via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 (claude-fable-5) — Dex (@dev) via Orion

### Debug Log References

- Sem bugs.

### Completion Notes List

- Canvas permanece montado (display:none) quando o master está ativo — desmontar mataria xterm/MessagePorts. Sidebar agora navega para o canvas ao selecionar.

### File List

**Criados:** `packages/ui/src/{master-dashboard.tsx,format-duration.ts,format-duration.test.ts}`
**Modificados:** `packages/shared/src/ipc.ts` (lastStatusChangeAt), `packages/core/src/session-registry.ts`, `packages/ui/src/{index.ts,shortcuts.ts,shortcuts.test.ts}`, `apps/desktop/src/renderer/src/App.tsx`

## QA Results

### Gate: **CONCERNS** (aprovado) — Quinn (@qa) via Orion, 2026-07-14

7 checks PASS (80 testes; ACs 1-4 com evidência de código; tempo real por push). Issue única (low): percepção visual do dashboard pendente do fundador.
