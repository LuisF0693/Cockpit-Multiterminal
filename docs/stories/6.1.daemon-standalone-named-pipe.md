# Story 6.1: Daemon standalone com protocolo por named pipe

## Status

InReview

## Story

**As a** desenvolvedor multi-agente,
**I want** o PTY Host rodando como daemon próprio (fora da árvore do Electron) servido por named pipe,
**so that** as sessões de terminal existam independentes do processo da GUI.

## Acceptance Criteria

1. Binário/entry `cockpit-daemon` hospeda SessionManager + AdapterRegistry atuais (contrato de adapter intacto — NFR7) escutando em named pipe com framing de mensagens (controle) + frames binários (dados).
2. Protocolo cobre o contrato atual do host: configure, create (com adapter/restore), close, resize, list-adapters, session-exit, session-status, shutdown.
3. Daemon sobrevive ao exit do cliente: PTYs continuam vivos e produzindo transcript (critério (a) do spike).
4. Testes unit/integration do protocolo de framing; suíte completa PASS.

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 96 testes PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — `framing.ts` (pty-host): frames length-prefixed `[len u32][kind u8][payload]` — kind 0 = controle JSON, kind 1 = dados binários `[idLen u8][sessionId][bytes]`; `FrameDecoder` stateful (chunks parciais/múltiplos) + `encodeControl`/`encodeData`; testes unit
- [x] Task 2 — `daemon-protocol.ts`: `hello`/`hello-ack` com `protocolVersion` + mensagens do contrato atual (configure/create/close/resize/list-adapters/shutdown ↔ created/create-error/closed/session-exit/session-status/adapters) + `data-ack` (backpressure via pipe, mesmos HIGH/LOW water do host)
- [x] Task 3 — `daemon-server.ts`: `DaemonServer` (classe testável; registry INJETADO) — sessões chaveadas pela TAG (ULID do registry, estável p/ attach da 6.2); desconexão de socket NÃO dispõe sessões (AC3); scrollback writer/restore como no host; shutdown dispõe tudo (0 órfãos)
- [x] Task 4 — `daemon-entry.ts`: entry standalone (`--pipe` opcional, default `\\.\pipe\cockpit-daemon`) registrando os 5 adapters (shell/cmd/claude-code/codex/grok — mesmo conjunto do host-entry); SIGTERM/SIGINT → shutdown gracioso
- [x] Task 5 — `daemon-client.ts`: `DaemonClient` (connect+hello, createSession/write/resize/close/listAdapters/shutdown, callbacks onData/onExit/onStatus, acks automáticos) — consumidor: testes agora, Main na 6.3
- [x] Task 6 — testes: framing unit (parciais/múltiplos/roundtrip) + integração in-process com pipe REAL e PTY REAL (create → echo → destruir socket → sessão VIVA → reconectar → close → shutdown 0 órfãos); verify suíte completa PASS (96)

## Dev Notes

- Host atual (utilityProcess) fica INTACTO nesta story — o app só migra na 6.3; daemon e host coexistem no pacote pty-host (NFR7 continua: adapters só importados ali).
- Sobrevivência CROSS-PROCESSO já provada pelo spike (63ms, 0 órfãos); o teste de integração valida em-processo com pipe real (desconectar socket ≠ dispose) — smoke cross-processo de produto vem na 6.3.
- Pipe de teste único por run (`cockpit-daemon-test-{pid}-{rand}`) para não colidir com daemon real.
- ids de sessão do daemon = tag (ULID) — elimina o mapeamento ptyId↔tag e prepara attach/list-sessions (6.2).

## Change Log

| 2026-07-14 | 0.1/0.2 | Draft + PO GO 10/10 (Ready) + dev start | River/Pax via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List / File List

- `daemon-entry` NÃO é exportado no index do pacote (importa adapters — NFR7); consumidores usam `DaemonClient`/binário; testes importam `startDaemon` direto.
- Frame inválido derruba a CONEXÃO, nunca o daemon; teto defensivo de 8MB por frame; handshake versionado rejeita cliente incompatível sem afetar sessões.
- `hello-ack` devolve `daemonPid` — insumo do heartbeat/lease da 6.4.
- Ack imediato no client (renderer-level backpressure chega na 6.3 com o proxy).
- **Criados:** pty-host `framing.ts`+test, `daemon-protocol.ts`, `daemon-server.ts`, `daemon-entry.ts`, `daemon-client.ts`, `daemon.integration.test.ts`. **Modificados:** pty-host `index.ts`.

## QA Results

### Gate: **CONCERNS** (aprovado) — Quinn (@qa), 2026-07-14. 7 checks PASS (96 testes; AC1-4 com evidência: framing unit 5 casos + integração pipe/PTY reais cobrindo sobrevivência à desconexão e shutdown 0 órfãos; host atual intacto — regressão zero). Issues low: smoke cross-processo de produto fica para a 6.3 (spike já provou o conceito); ACL do pipe validada na 6.4 (planejado no épico); CodeRabbit não executado (WSL indisponível — padrão).
