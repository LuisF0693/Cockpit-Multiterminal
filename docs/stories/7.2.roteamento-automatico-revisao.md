# Story 7.2: Roteamento automático escritor → revisores

## Status

InReview

## Story

**As a** desenvolvedor multi-agente,
**I want** que a conclusão do escritor dispare revisão automática nos revisores vinculados,
**so that** eu não precise lembrar de acionar cada revisor manualmente.

## Acceptance Criteria

1. Quando o agente `writer` de uma tarefa em modo three-brain transiciona para `done` ou `waiting-input`, o sistema envia automaticamente uma instrução de revisão a cada agente `reviewer` vinculado (sem ação humana).
2. Instrução de revisão referencia a tarefa (título/descrição) e pede explicitamente uma avaliação do trabalho do escritor.
3. Envio automático é registrado na timeline com origem `system` (distinto de instrução manual via master, que é `human`).
4. Roteamento só dispara UMA VEZ por transição do escritor (não repete em toda checagem de status) — idempotente.

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 149 testes PASS + smoke:daemon PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — `planSdcReviewRouting(writer, task, allSessions)` em `@cockpit/core` (NOVO `sdc-routing.ts`) — função PURA (sem I/O/Electron) que decide quando rotear: `taskRole==='writer'`, status novo `done`/`waiting-input`, tarefa em `in_progress` (lookup seguro via `taskManager.find`, sem lançar), `classifyTaskRoles(...).isThreeBrain`. Extraída do wiring do Main para ficar 100% testável — decisão tomada DURANTE a implementação (não estava nos Dev Notes originais, mas segue o mesmo princípio de centralização já aplicado na 7.1/5.4)
- [x] Task 2 — `registry.onEvent` no Main chama a função pura e só executa efeitos quando o retorno não é `null`: `taskManager.updateState(taskId, 'awaiting_decision', 'system')` (transição JÁ válida, FR20) + trilha `sdc.review_requested` por reviewer via `persistence.recordSdcReviewRequest` (terminalId=reviewerId, origin `system`) — reusa o padrão de `instruction.sent`
- [x] Task 3 — mensagem de revisão construída DENTRO da função pura (única fonte da redação, agora testável junto da decisão); `registry.onEvent` envolto em try/catch (um erro aqui não pode quebrar outros assinantes, já que `emit()` não isola listeners)
- [x] Task 4 — contrato: canal `sdc.reviewRequested`, `SdcReviewRequestedEventSchema {taskId, writerId, reviewerIds[], message}`, `CockpitApi.sdc.onReviewRequested`
- [x] Task 5 — preload + App.tsx: assina o push; para cada `reviewerId` chama `instructAgent(reviewerId, message)` já existente (3.2/5.2) — Main nunca escreve PTY diretamente (decisão crítica 4)
- [x] Task 6 — testes: 9 casos de `planSdcReviewRouting` (dispara em done/waiting-input, mensagem com título+descrição+escritor, NÃO dispara sem writer/status errado/tarefa não in_progress/tarefa null/fora do three-brain/sem taskId); AC4 (idempotência) garantida estruturalmente pelo guard já existente do `SessionRegistry.markAgentStatus` (só emite em transição real) — documentado, não retestado; verify suíte completa PASS (149) + smoke:daemon PASS

## Dev Notes

- A transição `in_progress → awaiting_decision` como efeito colateral do roteamento NÃO é uma aresta nova (já existe desde a 5.1) — é o encaixe natural com a fila de decisões existente (3.4/5.3): assim que os revisores são acionados, a tarefa fica visível na fila para o humano decidir depois de ler as duas revisões (7.3/7.4).
- `registry.onEvent` não isola exceções entre listeners (`emit()` é um for-loop simples) — QUALQUER lógica nova registrada aqui precisa de try/catch interno; um bug na orquestração SDC não pode derrubar a persistência ou o espelho de eventos para a UI.
- Mensagem de revisão é construída INTEIRAMENTE no Main — reusa o padrão já estabelecido no redirect da 5.3 (mensagem de designação também montada no chamador, não no `instructAgent`).
- Lookup de tarefa é DEFENSIVO (sem usar `taskManager.get` que lança) — um `taskId` órfão (tarefa desvinculada/apagada) não pode derrubar o listener.

## Change Log

| 2026-07-14 | 0.1/0.2 | Draft + PO GO 10/10 (Ready) + dev start | River/Pax via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List / File List

- Refino sobre o plano original: a lógica de decisão (guards + mensagem) foi extraída para `planSdcReviewRouting` (função pura em `@cockpit/core`) em vez de ficar inline no `registry.onEvent` do Main — session-ipc.ts é Electron-only e nunca foi unit-testado diretamente neste projeto; extrair a decisão permite testá-la sem mockar `ipcMain`/`BrowserWindow`, mantendo o Main como puro orquestrador de efeitos colaterais.
- `TaskManager.get(id)` lança se a tarefa não existe — inadequado para um listener de evento que roda em background; adicionado `TaskManager.find(id): TaskRecord | null` (lookup seguro) reutilizável em qualquer orquestração futura.
- Efeito colateral "task → awaiting_decision" é deliberado (não estava explícito no AC original, mas necessário para o encaixe com a fila de decisões existente e com a 7.4, que só rejeita a partir de awaiting_decision) — documentado nos Dev Notes desde o draft.
- **Criados:** core (`sdc-routing.ts`+test). **Modificados:** core (`task-manager.ts` find(), `index.ts`, `state-store/persistence.ts` recordSdcReviewRequest), shared (`ipc.ts`: canal/schema/CockpitApi.sdc), Main (`session-ipc.ts`), preload, App.tsx (assinatura + instructAgent por reviewer).

## QA Results

### Gate: **CONCERNS** (aprovado) — Quinn (@qa), 2026-07-14. 7 checks PASS (149 testes + smoke:daemon PASS; AC1/AC2 com evidência unit completa — 9 casos cobrindo disparo e todos os guards de não-disparo; AC3 por revisão de código (padrão idêntico a `instruction.sent`, já testado em outras stories); AC4 documentado como garantia estrutural pré-existente, não uma nova invariante a testar). Issues low: fluxo ponta-a-ponta (writer real transicionando → reviewers reais recebendo instrução) não tem teste de integração automatizado — precisaria de 3 CLIs reais simultâneos, fora do escopo de CI; validação manual do fundador pendente; CodeRabbit não executado (WSL indisponível — padrão).
