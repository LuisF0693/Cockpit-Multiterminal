# Story 3.3: Timeline de eventos e decisões

## Status

InReview

## Story

**As a** desenvolvedor multi-agente,
**I want** uma linha do tempo cronológica de tudo que aconteceu na sessão,
**so that** eu audite o fluxo e reconstrua o raciocínio da operação.

## Acceptance Criteria

1. Timeline registra: spawn/fechamento de terminais, mudanças de status, instruções enviadas, decisões humanas, recuperações de sessão.
2. Cada evento tem timestamp, origem (agente/humano/sistema) e payload resumido.
3. Timeline é filtrável por agente e por tipo de evento.
4. Eventos persistem (integração com state store) e sobrevivem a restart.

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 83 testes PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** — percepção visual pendente | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — `StateStore.listEvents` (sqlite com WHERE dinâmico + memory) + `PersistenceManager.timeline` com flush prévio
- [x] Task 2 — `session.recovered` registrado no restore (AC1 completo: created/renamed/closed/exited/instruction.sent/recovered)
- [x] Task 3 — IPC `timeline.get` (Zod, limit≤500) + preload + CockpitApi.timeline
- [x] Task 4 — `TimelineView`: timestamp pt-BR/origem(⚙️🤖👤)/tipo/agente/payload resumido; filtros agente+tipo; nav Timeline + Ctrl+T; auto-refresh 5s
- [x] Task 5 — +2 testes (filtros/flush; recovered) — 83 total PASS

## Change Log

| 2026-07-14 | 0.1/0.2 | Draft + PO GO 10/10 | River/Pax via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List / File List

- Leitura NÃO passa pela queue (só flush prévio p/ frescor); AC4 de graça (tabela events persiste desde a 1.4).
- **Criados:** `packages/ui/src/timeline-view.tsx`. **Modificados:** core (types/sqlite/memory/persistence+tests), shared (TimelineEvent/timeline.get), session-ipc, preload, ui (index/shortcuts+test), App.

## QA Results

### Gate: **CONCERNS** (aprovado) — Quinn (@qa), 2026-07-14. 7 checks PASS (83 testes; filtros com evidência unit; persistência herdada da 1.4). Issue low: percepção visual pendente do fundador.
