# Story 14.5: Painel de preview de arquivo

## Status

InReview

## Story

**As a** fundador,
**I want** que abrir um arquivo da árvore abra um painel dedicado de ~520px com toggle Preview/Markdown,
**so that** eu leia docs confortavelmente sem apertar a sidebar, como no mockup.

## Acceptance Criteria

1. Selecionar arquivo na árvore abre painel de 520px entre canvas e telemetria: header de aba (ícone+nome+×, borda superior ciano como o mock), sub-barra com toggle Preview/Markdown só pra `.md` (demais direto em raw) (FR51).
2. Preview renderizado reusa `markdown-lite` (FR35).
3. Preview embutido na sidebar (12.1) REMOVIDO — a árvore só navega/abre.
4. Painel fecha no × e ao trocar de projeto; efêmero (não persiste), como o mock.

## Tasks / Subtasks

- [x] Task 1 — `packages/ui/src/file-preview-panel.tsx` (NOVO): `FilePreviewPanel` + tipo `PreviewFile`; anatomia do mock (linhas 205-259): aba com `borderTop` ciano, toggle pill Preview/Markdown, corpo raw em `pre` mono (AC1, AC2)
- [x] Task 2 — App.tsx: estado `previewFile` + `openFilePreview` (readFile no Main, 256KB, binário → mensagem); montado entre canvas e TelemetryPanel; reset na troca de projeto (AC1, AC3, AC4)
- [x] Task 3 — Implementada JUNTO da 14.2 (acopladas: a sidebar nova não tem preview embutido — sem esta story, FR35 regrediria entre as duas)

## Change Log

| 2026-07-16 | 0.1 | Draft @sm (FR51) → @po GO (9/10) → @dev (junto da 14.2) → InReview; gate @qa CONCERNS (aprovado) | River/Pax/Dex/Quinn via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### File List

- `packages/ui/src/file-preview-panel.tsx` (NOVO) + `index.ts`
- `apps/desktop/src/renderer/src/App.tsx` — estado/handler/montagem

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:** 1. Code review — leitura continua no Main (fronteira da 8.4); componente puro. OK. 2. Testes — markdown-lite já testado (9 casos); painel é exibição. 3. ACs atendidos. 4. Regressões — verify+smoke PASS; FR35 preservado na nova casa. 5-6. n/a. 7. Docs ok.

**Observação (CONCERNS):** validação visual do fundador pendente (abrir .md e código, toggle, truncamento).
