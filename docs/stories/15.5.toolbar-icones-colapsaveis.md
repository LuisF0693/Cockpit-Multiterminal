# Story 15.5: Toolbar de ícones + canvas maior (colapsáveis)

## Status

Done

## Story

**As a** fundador,
**I want** uma barra de ícones com ações reais e painéis/rodapé colapsáveis,
**so that** eu tenha os atalhos à vista e o canvas ocupe a tela quase inteira quando eu quiser.

## Acceptance Criteria

1. Toolbar de 38px abaixo do header (estilo referência) com ícones + tooltips de AÇÕES REAIS: novo terminal, novo browser · timeline, learnings, agentes · colapsar sidebar/telemetria/rodapé · zoom 100%, configurações — NENHUM ícone morto (FR57).
2. Pill central de prontidão REAL: `{prontos}/{rodando} prontos` (prontos = sessões rodando em idle/done).
3. Sidebar, telemetria e rodapé de sessões colapsáveis (rodapé vira linha fina de 22px com contagem + expandir); estados persistidos nas settings — canvas ganha a tela com tudo colapsado (FR58).
4. `pnpm verify` + `smoke:daemon` PASS.

## Tasks / Subtasks

- [x] Task 1 — `app-toolbar.tsx` (NOVO): `AppToolbar` com grupos separados por divisores, `ToolButton` com estado ativo (toggles), pill de prontidão via `color-mix` do token ok (AC1, AC2)
- [x] Task 2 — Schema: `sidebarCollapsed`/`telemetryCollapsed`/`sessionsBarCollapsed` (catch+default false) + testes atualizados (AC3)
- [x] Task 3 — `SessionCardsBar`: modo colapsado (22px, contagem + ▴) e botão ▾ no expandido (AC3)
- [x] Task 4 — App: estados aplicados no boot, `toggleCollapsed` persiste no clique, `AppSidebar`/`TelemetryPanel` render-condicionais, toolbar montada abaixo do header (AC1, AC3)
- [x] Task 5 — verify + smoke PASS (AC4)

## Dev Notes

- "Mapa do código"/"comparar"/"limpar grafo" da referência continuam FORA (backlog do E13 aguardando elicitação — Artigo IV): a toolbar só ganha ícone quando a ação existir.
- Colapso é render-condicional (sidebar/telemetria não têm estado interno de I/O — seguro desmontar; os TILES continuam sempre montados, gotcha xterm intocado).

## Change Log

| 2026-07-16 | 0.1 | Draft @sm (FR57-58) â†’ @po GO â†’ @dev â†’ InReview; gate @qa CONCERNS (aprovado) | River/Pax/Dex/Quinn via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### File List

- `packages/ui/src/app-toolbar.tsx` (NOVO) + `index.ts`
- `packages/ui/src/session-cards-bar.tsx` — modo colapsado
- `packages/shared/src/ipc.ts` + `ipc.test.ts` — 3 campos novos
- `apps/desktop/src/renderer/src/App.tsx` — toolbar + toggles + renders condicionais

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:** 1. Code review — ações todas reais; toggles persistem por clique; pill derivada de dados vivos. OK. 2. Testes — schema coberto; toolbar é exibição (precedente). 3. ACs atendidos. 4. verify+smoke PASS. 5-6. n/a. 7. Docs ok.

**Observação (CONCERNS):** validação visual pendente (colapsar tudo e medir o ganho de canvas; pill com sessões reais).
