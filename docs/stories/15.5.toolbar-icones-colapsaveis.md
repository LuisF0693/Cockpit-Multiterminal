# Story 15.5: Toolbar de Ã­cones + canvas maior (colapsÃ¡veis)

## Status

Done

## Story

**As a** fundador,
**I want** uma barra de Ã­cones com aÃ§Ãµes reais e painÃ©is/rodapÃ© colapsÃ¡veis,
**so that** eu tenha os atalhos Ã  vista e o canvas ocupe a tela quase inteira quando eu quiser.

## Acceptance Criteria

1. Toolbar de 38px abaixo do header (estilo referÃªncia) com Ã­cones + tooltips de AÃ‡Ã•ES REAIS: novo terminal, novo browser Â· timeline, learnings, agentes Â· colapsar sidebar/telemetria/rodapÃ© Â· zoom 100%, configuraÃ§Ãµes â€” NENHUM Ã­cone morto (FR57).
2. Pill central de prontidÃ£o REAL: `{prontos}/{rodando} prontos` (prontos = sessÃµes rodando em idle/done).
3. Sidebar, telemetria e rodapÃ© de sessÃµes colapsÃ¡veis (rodapÃ© vira linha fina de 22px com contagem + expandir); estados persistidos nas settings â€” canvas ganha a tela com tudo colapsado (FR58).
4. `pnpm verify` + `smoke:daemon` PASS.

## Tasks / Subtasks

- [x] Task 1 â€” `app-toolbar.tsx` (NOVO): `AppToolbar` com grupos separados por divisores, `ToolButton` com estado ativo (toggles), pill de prontidÃ£o via `color-mix` do token ok (AC1, AC2)
- [x] Task 2 â€” Schema: `sidebarCollapsed`/`telemetryCollapsed`/`sessionsBarCollapsed` (catch+default false) + testes atualizados (AC3)
- [x] Task 3 â€” `SessionCardsBar`: modo colapsado (22px, contagem + â–´) e botÃ£o â–¾ no expandido (AC3)
- [x] Task 4 â€” App: estados aplicados no boot, `toggleCollapsed` persiste no clique, `AppSidebar`/`TelemetryPanel` render-condicionais, toolbar montada abaixo do header (AC1, AC3)
- [x] Task 5 â€” verify + smoke PASS (AC4)

## Dev Notes

- "Mapa do cÃ³digo"/"comparar"/"limpar grafo" da referÃªncia continuam FORA (backlog do E13 aguardando elicitaÃ§Ã£o â€” Artigo IV): a toolbar sÃ³ ganha Ã­cone quando a aÃ§Ã£o existir.
- Colapso Ã© render-condicional (sidebar/telemetria nÃ£o tÃªm estado interno de I/O â€” seguro desmontar; os TILES continuam sempre montados, gotcha xterm intocado).

## Change Log

| 2026-07-16 | 0.1 | Draft @sm (FR57-58) â†’ @po GO â†’ @dev â†’ InReview; gate @qa CONCERNS (aprovado) | River/Pax/Dex/Quinn via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 â€” Dex (@dev) via Orion

### File List

- `packages/ui/src/app-toolbar.tsx` (NOVO) + `index.ts`
- `packages/ui/src/session-cards-bar.tsx` â€” modo colapsado
- `packages/shared/src/ipc.ts` + `ipc.test.ts` â€” 3 campos novos
- `apps/desktop/src/renderer/src/App.tsx` â€” toolbar + toggles + renders condicionais

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:** 1. Code review â€” aÃ§Ãµes todas reais; toggles persistem por clique; pill derivada de dados vivos. OK. 2. Testes â€” schema coberto; toolbar Ã© exibiÃ§Ã£o (precedente). 3. ACs atendidos. 4. verify+smoke PASS. 5-6. n/a. 7. Docs ok.

**ObservaÃ§Ã£o (CONCERNS):** validaÃ§Ã£o visual pendente (colapsar tudo e medir o ganho de canvas; pill com sessÃµes reais).
