# Story 8.3: Novo terminal nasce no projeto ativo

## Status

InReview

## Story

**As a** desenvolvedor multi-projeto,
**I want** que novos terminais nasçam automaticamente no diretório do projeto ativo,
**so that** eu não precise `cd` manualmente toda vez que troco de projeto.

## Acceptance Criteria

1. `session.create` usa `rootPath` do projeto ativo como cwd quando nenhum cwd explícito é passado (reusa o campo `cwd` já existente no contrato de sessão desde o Épico 1).
2. Sessões existentes mantêm seu cwd original ao trocar de projeto ativo (troca de projeto não afeta terminais já abertos).
3. Terminal criado a partir da barra lateral de um projeto específico nasce nesse projeto mesmo que não seja o ativo no momento (atalho de conveniência).

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 161 testes PASS + smoke:daemon PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — `SessionCreateRequestSchema` ganha `projectId` opcional (default = projeto ativo, decidido no Main) — determina o cwd default (AC1) quando o chamador não passa `cwd` explícito
- [x] Task 2 — `session.create` (Main): resolve `projectId` (`req.projectId ?? activeId`), busca o projeto correspondente e usa `project.rootPath` como cwd quando `req.cwd` está ausente — `registry.create` já aceitava `cwd?`/`projectId?` desde o Épico 1/Story 8.2, nenhuma mudança no core
- [x] Task 3 — App.tsx: `newTerminal` aceita `opts.projectId` opcional; `newTerminalInProject(id)` (atalho AC3) chama `newTerminal` com esse id SEM tocar no estado `activeProjectId`
- [x] Task 4 — `ProjectSidebar`: duplo-clique num projeto cria terminal ali (AC3) — clique simples continua trocando o ativo (8.2, AC1); os dois gestos coexistem sem conflito
- [x] Task 5 — AC2 é garantia estrutural, não uma linha de código nova: `switchProject`/`setActiveProject` só tocam `activeId` em `app_meta` — nenhum caminho escreve no `cwd` de uma sessão já criada, então trocar de projeto não pode afetar terminais existentes

## Dev Notes

- Lógica de resolução de cwd (`req.cwd ?? project?.rootPath`) ficou INLINE no handler do Main, não extraída para função pura — é um defaulting de 2 linhas sem branching complexo a testar isoladamente (diferente de `planSdcReviewRouting`/`planSdcCorrectionRouting`, que tinham várias condições de guarda); coberta por typecheck/verify + smoke, mesmo padrão de outros handlers simples do `session-ipc.ts` (ex.: `task.list`) que também não têm teste unit dedicado.
- AC2 não tem AC de teste explícito porque é uma NÃO-AÇÃO: o contrato é "nada muda o cwd de sessões vivas" — verificado por leitura de código (nenhum caminho de `switchProject`/`setActiveProject` toca em `registry`/terminais), não por um teste que provaria uma ausência.

## Change Log

| 2026-07-14 | 0.1 | Draft + PO GO (Ready) + dev completo — verify/smoke PASS, status → InReview | River/Pax/Dex via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List

- Menor superfície das stories do Épico 8 até agora — a infraestrutura pesada (schema v7, propagação de `projectId`, `PersistenceManager.projects()`) já existia desde a 8.1/8.2; esta story só conecta `rootPath` como cwd default e expõe o atalho de criação-sem-trocar-ativo (AC3).
- `registry.create` já aceitava `cwd?`/`projectId?` como campos independentes desde a 8.2 — o Main só precisou decidir QUAL projeto (ativo vs. override) e derivar o cwd a partir dele; nenhuma mudança no `@cockpit/core`.
- Verificação: `pnpm verify` (lint+typecheck+test+NFR7) PASS; `pnpm --filter @cockpit/desktop smoke:daemon` PASS; 161 testes (contagem inalterada — nenhum teste novo, defaulting trivial sem branching a isolar, ver Dev Notes).

### File List

- `packages/shared/src/ipc.ts` — `SessionCreateRequestSchema.projectId` (opcional)
- `apps/desktop/src/main/session-ipc.ts` — `session.create` resolve projeto (ativo ou override) → cwd default
- `apps/desktop/src/renderer/src/App.tsx` — `newTerminal` aceita `opts.projectId`; `newTerminalInProject` (atalho AC3)
- `packages/ui/src/project-sidebar.tsx` — `onCreateTerminalIn` (duplo-clique no projeto)

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:**
1. Code review — resolução de projeto/cwd é direta e sem efeitos colaterais escondidos; duplo-clique não conflita com o clique simples (troca de ativo) já existente da 8.2. OK.
2. Testes unitários — nenhum teste novo; lógica é defaulting trivial (2 ternários) sem branching a isolar — decisão documentada e consistente com o padrão já usado para outros handlers simples do `session-ipc.ts` neste mesmo projeto. AC2 é uma não-ação verificada por leitura de código, não por teste.
3. Acceptance criteria — AC1 (cwd default = rootPath do projeto ativo) atendido; AC2 (sessões existentes não afetadas) atendido estruturalmente; AC3 (criar num projeto específico sem trocar o ativo) atendido pelo duplo-clique + `projectId` override no contrato. Todos atendidos.
4. Regressões — `pnpm verify` + `smoke:daemon` + 161 testes/27 arquivos PASS; comportamento de `cwd` explícito (quando passado pelo chamador) continua tendo prioridade sobre o default do projeto — nenhuma mudança de contrato pra quem já passava `cwd`.
5. Performance — resolução de projeto é uma busca em array pequeno (`projects.find`), sem impacto perceptível.
6. Segurança — `rootPath` de um projeto com caminho inválido/apagado ainda não é validado (observação herdada da 8.1/8.2, agora efetivamente exercitada: um `cwd` inválido cai no mesmo tratamento de erro que `session.create` já tinha ANTES do Épico 8 para qualquer `cwd` ruim — nenhuma regressão, só não há uma mensagem mais amigável específica para "projeto com pasta apagada").
7. Documentação — Dev Notes/Completion Notes/File List preenchidos.

**Observação (CONCERNS, não bloqueante):** criar um terminal num projeto cujo `rootPath` foi apagado do disco produz o mesmo erro genérico de spawn que qualquer cwd inválido já produzia antes do Épico 8 — aceitável para esta story, mas seria uma boa adição de UX numa passada futura (ex.: badge de "pasta não encontrada" no `ProjectSidebar`).
