# Story 2.4: Adapter Grok CLI

## Status

InReview

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"

## Story

**As a** desenvolvedor multi-agente,
**I want** rodar o Grok CLI como agente gerenciado,
**so that** os três providers do MVP estejam operacionais em paralelo.

## Acceptance Criteria

1. Adapter Grok CLI implementa o contrato completo com detecção de status.
2. Três agentes de providers distintos rodando simultaneamente, cada um com status correto.
3. Limitações/riscos do CLI no Windows documentados (pergunta aberta do brief respondida).

## Quality Gate Tasks

- [x] Pre-Commit (@dev) — verify 77 testes + NFR7 PASS
- [x] QA Gate (@qa): 7 checks — **CONCERNS (aprovado)**; 3 providers simultâneos com CLIs reais pendente do fundador
- [ ] Pre-PR (@devops): quando houver remote

## Tasks / Subtasks

- [x] Task 1 — GrokAdapter (AC: 1)
  - [x] statusStrategy 'output-parsing': `\r`→working (dedupe) + exit→done/error
  - [x] `detectAvailability()` com razão clara
  - [x] Dispose padrão (rejeita órfão)
- [x] Task 2 — Limitações no Windows documentadas (AC: 3) — `packages/adapters/grok/README.md` (distribuições, ausência de canal nativo, ConPTY ok, NFR6)
- [x] Task 3 — Registro + testes (AC: 2)
  - [x] Registrado (seletor: PowerShell/CMD/Claude Code/Codex/Grok)
  - [x] 4 testes unit com fakes
  - [ ] Manual (fundador): 3 providers simultâneos — **pendente**
  - [x] `pnpm verify` integral (77 testes)

## Dev Notes

**Grok CLI sem canal nativo de status:** diferente do Claude (hooks) e do Codex (notify), as distribuições conhecidas do grok CLI não expõem callbacks de ciclo de vida — o adapter nasce com heurística de input + processo, e `idle` só virá com parsing por fixtures (debt padrão do épico). A UI continua correta: working ao enviar, done/error no fim.

**Reuso:** padrão CodexAdapter sem a ponte de notify.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-14 | 0.1 | Draft (Epic 2, padrão 2.3 sem notify) | River (@sm) via Orion |
| 2026-07-14 | 0.2 | Validação PO: GO 10/10 — Approved | Pax (@po) via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 (claude-fable-5) — Dex (@dev) via Orion

### Debug Log References

- Sem bugs; padrão da 2.3 sem a ponte de notify.

### Completion Notes List

- AC3 respondido no README do adapter: sem canal nativo de status nas distribuições conhecidas → `idle` fica indetectável até parsing com fixtures (debt padrão do épico); heurística de input + exit cobrem o essencial.

### File List

**Criados:** `packages/adapters/grok/{package.json,tsconfig.json,README.md,src/index.ts,src/grok-adapter.ts,src/grok-adapter.test.ts}`
**Modificados:** `packages/pty-host/{package.json,src/host-entry.ts}`, `tsconfig.base.json`, `apps/desktop/electron.vite.config.ts`, `pnpm-lock.yaml`

## QA Results

### Gate: **CONCERNS** (aprovado) — Quinn (@qa) via Orion, 2026-07-14

7 checks PASS (77 testes; NFR6/7; README de limitações = AC3 ✓). Issue única (medium): AC2 — 3 providers simultâneos com CLIs reais pendente do roteiro do fundador (Claude + Codex + Grok lado a lado, dots independentes).
