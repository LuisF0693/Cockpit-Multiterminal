# Story 5.1: Entidades de tarefa com estados de lifecycle

## Status

InReview

## Story

**As a** desenvolvedor multi-agente,
**I want** criar tarefas com estados (planejada → em execução → aguardando decisão → revisada → concluída),
**so that** o trabalho seja mapeado de ponta a ponta, não disperso em terminais.

## Acceptance Criteria

1. CRUD de tarefas com título, descrição e estado; transições de estado válidas impostas pelo core.
2. Transições registradas na timeline com autor e timestamp.
3. Tarefas persistem no state store (sobrevivem a restart).

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 119 testes PASS + smoke:daemon PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — modelo: `TaskState = 'planned'|'in_progress'|'awaiting_decision'|'reviewed'|'done'` (FR13); tabela `tasks` (schema v4, ALTER-free — CREATE TABLE IF NOT EXISTS, mesmo padrão das demais); `PersistedTask {id,title,description,state,createdAt,updatedAt}`
- [x] Task 2 — `StateStore`: `createTask`, `updateTask` (title/description/state), `listTasks()`, `getTask(id)` — sqlite + memory
- [x] Task 3 — `TaskLifecycle` (core, puro): mapa de transições válidas (`planned→in_progress`, `in_progress→awaiting_decision`, `awaiting_decision→{in_progress,reviewed}` — rejeitar volta ao progresso, aprovar avança —, `reviewed→done`, `reviewed→in_progress` opcional p/ retrabalho); `canTransition(from,to)`; lançar em transição inválida (AC1)
- [x] Task 4 — `TaskManager` (core): `create`, `updateState` (valida via task-lifecycle, grava e emite evento), `list`; persistência via WriteQueue (mesmo padrão não-bloqueante — NFR8); evento `task.created`/`task.state_changed` na trilha com autor+timestamp (AC2)
- [x] Task 5 — contrato: `TaskSchema`, `TaskCreateRequestSchema`, `TaskUpdateStateRequestSchema`, `TaskEventSchema` (push), canais `task.create/updateState/list/event`, `CockpitApi.task`
- [x] Task 6 — IPC (Main) + preload
- [x] Task 7 — UI mínima: `TasksPanel` em nova view `'tasks'` (lista + criar + botões de transição válida) — o Board visual completo é a 5.4; aqui só a superfície funcional (AC1/2/3 verificáveis manualmente)
- [x] Task 8 — testes unit: transições válidas/inválidas (task-lifecycle); TaskManager (create/updateState/trilha/persistência sobrevive a "restart"); verify suíte completa PASS (119) + smoke:daemon PASS

## Dev Notes

- Autor da transição (AC2): por ora sempre `'human'` (UI ainda não distingue "sistema moveu automaticamente" — não há tal caso nesta story); campo fica pronto para a 5.3 (decisões).
- Task NÃO se vincula a terminal ainda (isso é a 5.2) — o campo de vínculo fica de fora aqui, propositalmente.
- Schema v4 usa `CREATE TABLE IF NOT EXISTS` (tabela nova, sem coluna a adicionar em tabela existente) — mais simples que os ALTERs de v2/v3.
- UI da 5.1 é utilitária (não é o Board da 5.4) — evita construir a mesma tela duas vezes.

## Change Log

| 2026-07-14 | 0.1/0.2 | Draft + PO GO 10/10 (Ready) + dev start | River/Pax via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List / File List

- TaskManager NÃO segue o split registry/PtyOps do SessionRegistry — tarefas não têm recurso externo (PTY) a decorrer, então estado vivo e persistência vivem juntos na mesma classe (menos indireção, sem sacrificar testabilidade — store já é injetável).
- Bug pego pelos próprios testes (não em produção): os primeiros testes do TaskManager esqueceram `queue.flush()` antes de checar o store diretamente — o padrão desta suíte SEMPRE exige flush explícito (mesmo da persistence.test.ts); corrigido antes do commit.
- Fix de `exactOptionalPropertyTypes` no handler `task.create`: `TaskCreateRequestSchema.parse()` infere `description?: string | undefined` (zod), incompatível com `description?: string` do `TaskManager.create()` sob esse strict flag — resolvido com spread condicional, mesmo padrão já usado no `createPty`.
- UI da 5.1 duplica DELIBERADAMENTE um mapa pequeno de transições (`NEXT_STATES`) em vez de importar `@cockpit/core` — mantém a fronteira de pacotes (`ui` só depende de `shared`); reavaliar se crescer na 5.4 (Board).
- **Criados:** core (`task-lifecycle.ts`+test, `task-manager.ts`+test), ui (`tasks-panel.tsx`). **Modificados:** core (state-store types/sqlite v4/memory/index), shared (`ipc.ts`: Task schemas/canais/CockpitApi), Main (`session-ipc.ts`), preload, ui (`index.ts`), App.tsx (view tasks + handlers).

## QA Results

### Gate: **CONCERNS** (aprovado) — Quinn (@qa), 2026-07-14. 7 checks PASS (119 testes + smoke:daemon PASS; AC1/AC2/AC3 com evidência unit completa — transições válidas/inválidas, trilha com autor+timestamp, sobrevivência a "restart" simulado). Issues low: UI é utilitária por design (Board real é 5.4); duplicação pequena e documentada do mapa de transições na UI (ui↛core); vínculo tarefa↔terminal (5.2) e decisões humanas (5.3) ainda não existem — esperado, escopo desta story é só a entidade. CodeRabbit não executado (WSL indisponível — padrão).
