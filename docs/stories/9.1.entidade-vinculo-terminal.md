# Story 9.1: Entidade de vínculo terminal-a-terminal

## Status

InReview

## Story

**As a** desenvolvedor multi-agente,
**I want** vincular um terminal a outro terminal diretamente pelo canvas ou pela sessão master,
**so that** eu monte cadeias de comando entre agentes sem depender de uma tarefa formal.

## Acceptance Criteria

1. Um vínculo tem `sourceId`, `targetId` e um modo (`manual` — só habilita o botão de enviar; `auto` — dispara sozinho no status do source).
2. Um terminal pode ter múltiplos vínculos de saída (comanda vários alvos) e múltiplos de entrada (é comandado por vários).
3. Vínculo persiste entre reinicializações (mesma garantia do FR10) e é removido automaticamente se origem ou alvo forem fechados/excluídos.
4. Vínculo é escopado ao projeto ativo (Épico 8) — origem e alvo precisam pertencer ao mesmo projeto.

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 176 testes PASS + smoke:daemon PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — schema v8 (SQLite): tabela NOVA `terminal_links` (id, source_id, target_id, mode, project_id, created_at) — `CREATE TABLE IF NOT EXISTS` cobre instalações antigas e novas, nenhum ALTER necessário (mesmo caso da tabela `tasks` na v3→v4); índices em `source_id`/`target_id` para a limpeza em cascata (AC3)
- [x] Task 2 — `TerminalLinkManager` (`@cockpit/core`, NOVO): estado vivo + persistência juntos (mesmo princípio do `TaskManager` da 5.1 — sem PTY a orquestrar); `create()` rejeita auto-vínculo (source===target); `removeForTerminal(id)` remove todo vínculo onde o terminal é origem OU alvo (AC3)
- [x] Task 3 — contrato: `TerminalLinkSchema {id, sourceId, targetId, mode, projectId, createdAt}`, `TerminalLinkCreateRequestSchema`, `TerminalLinkRemoveRequestSchema`, `TerminalLinkEventSchema` (mesmo padrão do `TaskEvent`), canais `terminalLink.*`, `CockpitApi.terminalLink`
- [x] Task 4 — Main (`session-ipc.ts`): `terminalLinkCreate` valida que origem E alvo existem e pertencem ao MESMO `projectId` (AC4) antes de delegar ao manager; NOVO listener em `registry.onEvent` (terceiro desta natureza, junto do de roteamento SDC) que chama `removeForTerminal` em eventos `'closed'` (exclusão definitiva — DIFERENTE de `'exited'`, que pode relançar no restore/adoção)
- [x] Task 5 — testes: 6 casos de `TerminalLinkManager` (create+persist+emit, rejeita auto-vínculo, múltiplos vínculos de saída/entrada — AC2, remove+persist+emit, `removeForTerminal` limpa origem E alvo — AC3, `load()` restaura do boot)

## Dev Notes

- Escopo desta story é BACKEND — nenhuma UI ainda (canvas/master ganham a visualização/interação na 9.3, roteamento automático na 9.2). Mesmo padrão de faseamento já usado no `TaskManager` (5.1 backend → 5.4 board visual).
- `'closed'` vs `'exited'`: a limpeza de vínculos (AC3) só dispara em `'closed'` (fechamento explícito/exclusão definitiva do terminal). Um `'exited'` sozinho (processo morreu mas o terminal ainda existe, pode ser relançado no próximo boot via restore/adoção) NÃO remove o vínculo — faria sentido perder o vínculo só quando o terminal deixa de existir de vez.
- Vínculo terminal-a-terminal (Épico 9) e vínculo tarefa-terminal (5.2/7.1) são conceitos INDEPENDENTES e coexistem — um terminal pode ter ambos ao mesmo tempo (decisão de design já documentada no épico).

## Change Log

| 2026-07-14 | 0.1 | Draft + PO GO (Ready) + dev completo — verify/smoke PASS, status → InReview | River/Pax/Dex via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List

- `TerminalLinkManager` é uma cópia estrutural quase direta do `TaskManager` (mesmo padrão: `Map` vivo + `WriteQueue` para persistência + `onEvent`/`emit`) — reuso de padrão arquitetural já validado, não código copiado literalmente (as entidades são diferentes).
- AC4 (mesmo projeto) validado no MAIN (não no `TerminalLinkManager`) — o manager não conhece `SessionRegistry`/sessões, só recebe `projectId` já resolvido; a validação de que origem e alvo pertencem ao mesmo projeto exige olhar as duas sessões vivas, responsabilidade do Main (mesmo princípio de "core não conhece Electron/registry" já seguido no roteamento SDC do Épico 7).
- Terceiro listener em `registry.onEvent` desta sessão (junto do de persistência e do de roteamento SDC) — `emit()` do registry não isola exceções entre listeners, então o novo listener segue o MESMO cuidado de try/catch já estabelecido para os outros dois.
- Verificação: `pnpm verify` (lint+typecheck+test+NFR7) PASS; `pnpm --filter @cockpit/desktop smoke:daemon` PASS; `npx turbo run test --force` → **176 testes / 29 arquivos** (era 170 na 8.4; +6 novos de `TerminalLinkManager`).

### File List

- `packages/core/src/state-store/types.ts` — `TerminalLinkMode`, `PersistedTerminalLink`, métodos novos na interface `StateStore`
- `packages/core/src/state-store/sqlite-state-store.ts` — schema v8 (tabela `terminal_links`), CRUD
- `packages/core/src/state-store/memory-state-store.ts` — mirror do acima
- `packages/core/src/terminal-link-manager.ts` (novo) — `TerminalLinkManager`
- `packages/core/src/terminal-link-manager.test.ts` (novo) — 6 testes
- `packages/core/src/index.ts` — exports novos
- `packages/shared/src/ipc.ts` — `TerminalLinkModeSchema`, `TerminalLinkSchema`, requests, `TerminalLinkEventSchema`, canais, `CockpitApi.terminalLink`
- `apps/desktop/src/main/session-ipc.ts` — `TerminalLinkManager` instanciado+`load()`+push; listener de limpeza em `'closed'`; handlers `terminalLink.create/remove/list`
- `apps/desktop/src/preload/index.ts` — `terminalLink.*` (Zod na borda)

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:**
1. Code review — reuso de padrão arquitetural do `TaskManager` consistente; validação de AC4 corretamente colocada no Main (não no manager, que não tem acesso a sessões); listener de limpeza segue o padrão de try/catch já estabelecido. OK.
2. Testes unitários — 6 casos cobrindo criação/persistência/emissão, rejeição de auto-vínculo, múltiplos vínculos (AC2), remoção/persistência/emissão, limpeza em cascata como origem E alvo (AC3), restauração no boot. Handlers do Main (validação de mesmo-projeto, listener 'closed') não têm teste unit dedicado — mesmo precedente de todos os outros handlers Electron-coupled deste projeto.
3. Acceptance criteria — AC1 (sourceId/targetId/mode) atendido pelo schema+manager; AC2 (múltiplos vínculos) atendido e testado explicitamente; AC3 (persistência+limpeza automática) atendido — persistência pela mesma garantia do FR10, limpeza testada via `removeForTerminal`; AC4 (mesmo projeto) atendido pela validação no handler `terminalLinkCreate`. Todos atendidos.
4. Regressões — `pnpm verify` + `smoke:daemon` + 176 testes/29 arquivos PASS; schema v8 é `CREATE TABLE IF NOT EXISTS` (mesmo padrão zero-risco da tabela `tasks`), sem ALTER em tabelas existentes.
5. Performance — operações são O(1)/O(n pequeno) sobre um Map em memória + índices SQLite nas colunas de busca (`source_id`/`target_id`), sem impacto perceptível.
6. Segurança — nenhuma superfície nova de risco; validação de mesmo-projeto prevê vazamento acidental de vínculo entre projetos diferentes.
7. Documentação — Dev Notes/Completion Notes/File List preenchidos; distinção `'closed'` vs `'exited'` documentada explicitamente (não é óbvia à primeira leitura).

**Observação (CONCERNS, não bloqueante):** nenhuma UI ainda (esperado — 9.2/9.3 cobrem roteamento automático e visualização/interação); validação manual do fundador com terminais reais fica pendente até a 9.3 expor a funcionalidade de ponta a ponta.
