# Story 8.1: Entidade Projeto (cadastro, persistência, ativo)

## Status

InReview

## Story

**As a** desenvolvedor multi-projeto,
**I want** cadastrar projetos com nome, cor e caminho raiz no disco,
**so that** eu possa organizar meu trabalho por projeto real, não só por rótulo dentro de um único repo.

## Acceptance Criteria

1. Um projeto tem `id`, `name`, `color` e `rootPath` (caminho absoluto validado no disco).
2. O sistema mantém um projeto "ativo" por vez (análogo ao workspace ativo da 3.6); a primeira execução do app cria um projeto "Padrão" apontando para o cwd atual (sem quebrar instalações existentes).
3. Projetos persistem entre reinicializações (mesma garantia do FR10/app_meta) e sobrevivem a crash (mesma trilha de recuperação da 4.x).
4. CRUD básico (criar, renomear, trocar cor, remover — exceto o último projeto restante) exposto via IPC com Zod na borda (mesmo padrão de workspace da 3.6).

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 160 testes PASS + smoke:daemon PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — contrato: `ProjectSchema {id, name, color, rootPath}`, `ProjectListSchema {projects[], activeId}`, requests de create/update/remove/setActive, canais `project.*`, `CockpitApi.project` — mesmo padrão de `workspace.*` da 3.6
- [x] Task 2 — `PersistenceManager` (core): `projects()`/`createProject()`/`updateProject()`/`removeProject()`/`setActiveProject()`/`ensureDefaultProject()` sobre `app_meta.projects` (JSON) + `app_meta.active_project` — MESMO mecanismo de `getMeta`/`setMeta` já usado por workspaces (3.6), sem tabela nova
- [x] Task 3 — `ensureDefaultProject(cwd)` chamado uma vez no boot (Main, junto do `markBootStart` da 4.3) com o MESMO cwd que sessões sem projeto já usavam (`session-registry.ts`, `opts.cwd ?? process.cwd()`) — zero regressão, idempotente (no-op se já existe projeto)
- [x] Task 4 — handlers IPC no Main (`session-ipc.ts`) + preload (Zod na borda nas duas pontas)
- [x] Task 5 — testes: `ensureDefaultProject` idempotente, CRUD completo (create/update com campos parciais/setActive), `removeProject` rejeita o último restante (AC4), ativo cai pro primeiro restante quando o removido era o ativo

## Dev Notes

- "Projeto" (Épico 8) e "workspace" (Story 3.6) são conceitos DIFERENTES e ambos permanecem: projeto é o caminho raiz no disco; workspace continua sendo o agrupamento livre de tiles DENTRO de um projeto. Esta story NÃO liga as duas coisas ainda (isso é a 8.2/8.3) — só cria a entidade projeto isoladamente.
- Nenhuma UI nesta story (a barra lateral é a 8.2) — escopo estritamente backend/contrato, como o `TaskManager` da 5.1 antes do board visual da 5.4.
- `updateProject` combina rename+recolor+reroot num único endpoint (campos opcionais) em vez de 3 endpoints separados — mais simples que o padrão de workspace (que tem `rename` dedicado) porque projeto tem mais campos mutáveis.

## Change Log

| 2026-07-14 | 0.1 | Draft + PO GO (Ready) + dev completo — verify/smoke PASS, status → InReview | River/Pax/Dex via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List

- Reusa 100% o mecanismo de persistência já existente para workspaces (`getMeta`/`setMeta` sobre `app_meta`, JSON blob) — nenhuma migração de schema SQLite necessária, nenhuma tabela nova.
- `ensureDefaultProject` usa o MESMO cwd de fallback que sessões já usavam (`process.cwd()` do processo Main) — o projeto "Padrão" reproduz exatamente o comportamento pré-Épico-8, sem quebrar nenhuma sessão/instalação existente.
- Gotcha de typecheck (mesmo padrão da 7.3): `ProjectUpdateRequestSchema.parse()` infere campos opcionais como `string | undefined`, incompatível com `exactOptionalPropertyTypes` — corrigido com spread condicional no handler do Main, não no core (a assinatura de `updateProject` em `PersistenceManager` já usa `field?: string`, correta para quem chama com JS puro/testes).
- Verificação: `pnpm verify` (lint+typecheck+test+NFR7) PASS; `pnpm --filter @cockpit/desktop smoke:daemon` PASS; core foi de 156→160 testes (+4 novos: idempotência do default, CRUD, rejeição do último projeto, fallback de ativo).

### File List

- `packages/shared/src/ipc.ts` — `ProjectSchema`, `ProjectListSchema`, `Project*RequestSchema` (create/update/remove/setActive), canais `project.*`, `CockpitApi.project`
- `packages/core/src/state-store/persistence.ts` — `ensureDefaultProject`/`projects`/`createProject`/`updateProject`/`removeProject`/`setActiveProject`/`parseProjectsMeta`
- `packages/core/src/state-store/persistence.test.ts` — 4 testes novos (`describe('projetos (Story 8.1, FR21)')`)
- `apps/desktop/src/main/session-ipc.ts` — `ensureDefaultProject` no boot + 5 handlers IPC
- `apps/desktop/src/preload/index.ts` — `project.*` (Zod na borda)

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:**
1. Code review — segue o padrão idêntico de workspace (3.6): getMeta/setMeta, fallback defensivo no getter (`activeId` cai pro primeiro projeto se o meta apontar pra um id inexistente, mesmo princípio do `active: 'Geral'` fallback). OK.
2. Testes unitários — 4 casos novos cobrindo AC2 (default idempotente), AC4 (CRUD + rejeição do último projeto + fallback de ativo na remoção). `parseProjectsMeta` (parsing defensivo de JSON corrompido) não tem teste dedicado — mesmo precedente de `parseWorkspacesMeta`, que também não tem. OK.
3. Acceptance criteria — AC1 (entidade com 4 campos) atendido pelo schema; AC2 (ativo + default no primeiro boot) atendido por `ensureDefaultProject`+`projects()`; AC3 (persistência) atendido pelo mesmo mecanismo garantido do FR10 (app_meta já sobrevive a restart/crash desde a 1.4/4.x — nenhuma trilha nova precisa ser testada aqui); AC4 (CRUD com proteção do último projeto) atendido. Todos atendidos.
4. Regressões — `pnpm verify` + `smoke:daemon` + 160 testes/27 arquivos PASS; `ensureDefaultProject` comprovadamente não muda o cwd de sessões existentes (usa o mesmo fallback de sempre).
5. Performance — operações são leituras/escritas de um JSON pequeno em app_meta (mesmo custo de workspaces), sem impacto perceptível.
6. Segurança — `rootPath` não é validado como caminho EXISTENTE no disco nesta story (só `min(1)` no schema) — aceitável para 8.1 (entidade pura), mas a 8.2/8.3 (seletor de pasta nativo, novo terminal usa o rootPath como cwd) precisarão lidar com um `rootPath` inválido/apagado — anotado como observação, não bloqueante aqui.
7. Documentação — Dev Notes/Completion Notes/File List preenchidos.

**Observação (CONCERNS, não bloqueante):** `rootPath` não é validado contra o disco nesta story (fica para 8.2, que já usa diálogo nativo de pasta — validação fica natural lá). Nenhuma UI ainda — puramente backend, consistente com o escopo declarado da story.
