# Story 1.3: Grid de múltiplos terminais

## Status

Approved

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["coderabbit", "qa-gate-checklist", "load-test-6-ptys"]

## Story

**As a** desenvolvedor multi-agente,
**I want** criar, nomear, redimensionar e fechar vários terminais em um grid,
**so that** eu opere múltiplos CLIs lado a lado em um único app.

## Acceptance Criteria

1. Usuário cria novos terminais (botão e atalho), cada um com PTY independente.
2. Terminais podem ser nomeados/renomeados e exibem cabeçalho com nome.
3. Grid permite reorganizar e redimensionar tiles; ≥ 6 terminais simultâneos estáveis e interativos (NFR3).
4. Fechar tile individual não afeta os demais.
5. Atalhos de teclado para alternar foco entre terminais.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Feature (multi-sessão + layout de canvas)
**Secondary Type(s)**: Frontend (interações de tile, atalhos)
**Complexity**: High

### Specialized Agent Assignment

**Primary Agents**:
- @dev (implementação)

**Supporting Agents**:
- @qa (gate + teste de carga 6+ PTYs)
- @ux-design-expert (consulta se interações do canvas desviarem do spec)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run before marking story complete
- [ ] QA Gate (@qa): 7 quality checks + teste de carga com 6+ terminais interativos
- [ ] Pre-PR (@devops): Run before creating pull request

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 30 minutes
- Severity Filter: [CRITICAL, HIGH]

**Predicted Behavior**:
- CRITICAL issues: auto_fix (máx. 2 iterações, depois HALT)
- HIGH issues: auto_fix na 1ª iteração; depois documentar como debt

### CodeRabbit Focus Areas

**Primary Focus**:
- Gestão de ciclo de vida de N sessões (criação/fechamento independentes — AC1/AC4, sem leaks)
- Performance de render com múltiplos xterm ativos (throttle de tiles desfocados — front-end spec)

**Secondary Focus**:
- Acessibilidade de teclado (atalhos, tab order, focus ring — princípios do spec)
- Estado do layout serializável (preparação para 1.4, sem persistir ainda)

## Tasks / Subtasks

- [ ] Task 1 — Session Manager em `packages/core` (AC: 1, 4)
  - [ ] Registro de sessões: id (ulid), nome, cwd, estado do processo; API create/rename/close/list
  - [ ] Eventos de domínio internos (terminal.created/renamed/closed) — base para timeline futura (E3)
  - [ ] Fechamento individual: dispose do PTY correspondente (padrão isPidAlive da 1.2) sem tocar nas demais sessões
  - [ ] Unit tests do registro (mock do pty-host)
- [ ] Task 2 — Canvas de tiles em `packages/ui` (AC: 2, 3)
  - [ ] Modelo de layout **serializável** (por tile: id, x, y, width, height, zIndex) — persistência entra na 1.4, o modelo nasce pronto
  - [ ] **Canvas com liberdade de arranjo** (Design References do fundador): tiles móveis por drag no cabeçalho, redimensionáveis pelas bordas, tamanhos livres com mínimo 320×200, snap opcional a grade de 8px
  - [ ] TerminalTile (organism do spec): cabeçalho com nome editável (duplo-clique), botão fechar, área xterm (TerminalView da 1.2)
  - [ ] Botão "+ novo terminal" (tile fantasma) e render com taxa reduzida para tiles desfocados (spec de performance)
- [ ] Task 3 — Sidebar de navegação em árvore (AC: 2)
  - [ ] Sidebar colapsável com lista/árvore das sessões abertas (nome + indicador básico); clique foca o tile
  - [ ] Escopo mínimo: só sessões nesta story (tarefas/documentos entram nos épicos 3/5)
- [ ] Task 4 — Atalhos de teclado (AC: 1, 5)
  - [ ] `Ctrl+N` novo terminal; `Ctrl+1..9` foca terminal N; `Ctrl+W` fecha o focado (com confirmação se processo ativo)
  - [ ] Foco visual claro no tile ativo (focus ring do spec); digitação vai sempre ao terminal focado
  - [ ] Registro central de atalhos (mapa único, sem listeners espalhados)
- [ ] Task 5 — Testes e validação de carga (AC: 3, 4)
  - [ ] Unit: modelo de layout (mover/redimensionar/limites), registro de atalhos
  - [ ] Integração: criar 6 sessões reais → interagir com todas → fechar uma → demais intactas (AC4) → fechar todas sem órfãos
  - [ ] Validação manual: 6+ terminais com TUIs ativas, digitação fluida (NFR3), drag/resize sem stutter
  - [ ] `pnpm verify` completo passando

## Dev Notes

> Contexto dos shards + decisões do fundador — não reler arquitetura.

**⛔ Dependência dura: Story 1.2 concluída.** Esta story MULTIPLICA o que a 1.2 cria (TerminalView + canal binário MessagePort + pty-host). Cada novo terminal = nova sessão no pty-host + novo par de MessagePorts negociado pelo Main. Não redesenhar esse fluxo — replicá-lo por sessão.

**Direção de design do fundador (front-end-spec.md → Design References):** o "grid" é um **canvas com liberdade de arranjo** — tiles móveis, tamanhos livres, snap opcional — como nas capturas em `docs/design-references/`. NÃO implementar grid rígido de células. Sidebar é **árvore de navegação** (sessões nesta story), colapsável, mais larga que rail de ícones.

**Session Manager (docs/architecture/components.md):** vive em `packages/core` (Main process). É a fonte de verdade do registro de sessões; a UI reflete eventos, nunca mantém estado próprio de sessões (CQRS leve — coding standards). O pty-host executa; o core registra e coordena.

**Modelo de layout serializável (preparação 1.4):** estrutura pura JSON-friendly (sem classes/funções), com versão de schema (`layoutVersion: 1`). A 1.4 vai gravá-lo no state store — aqui ele vive em memória (Zustand no renderer, espelho no core via eventos).

**Performance (NFR3 + front-end spec):** ≥ 6 xterm ativos → WebGL addon já vem da 1.2; adicionar throttle de render para tiles sem foco; drag/resize nunca pode bloquear o input do terminal focado. Números de referência do spike: 6 PTYs ≈ 141MB RSS estáveis.

**Interações do TerminalTile (front-end spec):** cabeçalho sempre visível (nome, futuro status/adapter — placeholder de status entra no E2); drag pelo cabeçalho; `Ctrl+Enter` expande (Focus View completo é do E3 — aqui só maximizar/restaurar simples se trivial, senão deixar de fora).

**Fora desta story:** persistência de layout (1.4), adapters/status coloridos (E2), Focus View com contexto (E3), Dashboard master (E3).

### Testing

- **Unit:** `packages/core/src/**/*.test.ts` (session registry), `packages/ui` (modelo de layout — lógica pura testável sem DOM).
- **Integração:** ciclo multi-sessão com PTYs reais (spawn 6 → interact → close 1 → assert 5 vivas → close all → 0 órfãos via isPidAlive).
- **Manual:** roteiro de carga — 6+ tiles com TUI, digitação, drag/resize, atalhos Ctrl+1..9/Ctrl+N/Ctrl+W.
- **Comando:** `pnpm verify` integral.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-10 | 0.1 | Draft criado (antecipado) com direção de canvas das Design References | River (@sm) |
| 2026-07-10 | 0.2 | Validação PO: GO 10/10 — Approved (implementação após 1.2) | Pax (@po) |

## Dev Agent Record

### Agent Model Used

_(preenchido pelo @dev)_

### Debug Log References

_(preenchido pelo @dev)_

### Completion Notes List

_(preenchido pelo @dev)_

### File List

_(preenchido pelo @dev)_

## QA Results

_(preenchido pelo @qa)_
