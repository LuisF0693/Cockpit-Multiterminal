# Story 6.4: Ciclo de vida e segurança do daemon

## Status

InReview

## Story

**As a** desenvolvedor multi-agente,
**I want** governança do daemon (heartbeat, stop explícito, upgrade sem queda, ACL),
**so that** a camada extra não vire fonte de órfãos nem superfície de ataque.

## Acceptance Criteria

1. `cockpit-daemon --stop` encerra graciosamente: dispose de todos os PTYs, 0 órfãos (critério (d) do spike).
2. Heartbeat/handshake com versão de protocolo: cliente incompatível recebe erro claro (sem travar sessões).
3. Pipe com ACL restrita ao usuário atual (DACL default de named pipe validada por teste/diagnóstico documentado).
4. Indicador de status do daemon na UI (conectado/reconectando/subindo) + reconexão automática do Main com backoff.

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 102 testes PASS + smoke:daemon PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — `--stop` (AC1): daemon-entry ganha modo stop (conecta como cliente, `shutdown`, reporta órfãos, exit code); `stopDaemon(pipe)` exportada p/ teste; integração: create → stop → 0 órfãos → pipe morto
- [x] Task 2 — heartbeat (AC2): `ping {requestId}` → `pong {requestId, daemonPid, sessions, protocolVersion}` no protocolo + `DaemonClient.ping()`; handshake versionado da 6.1 (hello-error) JÁ cobre cliente incompatível — teste existente
- [x] Task 3 — reconexão com backoff (AC4): DaemonManager detecta queda inesperada → `reconnecting` → tentativas 500ms→8s; sucesso = re-attach de cada proxy com `tailBytes: 0` (resubscreve SEM duplicar replay; sessão sumida → session-exit −1); exaustão = `disconnected` + hostExit (marca exited — comportamento clássico); proxies usam `this.client` dinâmico (sobrevivem à troca de conexão)
- [x] Task 4 — status na UI (AC4): canal push `daemon.status` (Zod) + preload `cockpit.daemon.onStatus` + badge no header do App (visível quando ≠ connected: subindo/reconectando/desconectado)
- [x] Task 5 — ACL (AC3): documentação na decisão crítica 5 — DACL default de named pipe (write só p/ usuário criador, Admins e SYSTEM ⇒ outros usuários não conectam pipe duplex) + defesa em profundidade: handshake obrigatório antes de qualquer comando (6.1) + validação manual documentada; SDDL explícito = debt consciente
- [x] Task 6 — testes: ping/pong; stop gracioso com 0 órfãos e pipe morto depois; attach com tailBytes 0 não replayça; latência da 6.2 endurecida (melhor-de-3 — flake real visto sob suíte paralela); verify suíte completa PASS (102)

## Dev Notes

- Re-attach pós-reconexão usa `tailBytes: 0` — o renderer JÁ tem o histórico; replay duplicaria output no xterm.
- Reconexão distingue cenários: daemon vivo com conexão quebrada (re-attach ok), daemon reiniciado vazio (attach ok=false → exited), daemon morto (exaustão → hostExit).
- ACL: node/libuv cria o pipe com security descriptor default do token — sem GENERIC_WRITE para Everyone; conexão duplex exige read+write ⇒ bloqueada p/ outros usuários não-admin. Hardening custom (SDDL explícito) fica como debt documentado (exigiria native addon).
- Badge default 'connected' no renderer (modo utilityProcess nunca emite → badge oculto).

## Change Log

| 2026-07-14 | 0.1/0.2 | Draft + PO GO 10/10 (Ready) + dev start | River/Pax via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List / File List

- Reconexão diferencia 3 cenários: conexão quebrada (re-attach tail 0), daemon reiniciado vazio (attach ok=false → exited), daemon morto (exaustão → hostExit clássico).
- Flake previsto pela Quinn no gate da 6.2 MATERIALIZOU sob suíte paralela — asserção de latência virou melhor-de-3 (o AC mede o túnel, não o engasgo do shell).
- Badge default 'connected' no renderer: modo utilityProcess nunca emite → badge oculto.
- **Criados:** pty-host `daemon-lifecycle.integration.test.ts`. **Modificados:** pty-host (protocol ping/pong, server, client, entry --stop/stopDaemon), daemon-manager (backoff/estados/proxies dinâmicos), shared (DaemonStatus/canal), preload, index.ts (broadcast), App.tsx (badge), transcript-test (latência), decisão crítica 5 (ACL).

## QA Results

### Gate: **CONCERNS** (aprovado) — Quinn (@qa), 2026-07-14. 7 checks PASS (102 testes + smoke:daemon PASS; AC1/AC2 com evidência de integração; AC4 por revisão de código — reconexão não é automatizável em vitest sem Electron; AC3 documentado com racional técnico do DACL). Issues low: reconexão com backoff sem teste automatizado (validação manual do fundador: matar o daemon com o app aberto); SDDL explícito como debt; CodeRabbit não executado (WSL indisponível — padrão).
