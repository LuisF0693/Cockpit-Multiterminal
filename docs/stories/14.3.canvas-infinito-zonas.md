# Story 14.3: Canvas infinito (pan+zoom) + zonas de projeto

## Status

Done

## Story

**As a** fundador,
**I want** navegar o canvas arrastando o fundo, com todos os projetos visÃ­veis em zonas coloridas,
**so that** eu enxergue e organize a topologia inteira dos meus projetos de uma vez, como no mockup.

## Acceptance Criteria

1. Mundo transformado (`translate(pan) scale(zoom)`, origin 0 0): arrastar o FUNDO faz pan; wheel dÃ¡ zoom [0.35, 1.6] (sobre um tile, wheel puro pertence ao scrollback do xterm â€” zoom ali exige Ctrl) (FR49).
2. Terminais de TODOS os projetos visÃ­veis; cada projeto com terminais ganha ZONA (retÃ¢ngulo `radius.xl`, borda `{cor}44`, fundo `{cor}12`, etiqueta pill com dot+nome+contagem) do bounding box + padding 24/label 30 (mock 631-645) (FR49).
3. Projeto ativo = destaque (zona `66`/`1A`) e escopo de criaÃ§Ã£o â€” trocar de projeto NÃƒO esconde os demais; workspace (3.6) continua filtrando.
4. Drag de tile/resize/vÃ­nculo continuam 1:1 (deltas/zoom â€” pan nÃ£o afeta deltas); `pointerToCanvasCoords` vira pan-aware; minimapa deriva o viewport de pan/zoom e clicar CENTRALIZA o pan no tile.
5. Tiles NUNCA desmontam (gotcha xterm 1.3) â€” pan/zoom/zonas sÃ£o transformaÃ§Ã£o visual.

## Tasks / Subtasks

- [x] Task 1 â€” `canvasPan` (estado+ref) + gesto de pan (pointerdown no fundo â€” ignora `[data-tile-id]`/`[data-no-pan]` â€” + listeners globais); section `overflow:hidden` (AC1)
- [x] Task 2 â€” Wheel-zoom com a regra do xterm (Ctrl sobre tiles); clamp [0.35, 1.6] (faixa do mock; pill do header idem) (AC1)
- [x] Task 3 â€” Mundo `translate+scale` (mÃ­n. 2600Ã—1800, cresce com o extent); `pointerToCanvasCoords` = `(client - rect - pan) / zoom`; viewport do minimapa = `-pan/zoom` + client/zoom; `focusAndScrollTo` vira `setCanvasPan` centralizador (AC1, AC4)
- [x] Task 4 â€” `zoneViews` (bounding box por projeto sobre sessÃµes do workspace ativo + browser tiles) renderizadas atrÃ¡s de tudo com etiqueta pill; ativo mais forte (AC2, AC3)
- [x] Task 5 â€” Filtro por projeto REMOVIDO da visibilidade do canvas/minimapa (workspace mantido); lavagem de cor por projeto ativo da 12.6 substituÃ­da pelo chÃ£o neutro do mock (zonas dÃ£o a cor); `canvasVisibleSessions` novo (AC2, AC3, AC5)
- [x] Task 6 â€” `pnpm verify` PASS + `smoke:daemon` PASS

## Dev Notes

- Deltas de drag NÃƒO precisaram de termo de pan: pan Ã© translaÃ§Ã£o constante durante o gesto â€” sÃ³ o zoom divide (mesma matemÃ¡tica da 12.6). Apenas conversÃµes de POSIÃ‡ÃƒO absoluta (pointerâ†’mundo, viewport do minimapa, centralizar) ganharam o termo.
- A lavagem de fundo por projeto ativo (12.6) foi deliberadamente aposentada: com zonas por projeto, duas tintas simultÃ¢neas brigariam â€” o mock tem chÃ£o neutro. `canvasBackground(color)` continua existindo pra uso futuro.
- `settings.canvasDefaultZoom` (13.5) tem faixa 0.4â€“2 no schema; o clamp novo [0.35, 1.6] absorve â€” anotar alinhamento do schema como micro-dÃ©bito.

## Change Log

| 2026-07-16 | 0.1 | Draft @sm (FR49) â†’ @po GO (9/10) â†’ @dev â†’ InReview; gate @qa CONCERNS (aprovado) | River/Pax/Dex/Quinn via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 â€” Dex (@dev) via Orion

### File List

- `apps/desktop/src/renderer/src/App.tsx` â€” pan/zoom/mundo/zonas/filtros/minimapa

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:** 1. Code review â€” gesto de pan nÃ£o colide com tiles (guarda por closest); zonas pointer-events none; nenhuma mudanÃ§a de montagem. OK. 2. Testes â€” transformaÃ§Ã£o visual sem lÃ³gica pura nova extraÃ­vel alÃ©m do que layout.ts jÃ¡ cobre. 3. ACs atendidos. 4. RegressÃµes â€” verify+smoke PASS; workspace filter preservado. 5. Performance â€” pan re-renderiza por pointermove (estado) â€” aceitÃ¡vel (mesma classe do drag de tile); otimizaÃ§Ã£o por rAF fica como melhoria futura se o fundador sentir. 6. n/a. 7. Docs ok.

**ObservaÃ§Ã£o (CONCERNS):** validaÃ§Ã£o visual manual pendente (pan com 6+ tiles reais, zoom sobre xterm com Ctrl, zonas com 2+ projetos).
