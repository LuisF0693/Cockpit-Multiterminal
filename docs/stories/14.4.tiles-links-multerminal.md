# Story 14.4: Tiles e vÃ­nculos estilo Multerminal

## Status

Done

## Story

**As a** fundador,
**I want** tiles e links visualmente idÃªnticos ao mockup (barra compacta, pontos de conexÃ£o, bezier animado com etiqueta),
**so that** a interaÃ§Ã£o de vincular terminais seja tÃ£o legÃ­vel quanto na referÃªncia.

## Acceptance Criteria

1. Tile: raio 9, borda `border.strong` (ciana quando VINCULANDO), sombra por foco (sem anel); barra de tÃ­tulo 30px com `>_` ciano, dot de status PULSANTE (`cockpit-pulse-dot`), nome editÃ¡vel, badge do papel REAL (ESCRITOR violeta / REVISOR laranja â€” sÃ³ com `taskRole`), chip do adapter (cor de identidade 12.4), botÃµes â¤¢ maximizar e Ã— fechar; rodapÃ© 24px com cwd real + adapterId (FR50).
2. Pontos de conexÃ£o nas 4 bordas (10px, borda ciana; inferior Ã¢mbar como o mock): arrastar cria vÃ­nculo (gesto primÃ¡rio; Alt+drag continua); durante o arraste, linha tracejada ciana + hint "Solte sobre outro terminal para conectar Â· esc cancela"; Esc cancela (FR50).
3. VÃ­nculos: bezier horizontal tracejado ANIMADO (`cockpit-dashflow`) em paleta cÃ­clica, bolinha percorrendo o caminho (`cockpit-flowmove`, offset-path), etiqueta central com o modo (`manual`/`auto`) e botÃ£o Ã— vermelho no meio que REMOVE o vÃ­nculo direto no canvas; Ã¢ncoras nas laterais a y+15 (FR50).
4. 8 alÃ§as de resize (9px, borda ciano-claro) visÃ­veis sÃ³ no tile focado â€” n/w movem a origem junto (clamp local nos mÃ­nimos pra origem nÃ£o derivar).

## Tasks / Subtasks

- [x] Task 1 â€” `TerminalTile` reescrito na anatomia do mock (linhas 149-186/600-626): wrapper `overflow:visible` (dots/alÃ§as ficam FORA do frame) + frame interno com o conteÃºdo; header/rodapÃ©/badge/chip; maximizar via prop (AC1)
- [x] Task 2 â€” Resize generalizado pra 8 alÃ§as (edges n/e/s/w combinadas; `onMove`+`onResizeTile` juntos nas bordas n/w) com clamp local em `MIN_TILE_WIDTH/HEIGHT` (AC4)
- [x] Task 3 â€” Keyframes `cockpit-dashflow`/`cockpit-flowmove`/`cockpit-pulse-dot` no `StatusPulseStyles` (provider global Ãºnico) (AC1, AC3)
- [x] Task 4 â€” App.tsx: SVG de vÃ­nculos re-desenhado (bezier + etiqueta + Ã— com `pointer-events:all` e `data-no-pan`); TODOS os vÃ­nculos visÃ­veis (zonas da 14.3); `LINK_PALETTE` cÃ­clica; hint de arraste + Esc; `toggleMaximizeTile` (retÃ¢ngulo anterior em ref, ocupa o viewport atual pan+zoom-aware) (AC2, AC3)
- [x] Task 5 â€” `pnpm verify` PASS (25/25 + NFR7) + `smoke:daemon` PASS

## Dev Notes

- O `<section>` do tile virou wrapper `overflow:visible` com frame interno `inset:0` â€” os pontos de conexÃ£o e alÃ§as vivem a -5px pra FORA da borda (mock); o frame mantÃ©m `overflow:hidden` pro conteÃºdo.
- Cor do vÃ­nculo Ã© POSICIONAL (Ã­ndice na lista, paleta cÃ­clica) â€” a entidade `TerminalLink` nÃ£o ganhou coluna de cor (aditivo visual puro; persistir cor Ã© decisÃ£o futura se o fundador pedir estabilidade).
- `stopPropagation` adicionado aos pointerdown do tile â€” necessÃ¡rio agora que o section pai faz PAN (antes o bubble era inofensivo).
- Maximizar NÃƒO persiste o "anterior" (ref) â€” relanÃ§ar o app volta ao layout salvo normal; aceito (mock tambÃ©m Ã© efÃªmero).

## Change Log

| 2026-07-16 | 0.1 | Draft @sm (FR50) â†’ @po GO (9/10) â†’ @dev â†’ InReview; gate @qa CONCERNS (aprovado) | River/Pax/Dex/Quinn via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 â€” Dex (@dev) via Orion

### File List

- `packages/ui/src/terminal-tile.tsx` â€” reescrito (anatomia mock, 8 alÃ§as, dots de conexÃ£o, maximizar)
- `packages/ui/src/status-colors.tsx` â€” keyframes globais novos
- `apps/desktop/src/renderer/src/App.tsx` â€” SVG bezier/etiqueta/remoÃ§Ã£o, hint+Esc, toggleMaximizeTile, props novas

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:** 1. Code review â€” gestos isolados por stopPropagation (novo requisito com o pan); remoÃ§Ã£o de vÃ­nculo reusa `terminalLink.remove` existente; nada de entidade nova. OK. 2. Testes â€” interaÃ§Ã£o visual (precedente das 12.2/12.5); lÃ³gica de resize por bordas Ã© espelho da matemÃ¡tica do mock com clamp dos tokens de layout testados. 3. ACs atendidos. 4. RegressÃµes â€” verify+smoke PASS; Alt+drag preservado; FR9 (pulso Ã¢mbar) preservado. 5. Performance â€” animaÃ§Ãµes CSS puras (sem JS por frame). 6. n/a. 7. Docs â€” decisÃµes (cor posicional, maximizar efÃªmero) documentadas.

**ObservaÃ§Ã£o (CONCERNS):** validaÃ§Ã£o visual manual pendente (arrastar pelos dots, remover pelo Ã—, maximizar, resize pelas 8 alÃ§as em zoom â‰  100%). `offset-path` em SVG exige Chromium moderno â€” Electron 31 ok.
