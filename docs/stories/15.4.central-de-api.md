# Story 15.4: Central de API (chaves no keychain do SO)

## Status

Done

## Story

**As a** fundador,
**I want** cadastrar providers de LLM (tipo, apelido, base URL, chave, modelo) com a chave protegida pelo keychain do Windows,
**so that** os futuros adapters cloud jÃ¡ encontrem as credenciais prontas â€” sem nunca gravar chave em texto plano.

## Acceptance Criteria

1. SeÃ§Ã£o "Central de API" na janela de ConfiguraÃ§Ãµes (15.3): CHAVES CADASTRADAS (tipo/apelido/baseUrl/modelo â€” a chave NUNCA Ã© reexibida) + CADASTRAR PROVIDER (Tipo, Nome, Base URL, API Key em campo password, Modelo default opcional), fiel Ã  foto (FR56).
2. Chave criptografada com `safeStorage.encryptString` (DPAPI/keychain) â€” ciphertext base64 em `app_meta.api_providers`; sem `isEncryptionAvailable()`, cadastro RECUSADO com mensagem (nunca texto plano).
3. Remover apaga o registro (ciphertext junto); NENHUM canal devolve a chave (nem cifrada) ao renderer â€” nÃ£o existe decrypt hoje (sem consumidor, decisÃ£o consciente do fundador).
4. Documentado no rodapÃ© da prÃ³pria tela: nenhum adapter consome ainda (preparaÃ§Ã£o; NFR6 preservado pros CLIs).

## Tasks / Subtasks

- [x] Task 1 â€” `@cockpit/shared`: `ApiProviderSchema` (SEM chave), `ApiProviderCreateRequestSchema` (chave sÃ³ no trajeto cadastroâ†’Main), `Remove`; canais `apiProvider.list/create/remove`; `CockpitApi.apiProvider`
- [x] Task 2 â€” `PersistenceManager.apiProvidersRaw()/setApiProvidersRaw()` (transporte cru, padrÃ£o settings)
- [x] Task 3 â€” Main: `StoredApiProvider` interno com `keyCipher`; `publicApiProviders()` DESPE o cipher antes de qualquer resposta; create valida + recusa sem keychain; id via `ulid()`
- [x] Task 4 â€” preload com Zod na borda (parse do array pÃºblico)
- [x] Task 5 â€” `ApiCenterSection` no `SettingsWindow` (nav ðŸ”‘): lista com remover, form da foto, erro do Main exibido (ex.: keychain indisponÃ­vel), submit limpa nome/chave/modelo
- [x] Task 6 â€” App: `apiProviders` carrega ao ABRIR a janela; handlers create (Promise pro erro chegar Ã  tela) e remove
- [x] Task 7 â€” `pnpm verify` PASS (25/25 + NFR7) + `smoke:daemon` PASS

## Dev Notes

- Fluxo da chave: input password â†’ IPC local (rendererâ†’Main, processo a processo na MESMA mÃ¡quina) â†’ `safeStorage.encryptString` â†’ base64 no SQLite. Volta: NUNCA. Decrypt sÃ³ existirÃ¡ no Main quando um adapter cloud consumir (Ã©poca em que a chave descriptografada seguirÃ¡ confinada ao Main).
- `type` Ã© tag livre de um select (Ollama Cloud/OpenAI-compatÃ­vel/Anthropic/OpenRouter/Outro) â€” sem semÃ¢ntica por trÃ¡s ainda, deliberado.

## Change Log

| 2026-07-16 | 0.1 | Draft @sm (FR56) â†’ @po GO (9/10) â†’ @dev â†’ InReview; gate @qa CONCERNS (aprovado) | River/Pax/Dex/Quinn via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 â€” Dex (@dev) via Orion

### File List

- `packages/shared/src/ipc.ts` â€” schemas/canais/CockpitApi
- `packages/core/src/state-store/persistence.ts` â€” transporte cru
- `apps/desktop/src/main/session-ipc.ts` â€” handlers + safeStorage + strip do cipher
- `apps/desktop/src/preload/index.ts` â€” ponte
- `packages/ui/src/settings-window.tsx` â€” seÃ§Ã£o Central de API
- `apps/desktop/src/renderer/src/App.tsx` â€” estado/handlers

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:** 1. Code review â€” cipher despido em TODA resposta (Ãºnica funÃ§Ã£o de saÃ­da); recusa explÃ­cita sem keychain; Zod nas duas bordas. OK. 2. Testes â€” schemas cobertos pelo padrÃ£o do ipc.test; handler Ã© integraÃ§Ã£o Electron (safeStorage nÃ£o roda em vitest node â€” validaÃ§Ã£o manual). 3. ACs 1-4 atendidos. 4. RegressÃµes â€” verify+smoke PASS. 5. n/a. 6. SeguranÃ§a â€” revisada acima; chave nunca loga, nunca volta. 7. Docs â€” fluxo da chave documentado.

**ObservaÃ§Ã£o (CONCERNS):** validaÃ§Ã£o manual pendente (cadastrar chave real, reabrir app, conferir que sÃ³ metadados aparecem; remover).
