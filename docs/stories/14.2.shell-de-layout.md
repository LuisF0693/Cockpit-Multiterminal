# Story 14.2: Shell de layout (header, sidebar, telemetria, rodapÃ© de sessÃµes)

## Status

Done

## Story

**As a** fundador,
**I want** a estrutura de tela do mockup (header compacto, sidebar em seÃ§Ãµes, painel direito, rodapÃ© de cards),
**so that** o app tenha a MESMA anatomia da referÃªncia, sÃ³ que com meus dados reais.

## Acceptance Criteria

1. Header 42px: marca "MEU **COCKPIT**" (ciano), abas de view, `v{versÃ£o} Â· N sessÃµes Â· â— daemon` (dot colorido pelo estado real), pill de zoom (movido da toolbar 13.2) e "+ novo terminal" (FR48).
2. Sidebar esquerda 240px em seÃ§Ãµes maiÃºsculas: PROJETO (ativo com cor/caminho/badge git-native + `âŽ‡ branch` real da 13.3), NOVO AGENTE (adapters reais com descriÃ§Ã£o do catÃ¡logo 13.4 â€” clicar cria terminal), PROJETOS (dot+nome, + cria), ARQUIVOS (Ã¡rvore real), APP & SISTEMA (views secundÃ¡rias REAIS: Timeline/Learnings/Agentes/ConfiguraÃ§Ãµes) e rodapÃ© de build (versÃ£o real, SEM custos) (FR48).
3. Painel direito 230px: TELEMETRIA + STATUS (card de decisÃµes pendentes real, clicÃ¡vel â†’ master) + EVENTOS (timeline real, cores por tipo, poll de 10s) (FR48).
4. RodapÃ© 88px: cards das sessÃµes ATIVAS reais (dot de status, nome, cwd, tempo no status, chip do adapter â€” SEM custo); clicar foca o terminal (FR48).
5. Nenhuma funcionalidade removida â€” views re-hospedadas (secundÃ¡rias na sidebar), criaÃ§Ã£o por adapter na sidebar/catÃ¡logo.

## Tasks / Subtasks

- [x] Task 1 â€” Componentes novos em `@cockpit/ui`: `AppSidebar` (seÃ§Ãµes do mock, linhas 64-119), `TelemetryPanel` (261-275), `SessionCardsBar` (278-293), `FileTree` (extraÃ­do do ProjectFilesSidebar) (AC2-4)
- [x] Task 2 â€” App.tsx: header re-anatomizado (marca, abas minimalistas master/canvas/tarefas/board, info com dot do daemon, pill de zoom, + novo terminal/+ browser); seletor de adapter + campo Ollama REMOVIDOS do header (criaÃ§Ã£o especÃ­fica â†’ sidebar/catÃ¡logo; Ollama usa o default das ConfiguraÃ§Ãµes 13.5) (AC1, AC5)
- [x] Task 3 â€” App.tsx: `rootEntries` (Ã¡rvore) e `telemetryEvents` (poll 10s) no App; `openFilePreview` alimenta o painel da 14.5 (AC2, AC3)
- [x] Task 4 â€” SuperfÃ­cies SUPERSEDED removidas (cÃ³digo + exports): `Sidebar` (sessÃµes â†’ rodapÃ© de cards), `ProjectFilesSidebar` (â†’ AppSidebar), `StatusBar` 13.3 (informaÃ§Ã£o preservada: daemon no header, branch/projeto na sidebar, decisÃµes na telemetria, sessÃµes no rodapÃ©), `CanvasToolbar` 13.2 (zoom â†’ header; toggles de minimapa/links removidos â€” mock nÃ£o os tem, tudo sempre visÃ­vel) (AC5)
- [x] Task 5 â€” `pnpm verify` PASS (25/25 + NFR7) + `smoke:daemon` PASS

## Dev Notes

- **Desvio documentado de FR42/FR43:** as SUPERFÃCIES da toolbar do canvas e da status bar (Ã‰pico 13) foram superseded pelo layout do mock (FR48) â€” o CONTRATO DE INFORMAÃ‡ÃƒO delas continua satisfeito em outras casas. Removidas em vez de deixadas mortas (Quality First).
- `selectedAdapter` virou constante `'shell'` (default do Ctrl+N); criaÃ§Ã£o por adapter especÃ­fico Ã© sempre explÃ­cita (sidebar/catÃ¡logo).
- Workspace selector permaneceu no header (real, 3.6) â€” o mock nÃ£o tem workspaces, mas remover seria perda de funÃ§Ã£o (AC5 proÃ­be).

## Change Log

| 2026-07-16 | 0.1 | Draft @sm (FR48) â†’ @po GO (9/10) â†’ @dev â†’ InReview; gate @qa CONCERNS (aprovado) | River/Pax/Dex/Quinn via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 â€” Dex (@dev) via Orion

### File List

- `packages/ui/src/app-sidebar.tsx`, `file-tree.tsx`, `telemetry-panel.tsx`, `session-cards-bar.tsx` (NOVOS)
- `packages/ui/src/sidebar.tsx`, `project-files-sidebar.tsx`, `status-bar.tsx`, `canvas-toolbar.tsx` (REMOVIDOS)
- `packages/ui/src/index.ts` â€” exports
- `apps/desktop/src/renderer/src/App.tsx` â€” shell completo

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:** 1. Code review â€” componentes puros, dados reais em tudo, remoÃ§Ã£o limpa das superfÃ­cies mortas. OK. 2. Testes â€” componentes de exibiÃ§Ã£o (precedente); FileTree preserva a lÃ³gica lazy testada por uso desde a 8.4. 3. ACs atendidos. 4. RegressÃµes â€” verify+smoke PASS; funcionalidade re-hospedada, nÃ£o removida. 5. Performance â€” poll novo de 10s (30 eventos) Ã© desprezÃ­vel. 6. SeguranÃ§a â€” n/a. 7. Docs â€” desvios FR42/FR43 documentados.

**ObservaÃ§Ã£o (CONCERNS):** validaÃ§Ã£o visual do fundador pendente; atalho Ctrl+N continua criando shell (default) â€” se o fundador preferir "Ãºltimo adapter usado", Ã© um ajuste de 1 linha.
