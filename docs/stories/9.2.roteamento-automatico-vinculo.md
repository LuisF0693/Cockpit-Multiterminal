# Story 9.2: Roteamento automático (modo auto)

## Status

InReview

## Story

**As a** desenvolvedor multi-agente,
**I want** que um vínculo em modo automático dispare instrução no terminal alvo quando a origem terminar,
**so that** eu não precise ficar copiando manualmente o resultado de um agente para o próximo.

## Acceptance Criteria

1. Quando o terminal de origem de um vínculo `auto` transiciona para `done`/`waiting-input`, o sistema envia automaticamente uma instrução ao(s) terminal(is) alvo (mesmo gatilho de status do FR17).
2. Mensagem referencia o terminal de origem (nome/adapter) e pede ao alvo para agir sobre o resultado — mesmo princípio de redação centralizada numa função pura já usado no Épico 7.
3. Envio automático é registrado na timeline com origem `system` (mesma distinção `human`/`system` do FR17/AC3 da 7.2).
4. Disparo é idempotente por transição (não repete em toda checagem de status) — mesma garantia estrutural do AC4 da 7.2.

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 183 testes PASS + smoke:daemon PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — `planTerminalLinkRouting(session, allLinks)` em `@cockpit/core/terminal-link-routing.ts` (NOVO) — função PURA que decide quando rotear: status novo `done`/`waiting-input`, existe ≥1 vínculo `auto` com `sourceId === session.id`; agrega TODOS os alvos auto dessa origem numa única decisão (múltiplos vínculos auto da mesma origem = múltiplos `targetIds`, um só evento)
- [x] Task 2 — QUARTO listener em `registry.onEvent` (Main, junto dos de persistência/roteamento SDC/limpeza de vínculo) chama a função pura e só executa efeitos quando o retorno não é `null`: trilha `terminal_link.routed` por alvo via novo `persistence.recordTerminalLinkRouting` (origem `system`) + push `terminalLink.routed` para o renderer — try/catch (mesmo cuidado dos outros 3 listeners, `emit()` não isola exceções)
- [x] Task 3 — mensagem construída DENTRO da função pura (mesmo padrão da 7.2 — a redação vive junto da decisão)
- [x] Task 4 — contrato: canal `terminalLink.routed`, `TerminalLinkRoutedEventSchema {sourceId, targetIds[], message}`, `CockpitApi.terminalLink.onRouted`
- [x] Task 5 — preload + App.tsx: assina o push; para cada `targetId` chama `instructAgent(targetId, message)` já existente (3.2/7.2/7.4) — Main nunca escreve PTY diretamente (decisão crítica 4)
- [x] Task 6 — testes: 7 casos de `planTerminalLinkRouting` (dispara em done/waiting-input, mensagem referencia origem, agrega múltiplos alvos da mesma origem, NÃO dispara com status errado/modo manual/sem vínculo correspondente); AC4 (idempotência) garantida estruturalmente pelo mesmo guard de `markAgentStatus` já documentado na 7.2 — não retestado aqui

## Dev Notes

- Estrutura é uma cópia quase direta do padrão da 7.2 (`planSdcReviewRouting`), só trocando a fonte de "quem escutar" (`classifyTaskRoles`/tarefa → `TerminalLinkManager.list()` filtrado por `sourceId`+`mode==='auto'`) — reforça que o princípio decisão-pura/efeito-colateral do Épico 7 generaliza bem para vínculos fora do contexto de tarefa.
- QUATRO listeners agora escutam `registry.onEvent` neste arquivo (persistência, roteamento SDC, limpeza de vínculo da 9.1, roteamento de vínculo desta story) — todos independentes, todos com try/catch próprio; nenhum sabe da existência dos outros (mesmo princípio de baixo acoplamento já estabelecido).
- Vínculo terminal-a-terminal e vínculo tarefa-terminal continuam INDEPENDENTES: um terminal pode disparar roteamento automático via vínculo (9.2) E via three-brain (7.2) ao mesmo tempo, se ambos existirem — cada mecanismo decide e age sem saber do outro.

## Change Log

| 2026-07-14 | 0.1 | Draft + PO GO (Ready) + dev completo — verify/smoke PASS, status → InReview | River/Pax/Dex via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List

- `planTerminalLinkRouting` agrega TODOS os vínculos `auto` da mesma origem numa única decisão/evento (`targetIds: string[]`) em vez de um evento por vínculo — reduz ruído na timeline e no push quando um terminal tem vários alvos automáticos (AC2 não especifica isso, mas é a leitura natural do "mesmo princípio" do FR17, que já agrega `reviewerIds`).
- Verificação: `pnpm verify` (lint+typecheck+test+NFR7) PASS; `pnpm --filter @cockpit/desktop smoke:daemon` PASS; `npx turbo run test --force` → **183 testes / 30 arquivos** (era 176 na 9.1; +7 novos de `planTerminalLinkRouting`).

### File List

- `packages/core/src/terminal-link-routing.ts` (novo) — `planTerminalLinkRouting`
- `packages/core/src/terminal-link-routing.test.ts` (novo) — 7 testes
- `packages/core/src/index.ts` — exports novos
- `packages/core/src/state-store/persistence.ts` — `recordTerminalLinkRouting`
- `packages/shared/src/ipc.ts` — `TerminalLinkRoutedEventSchema`, canal `terminalLink.routed`, `CockpitApi.terminalLink.onRouted`
- `apps/desktop/src/main/session-ipc.ts` — quarto listener em `registry.onEvent` (roteamento automático de vínculo)
- `apps/desktop/src/preload/index.ts` — `terminalLink.onRouted`
- `apps/desktop/src/renderer/src/App.tsx` — assinatura `onRouted` → `instructAgent` por `targetId`

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:**
1. Code review — segue à risca o padrão já validado da 7.2 (decisão pura + efeito colateral condicional + try/catch); quarto listener independente dos outros três, sem acoplamento cruzado. OK.
2. Testes unitários — 7 casos cobrindo disparo (AC1), conteúdo da mensagem (AC2), agregação de múltiplos alvos, e todos os guards de não-disparo (status errado, modo manual, sem vínculo correspondente); AC4 documentado como garantia estrutural pré-existente (mesmo tratamento da 7.2), não retestado. OK.
3. Acceptance criteria — AC1 (dispara em done/waiting-input) atendido e testado; AC2 (mensagem referencia origem) atendido e testado; AC3 (trilha com origem system) atendido via `recordTerminalLinkRouting`, mesmo padrão já testado indiretamente em `instruction.sent`/`sdc.review_requested`; AC4 (idempotência) garantida estruturalmente. Todos atendidos.
4. Regressões — `pnpm verify` + `smoke:daemon` + 183 testes/30 arquivos PASS.
5. Performance — `terminalLinkManager.list()` é um array em memória (não hit de disco), filtro O(n) sobre vínculos — nenhum impacto perceptível mesmo com muitos vínculos.
6. Segurança — nenhuma superfície nova; mensagem construída inteiramente no Main a partir de dados já confiáveis (nome/adapter da sessão de origem).
7. Documentação — Dev Notes/Completion Notes/File List preenchidos; decisão de agregação de múltiplos alvos documentada.

**Observação (CONCERNS, não bloqueante):** fluxo ponta-a-ponta com 2+ CLIs reais (origem terminando → alvo recebendo instrução automática) não tem teste de integração automatizado — mesma lacuna já documentada e aceita na 7.2, fora do escopo de CI. Validação manual do fundador pendente (junto da 9.3, quando a UI de criar vínculo estiver completa).
