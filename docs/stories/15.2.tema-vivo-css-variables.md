# Story 15.2: Runtime de tema vivo (CSS variables)

## Status

Done

## Story

**As a** fundador,
**I want** trocar tema, cor de destaque e fontes com aplicaÃ§Ã£o IMEDIATA (sem reiniciar),
**so that** a AparÃªncia das ConfiguraÃ§Ãµes (15.3) funcione como na referÃªncia OmniRift.

## Acceptance Criteria

1. Tokens expostos como CSS variables (`--ck-*`) no `:root`; o objeto `theme` mantÃ©m a MESMA forma com valores `var(--ck-*)` â€” nenhum componente muda a leitura (FR55).
2. Presets: Multerminal Escuro (default, valores EXATOS do Ã‰pico 14), Multerminal Claro, Meia-noite; 7 cores de destaque (par primÃ¡ria/clara); fontes texto/mono selecionÃ¡veis (stacks seguras).
3. ConcatenaÃ§Ãµes de alpha sobre valores de tema migradas pra `color-mix()` (4 pontos: minimapa Ã—2, board Ã—2) â€” nada quebra com var().
4. xterm re-tematiza AO VIVO: `TerminalView` consome os dados CRUS via `getActiveTheme()`/`subscribeTheme()` e muta `term.options.theme`/`fontFamily` na troca.
5. PersistÃªncia: `themePreset`/`accentColor`/`fontText`/`fontMono` nas settings (FR46); App compÃµe+aplica no boot.

## Tasks / Subtasks

- [x] Task 1 â€” `theme-runtime.ts` (NOVO): `ThemeData`, `THEME_PRESETS` (3), `ACCENT_OPTIONS` (7 pares), `FONT_*_OPTIONS`+`fontStackOf`, `composeTheme` (PURA), `themeToCssVars` (PURA), `applyTheme`/`getActiveTheme`/`subscribeTheme` (aplica no import com guard de `document` pra testes node)
- [x] Task 2 â€” `theme.ts` vira PONTE de `var(--ck-*)` (nÃºmeros continuam estÃ¡ticos); `canvasBackground` usa `--ck-canvas-dot`
- [x] Task 3 â€” `color-mix()` nos 4 pontos de alpha-concatenaÃ§Ã£o (canvas-minimap, lifecycle-board)
- [x] Task 4 â€” `TerminalView` assina o runtime (cleanup no unmount)
- [x] Task 5 â€” Schema: 4 campos novos com catch+default; testes atualizados (+1 caso de accentColor invÃ¡lido)
- [x] Task 6 â€” `theme.test.ts` REESCRITO: ponte sÃ³ de var(), presets com hex vÃ¡lidos + invariante terminal.background===surface.tile POR PRESET + default exato do Ã‰pico 14, composeTheme (override + degradaÃ§Ã£o), themeToCssVars cobre TODAS as vars referenciadas pela ponte (teste-guarda de paridade)
- [x] Task 7 â€” App boot: `applyTheme(composeTheme(settings))`; `pnpm verify` PASS (25/25 + NFR7) + `smoke:daemon` PASS; UI 44â†’46 testes

## Dev Notes

- EstratÃ©gia-chave: manter a FORMA do `theme` fez a migraÃ§Ã£o custar ~0 nos 20+ componentes â€” sÃ³ os 4 pontos de concatenaÃ§Ã£o de alpha precisaram de `color-mix` (Chromium 111+, Electron 31 ok).
- `STATUS_COLORS`/`ADAPTER_COLORS`/`PROJECT_PALETTE`/`LINK_PALETTE` ficam FIXOS (identidade de estado/agente/projeto nÃ£o muda com tema â€” decisÃ£o deliberada).
- Sombras continuam estÃ¡ticas (pretas funcionam nos 3 presets; tema claro usa opacidade menor implÃ­cita pela cor de fundo) â€” refinÃ¡vel depois se o claro incomodar.
- A UI de troca (AparÃªncia) Ã© a 15.3 â€” esta story entrega o MOTOR + boot-apply.

## Change Log

| 2026-07-16 | 0.1 | Draft @sm (FR55) â†’ @po GO (9/10) â†’ @dev â†’ InReview; gate @qa CONCERNS (aprovado) | River/Pax/Dex/Quinn via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 â€” Dex (@dev) via Orion

### File List

- `packages/ui/src/theme-runtime.ts` (NOVO)
- `packages/ui/src/theme.ts` â€” ponte de vars
- `packages/ui/src/theme.test.ts` â€” reescrito
- `packages/ui/src/terminal-view.tsx` â€” re-tema vivo
- `packages/ui/src/canvas-minimap.tsx`, `lifecycle-board.tsx` â€” color-mix
- `packages/ui/src/index.ts` â€” exports do runtime
- `packages/shared/src/ipc.ts` + `ipc.test.ts` â€” campos/testes
- `apps/desktop/src/renderer/src/App.tsx` â€” boot-apply

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:** 1. Code review â€” funÃ§Ãµes puras separadas de efeito (composeTheme/themeToCssVars vs applyTheme); guard de document pros testes; cleanup do subscribe. OK. 2. Testes â€” paridade ponteâ†”vars com teste-guarda; presets validados; degradaÃ§Ã£o coberta. 3. ACs 1-5 atendidos. 4. RegressÃµes â€” verify+smoke PASS; preset default Ã© bit-a-bit o Ã‰pico 14 (teste trava isso). 5. Performance â€” troca de tema Ã© ~28 setProperty + 1 mutaÃ§Ã£o de options por terminal (sÃ³ na troca). 6. n/a. 7. Docs ok.

**ObservaÃ§Ã£o (CONCERNS):** validaÃ§Ã£o visual pendente â€” em especial o tema CLARO (valores novos, sem referÃªncia do fundador) e fontes nÃ£o instaladas caindo nos fallbacks.
