# Story 11.2: Qualificação (fila de decisão humana)

## Status

InReview

## Story

**As a** desenvolvedor multi-projeto,
**I want** revisar um learning e marcá-lo como reutilizável (ou descartá-lo),
**so that** só aprendizados validados entrem na base que a metodologia reusa.

## Acceptance Criteria

1. Um learning tem 3 estados: `draft` (recém-capturado) → `reviewed` (revisado, mas não necessariamente valioso) → `reusable` (qualificado como reaproveitável); ou `draft` → `discarded` (não vale a pena manter).
2. Transições são decisão HUMANA explícita (mesmo princípio do FR15 — nunca automática/heurística) — reusa o MESMO padrão de ponto de decisão já usado para tarefas (5.3), sem inventar uma máquina de estados nova.
3. Learnings em `draft` aparecem numa fila simples (mesmo padrão da fila de decisões pendentes já existente na sessão master) para o humano revisar quando quiser — não é bloqueante, não pausa nada.
4. Trilha auditável: cada transição de status registrada com autor e timestamp (mesmo padrão de `task.decision`).

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 200 testes PASS + smoke:daemon PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — `canTransitionLearning`/`VALID_TRANSITIONS` (4 estados, AC1) já existiam desde a 11.1 (antecipados ali para que `updateStatus` nascesse com integridade garantida) — esta story só adiciona a TRILHA à mesma operação
- [x] Task 2 — `LearningManager.updateStatus` grava `learning.status_changed` na trilha (origem `human` — SEMPRE, nenhum caminho automático neste projeto chama esta função; AC2/AC4) na MESMA escrita da mudança de status, mesmo padrão de `TaskManager.updateState`
- [x] Task 3 — fila de qualificação no `MasterDashboard`: seção "🎓 Learnings para qualificar" lista `draft`+`reviewed` (os dois estados que ainda precisam de uma decisão humana) — `draft` mostra botão "revisado" (→`reviewed`) + "descartar"; `reviewed` mostra "✓ reutilizável" (→`reusable`) + "descartar" (AC1/AC3); NÃO bloqueante — some sozinha quando não há pendências, mesmo princípio visual da fila de decisões de tarefa já existente
- [x] Task 4 — testes: 1 caso novo cobrindo a trilha auditável (`learning.status_changed` com `origin: 'human'` e payload `{learningId, from, to}`) — as transições em si (válidas/inválidas) já estavam cobertas desde a 11.1

## Dev Notes

- Como `canTransitionLearning` e a validação de transição já tinham sido construídas na 11.1 (decisão documentada lá: "a entidade não podia nascer sem nenhuma garantia de integridade"), o trabalho REAL desta story foi menor do que o AC sugere à primeira leitura — só a trilha (AC4) e a UI da fila (AC3) eram genuinamente novas.
- A fila mistura `draft` E `reviewed` numa lista só (em vez de duas filas separadas) — ambos os estados representam "precisa de uma decisão humana", só com ações diferentes disponíveis; menos uma divisão visual desnecessária.
- Nenhum caminho AUTOMÁTICO chama `updateStatus` neste projeto — `origin: 'human'` é hardcoded (não um parâmetro), porque não existe hoje nenhum mecanismo de "digestão automática" de learnings (decisão de design já registrada no épico, Artigo IV) que justificasse um parâmetro `origin` variável.

## Change Log

| 2026-07-14 | 0.1 | Draft + PO GO (Ready) + dev completo — verify/smoke PASS, status → InReview | River/Pax/Dex via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List

- Escopo desta story acabou sendo majoritariamente TRILHA + UI, já que a lógica de validação de transição foi antecipada na 11.1 — decisão de sequenciamento que se provou correta (a 11.1 não teria integridade sem ela).
- `MasterDashboardProps` trocou `learningCount: number` (11.1) por `learnings: Learning[]` (11.2) — evolução natural do prop surface dentro do mesmo épico, já que a fila de qualificação precisa da lista completa, não só da contagem (o contador em si agora deriva de `learnings.length` direto no componente).
- Verificação: `pnpm verify` (lint+typecheck+test+NFR7) PASS; `pnpm --filter @cockpit/desktop smoke:daemon` PASS; `npx turbo run test --force` → **200 testes / 32 arquivos** (era 199 na 11.1; +1 novo, cobrindo a trilha auditável).

### File List

- `packages/core/src/learning-manager.ts` — `updateStatus` grava trilha `learning.status_changed`
- `packages/core/src/learning-manager.test.ts` — 1 teste novo (trilha)
- `packages/ui/src/master-dashboard.tsx` — prop `learnings` (substitui `learningCount`), fila de qualificação
- `apps/desktop/src/renderer/src/App.tsx` — `updateLearningStatus`, wiring das props novas no `MasterDashboard`

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:**
1. Code review — trilha gravada na MESMA escrita atômica da mudança de status (mesmo padrão do `TaskManager`); fila mistura draft+reviewed de forma sensata (ambos precisam de decisão humana). OK.
2. Testes unitários — 1 caso novo cobrindo especificamente a trilha (payload+origin); transições válidas/inválidas já cobertas na 11.1, não reduplicadas aqui.
3. Acceptance criteria — AC1 (4 estados) já atendido desde a 11.1, confirmado aqui; AC2 (decisão humana explícita, nunca automática) atendido — `origin: 'human'` hardcoded, decisão documentada; AC3 (fila não-bloqueante) atendido pela seção no master; AC4 (trilha com autor+timestamp) atendido e testado. Todos atendidos.
4. Regressões — `pnpm verify` + `smoke:daemon` + 200 testes/32 arquivos PASS; mudança de prop (`learningCount`→`learnings`) é interna ao componente, sem quebra de contrato externo (IPC inalterado).
5. Performance — fila filtra um array pequeno (`.filter()`), sem impacto perceptível.
6. Segurança — nenhuma superfície nova; transições continuam validadas no core antes de qualquer persistência.
7. Documentação — Dev Notes/Completion Notes/File List preenchidos; decisão de `origin` hardcoded documentada com justificativa (Artigo IV, sem digestão automática hoje).

**Observação (CONCERNS, não bloqueante):** ainda não há tela de consulta/busca dedicada (11.3) — a fila do master só mostra pendências de qualificação, não o histórico completo de learnings já qualificados; esperado, dado o faseamento explícito do épico.
