# Story 7.4: Ciclo de correção com feedback agregado

## Status

InReview

## Story

**As a** desenvolvedor multi-agente,
**I want** que uma rejeição na revisão volte ao escritor com o feedback de todos os revisores,
**so that** o ciclo de correção seja informado, não um reset em branco.

## Acceptance Criteria

1. Rejeitar (fluxo humano existente, 5.3/FR15) numa tarefa em modo three-brain agrega o texto mais recente de cada revisor numa única instrução de correção enviada automaticamente ao escritor (reusa FR19).
2. Ciclo (escrever → revisar → corrigir) pode repetir; cada rodada fica auditável na timeline (reusa a trilha de decisão da 5.3 + o roteamento automático da 7.2).
3. Redirecionar (5.3) numa tarefa three-brain troca o PAPEL do agente redirecionado para `writer`, preservando os revisores — não quebra o modo three-brain.
4. Nenhuma máquina de estados nova: o ciclo inteiro é composição de mecanismos já existentes (task-lifecycle da 5.1, decisão humana da 5.3, roteamento da 7.2) — verificável por teste de integração ponta a ponta.

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 156 testes PASS + smoke:daemon PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — `planSdcCorrectionRouting(taskId, taskTitle, roles, transcripts, justification?)` em `@cockpit/core/sdc-routing.ts` (mesmo arquivo da 7.2) — função PURA: fora do three-brain ou sem escritor retorna `null`; dentro, agrega `transcripts[reviewerId]` de cada revisor numa mensagem única (nome+adapter+trecho, ou "(sem saída recente)") anexando a justificativa opcional (AC1)
- [x] Task 2 — `planSdcRedirect(rolesBefore, allLinkedIds, redirectTo)` em `@cockpit/core/sdc-routing.ts` — função PURA que decide o plano de vínculo do redirect: se `isThreeBrain`, só desvincula o escritor antigo e vincula `redirectTo` como `writer` (revisores intocados, AC3); senão, mantém o comportamento atual (desvincula todos exceto `redirectTo`, vínculo neutro)
- [x] Task 3 — `taskDecide` (Main, `session-ipc.ts`) usa as duas funções puras: no `reject`, lê `readScrollbackTail` de cada revisor (mesmo mecanismo da 7.3, já configurado com `scrollbackDir`), chama `planSdcCorrectionRouting`, se não-null grava trilha `sdc.correction_requested` (origin `system`, via novo `persistence.recordSdcCorrectionRequest`) e empurra `sdc.correctionRequested` para o renderer; no `redirect`, chama `planSdcRedirect` e aplica o plano via `registry.linkTask`
- [x] Task 4 — contrato: canal `sdc.correctionRequested`, `SdcCorrectionRequestedEventSchema {taskId, writerId, message}`, `CockpitApi.sdc.onCorrectionRequested`
- [x] Task 5 — preload + App.tsx: assina o push; chama `instructAgent(writerId, message)` já existente (3.2/7.2) — Main nunca escreve PTY diretamente (decisão crítica 4)
- [x] Task 6 — testes: casos de `planSdcCorrectionRouting` (agrega N revisores, inclui justificativa quando presente, fallback "(sem saída recente)", `null` fora do three-brain/sem escritor) e de `planSdcRedirect` (three-brain preserva revisores + papel writer no novo agente; fora do three-brain mantém comportamento antigo) — mesmo padrão de cobertura pura da 7.2, sem integração Electron nova (3 CLIs reais simultâneos + rejeição real está fora do escopo de CI, mesma nota da 7.2)

## Dev Notes

- Reaproveita a leitura de scrollback já construída na 7.3 (`readScrollbackTail` sobre `opts.scrollbackDir`) — nenhuma infraestrutura nova de transcript.
- Mensagem de correção construída INTEIRAMENTE dentro da função pura (mesmo padrão da 7.2: a redação vive junto da decisão, não espalhada no Main nem no `instructAgent`).
- `taskManager.decide()` já move `reject`/`redirect` para `in_progress` (5.3) — nenhuma aresta nova no lifecycle; o roteamento automático de revisão (7.2) volta a disparar naturalmente quando o escritor corrigido transicionar de novo para `done`/`waiting-input`, fechando o ciclo sem lógica adicional (AC2/AC4).
- `rolesBefore` (papéis ANTES da decisão) precisa ser capturado antes de `taskManager.decide()`/`registry.linkTask()` mutarem qualquer coisa — em `reject` os vínculos não mudam, mas em `redirect` o plano de link depende do estado anterior.

## Change Log

| 2026-07-14 | 0.1 | Draft + PO GO 10/10 (Ready) | River/Pax via Orion |
| 2026-07-14 | 0.2 | Dev completo — verify/smoke/testes PASS, status → InReview. **ÉPICO 7 FECHADO (4/4)** | Dex via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List

- `planSdcCorrectionRouting`/`planSdcRedirect` (Task 1/2): duas funções puras novas em `packages/core/src/sdc-routing.ts`, junto de `planSdcReviewRouting` (7.2) — mesmo arquivo, mesmo princípio de decisão-testável-sem-Electron.
- `taskDecide` (Task 3): capturei `rolesBefore = classifyTaskRoles(registry.list(), taskId)` ANTES de `taskManager.decide()`/`registry.linkTask()` mutarem qualquer coisa — necessário porque o `redirect` depende de saber quem era o escritor ANTIGO para decidir o plano de vínculo. No `reject`, leio `readScrollbackTail` de cada revisor (mesmo `opts.scrollbackDir` da 7.3) e, se `planSdcCorrectionRouting` retornar não-null, gravo a trilha `sdc.correction_requested` (origem `system`) e empurro o evento — tudo dentro de um try/catch (mesmo cuidado do `registry.onEvent` da 7.2: um bug aqui não pode quebrar a decisão humana em si, que já foi persistida por `taskManager.decide()`).
- AC3 (redirect preserva revisores): `planSdcRedirect` decide friamente o plano (`unlinkIds`+`link`); o handler só executa `registry.linkTask` para cada id do plano — fora do three-brain o comportamento é IDÊNTICO ao pré-7.4 (nenhuma regressão no fluxo clássico de redirect da 5.3).
- AC2/AC4 (ciclo repetível, sem máquina de estados nova): nenhuma aresta nova no lifecycle — `reject`/`redirect` continuam indo para `in_progress` (5.3); quando o escritor corrigido volta a `done`/`waiting-input`, o roteamento da 7.2 dispara de novo NATURALMENTE (mesmo guard de idempotência da `SessionRegistry`) — o "ciclo" é só a composição de mecanismos já existentes e testados, sem lógica adicional de orquestração.
- Verificação: `pnpm verify` (lint+typecheck+test+NFR7) PASS; `pnpm --filter @cockpit/desktop smoke:daemon` PASS; `npx turbo run test --force` → **156 testes / 27 arquivos** (era 149 na 7.3; +7 testes novos: 5 de `planSdcCorrectionRouting` + 2 de `planSdcRedirect`), todos PASS.
- **ÉPICO 7 (SDC embutido & Three-Brain) FECHADO — 4/4 stories.** Próximo passo (fora desta story): PR de `feature/epic-7-sdc-three-brain` → `main`, e então formalizar o Épico 8 (learning logs globais) pelo mesmo processo de spec pipeline usado para o 7.

### File List

- `packages/core/src/sdc-routing.ts` — `planSdcCorrectionRouting`, `SdcCorrectionRouting`, `planSdcRedirect`, `SdcRedirectPlan`
- `packages/core/src/sdc-routing.test.ts` — 7 testes novos
- `packages/core/src/index.ts` — exports novos
- `packages/core/src/state-store/persistence.ts` — `recordSdcCorrectionRequest`
- `packages/shared/src/ipc.ts` — `IpcChannels.sdcCorrectionRequested`, `SdcCorrectionRequestedEventSchema`, `CockpitApi.sdc.onCorrectionRequested`
- `apps/desktop/src/main/session-ipc.ts` — `taskDecide` estendido (reject agrega+empurra correção; redirect usa `planSdcRedirect`)
- `apps/desktop/src/preload/index.ts` — `sdc.onCorrectionRequested`
- `apps/desktop/src/renderer/src/App.tsx` — assinatura `onCorrectionRequested` → `instructAgent`

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:**
1. Code review — `rolesBefore` capturado antes de qualquer mutação (correto: reject não muda vínculos, mas redirect precisa do estado PRÉ-decisão); try/catch no ramo `reject` mirra o cuidado já estabelecido no `registry.onEvent` da 7.2; `planSdcRedirect` preserva 100% o comportamento pré-7.4 fora do three-brain (sem regressão no redirect clássico da 5.3). OK.
2. Testes unitários — 7 casos novos cobrindo agregação (N revisores, justificativa, fallback, nulls) e o plano de redirect (three-brain vs. fora dele); ambas as funções são puras, mesmo padrão de cobertura da 7.2 (`planSdcReviewRouting`). `recordSdcCorrectionRequest` não tem teste dedicado — mesmo precedente de `recordSdcReviewRequest`/`recordInstruction`/`recordAdoption` (wrapper fino de 1 linha sobre `appendEvent`, coberto indiretamente por verify+smoke). OK.
3. Acceptance criteria — AC1 (feedback agregado no reject) atendido por `planSdcCorrectionRouting`+wiring; AC2 (ciclo repetível, auditável) atendido pela composição de trilhas já existentes (`task.decision` da 5.3 + `sdc.review_requested` da 7.2 + `sdc.correction_requested` novo); AC3 (redirect preserva revisores) atendido por `planSdcRedirect`; AC4 (nenhuma máquina de estados nova) verificado — `taskManager.decide()` não ganhou nenhuma aresta nova, só orquestração em cima. Todos atendidos.
4. Regressões — `pnpm verify` + `smoke:daemon` + 156 testes/27 arquivos PASS, sem regressão detectada; redirect fora do three-brain testado explicitamente para garantir paridade com o comportamento pré-7.4.
5. Performance — leitura de scrollback no reject é síncrona mas limitada a `maxBytes=4096` por revisor (mesmo teto da 7.3), evento raro (decisão humana), sem impacto perceptível.
6. Segurança — nenhuma superfície nova de escrita externa; mensagem de correção é construída inteiramente no Main a partir de dados já confiáveis (scrollback local + task title); push validado no preload via schema Zod na borda.
7. Documentação — Dev Notes/Completion Notes/File List preenchidos nesta story; épico 7 marcado como fechado no Change Log.

**Observação (CONCERNS, não bloqueante):** o fluxo ponta-a-ponta completo (3 CLIs reais simultâneos — escritor rejeitado, corrigindo, revisores reavaliando) não tem teste de integração automatizado, mesma lacuna já documentada na 7.2 (fora do escopo de CI); validação manual do fundador com agentes reais continua pendente para todo o Épico 7. CodeRabbit não executado (WSL indisponível — padrão desta sessão).
