# Story 12.5: Minimapa do canvas

## Status

Done

## Story

**As a** desenvolvedor multi-agente,
**I want** um minimapa no canto do canvas mostrando todos os tiles,
**so that** eu navegue rápido quando tenho muitos terminais abertos ao mesmo tempo.

## Acceptance Criteria

1. Minimapa fixo num canto do canvas mostra um retângulo por tile (posição/tamanho proporcional ao `layout.tiles` real, cor por projeto quando aplicável — reusa a 12.3).
2. Clicar num retângulo do minimapa foca o tile correspondente (mesmo `focus`/`bringToFront` já existente) e rola o canvas até ele.
3. Minimapa só aparece quando há tiles fora da área visível do canvas (evita ruído em telas com poucos terminais) — ou é colapsável, qualquer uma das duas soluções satisfaz o AC.
4. Minimapa é OMITIDO da view `'canvas'` quando o canvas está oculto (master ativo) — mesmo princípio de "só monta o que está em uso" já aplicado no resto do app.

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 221 testes PASS + smoke:daemon PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** | [x] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — `canvas-minimap.tsx` (packages/ui, NOVO): `computeMinimapScale` PURA (testável, mesmo princípio decisão-pura/efeito da 12.1/`sdc-routing.ts`) calcula o fator de escala pra caber conteúdo+viewport numa caixa fixa 180×120; `CanvasMinimap` (thin wrapper) renderiza um `<button>` por tile — cor do projeto (12.3) ou neutro, clicável — mais um retângulo de outline mostrando o viewport visível atual (AC1)
- [x] Task 2 — decisão de AC3 (opção "colapsável" em vez de "detecção de overflow"): botão `▼`/`▲` no cabeçalho do minimapa recolhe só o corpo (mantém a barrinha de título visível) — evita a complexidade de detectar "tiles fora da área visível" em tempo real; componente retorna `null` quando não há tiles (nenhum ruído com canvas vazio)
- [x] Task 3 — App.tsx: `canvasViewport` (estado) espelha `scrollLeft/scrollTop/clientWidth/clientHeight` do `canvasSectionRef` (já existente da 12.2) via listener de `scroll` + `ResizeObserver` (cobre resize de janela E o canvas saindo de `display:none` ao trocar pra view `'canvas'`); `minimapTiles` (derivado) usa o MESMO filtro de visibilidade do render principal (workspace ativo + projeto ativo pra sessões, só projeto pra browser tiles) — minimapa nunca mostra um tile escondido do canvas real
- [x] Task 4 — `focusAndScrollTo(id)`: chama `useCockpitStore.getState().focus(id)` (mesmo `focus`/`bringToFront` já existente) + `canvasSectionRef.current.scrollTo({..., behavior:'smooth'})` centralizando o tile na área visível (AC2)
- [x] Task 5 — `{view === 'canvas' && <CanvasMinimap .../>}` — DIFERENTE do padrão de tiles (que ficam montados escondidos por causa do xterm/portas), o minimapa não tem NENHUM estado de I/O persistente, então desmontar de verdade quando o canvas não está ativo é seguro e correto (AC4)

## Dev Notes

- `computeMinimapScale` extraída como função pura testável (4 casos: conteúdo pequeno cabe com escala <1, conteúdo maior gera escala menor, funciona sem tiles usando só o viewport, mantém proporção usando o menor fator dos dois eixos) — mesmo princípio já seguido em `markdown-lite.tsx`/`layout.ts`: a lógica de cálculo é pura e testável, só a montagem em JSX (`CanvasMinimap`) fica sem teste unitário (mesmo precedente de todo componente React do projeto).
- Escolha de AC3 (colapsável, não detecção de overflow): calcular "há tiles fora da área visível" exigiria comparar cada `layout.tiles` contra o retângulo de scroll em tempo real — mais uma camada de estado derivado pra um ganho pequeno (o minimapa colapsado já resolve "ruído com poucos terminais" com uma linha de UI mínima, sempre visível como lembrete de que existe).
- `ResizeObserver` no `canvasSectionRef` (em vez de só um listener de `window resize`) cobre o caso em que o canvas estava com `display:none` (master ativo) e volta a `display:block` — o navegador dispara um resize do box observado nesse momento, então o viewport do minimapa já vem correto na primeira renderização após trocar de view.
- Retângulo de viewport no minimapa é só um indicador visual (`pointerEvents:none`) — não é clicável nem arrastável nesta story (fora do escopo do AC2, que só pede clique nos tiles).

## Change Log

| 2026-07-15 | 0.1 | Dev completo — verify/smoke PASS, status → InReview | Dex via Orion |
| 2026-07-15 | 0.2 | PR #7 mergeado em `main` (Épico 12 completo) — status → Done | Gage via Orion |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 — Dex (@dev) via Orion

### Completion Notes List

- Zero entidade nova: minimapa é 100% derivado de `layout.tiles` (Story 1.4) + `Project.color` (12.3) — nenhum novo estado persistido, nenhuma nova IPC.
- Verificação: `pnpm verify` (lint+typecheck+test+NFR7) PASS — 23/23 tasks; `pnpm --filter @cockpit/desktop smoke:daemon` PASS; `npx turbo run test --force` → **221 testes / 36 arquivos** (era 217/35 na 12.4; +4 testes/+1 arquivo de `computeMinimapScale`).
- Este era o ÚLTIMO story do Épico 12 (ordem 12.1→12.2→12.3→12.4→12.5 confirmada pelo fundador) — todas as 5 stories entregues nesta sequência, sem desvio de escopo.

### File List

- `packages/ui/src/canvas-minimap.tsx` (novo) — `computeMinimapScale`, `CanvasMinimap`
- `packages/ui/src/canvas-minimap.test.ts` (novo) — 4 testes de `computeMinimapScale`
- `packages/ui/src/index.ts` — exports de `canvas-minimap`
- `apps/desktop/src/renderer/src/App.tsx` — `canvasViewport` (estado + efeito de scroll/resize), `minimapTiles` (derivado), `focusAndScrollTo`, `<CanvasMinimap>` montado condicionalmente

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:**
1. Code review — separação pura/efeito consistente com o resto do projeto; filtro de visibilidade do minimapa replica EXATAMENTE o filtro do render principal (mesma expressão booleana), evitando divergência entre "o que o canvas mostra" e "o que o minimapa mostra". OK.
2. Testes unitários — 4 casos de `computeMinimapScale` (escala válida, proporcionalidade inversa ao tamanho do conteúdo, funciona sem tiles, mantém proporção via `Math.min` dos dois eixos); `CanvasMinimap` em si não testado (mesmo precedente de componente React sem harness de render).
3. Acceptance criteria — AC1 (retângulo por tile, proporcional, cor por projeto) atendido; AC2 (clique foca + rola) atendido via `focusAndScrollTo`; AC3 (colapsável, opção válida pelo "ou" do critério) atendido; AC4 (omitido quando canvas oculto) atendido via montagem condicional real (não só CSS). Todos atendidos.
4. Regressões — `pnpm verify` + `smoke:daemon` + 221 testes/36 arquivos PASS; nenhuma mudança nos componentes/handlers já existentes (`focus`, `scrollTo` são chamadas novas, não substituições).
5. Performance — `ResizeObserver` + listener de `scroll` são baratos (só setState em mudança real de tamanho/posição); `minimapTiles` recalcula a cada render, mas é um `.map`/`.filter` sobre listas tipicamente pequenas (dezenas de tiles no máximo).
6. Segurança — nenhuma superfície nova; nenhum dado sensível exposto (só posições/tamanhos de tile já visíveis no próprio canvas).
7. Documentação — Dev Notes/Completion Notes/File List preenchidos; decisão "colapsável em vez de detecção de overflow" documentada com a razão (menos estado derivado pra um ganho pequeno).

**Observação (CONCERNS, não bloqueante):** validação visual manual (abrir vários terminais espalhados, conferir que o minimapa reflete a posição real e que o clique navega corretamente) ainda pendente — mesma categoria de pendência já registrada em todas as stories anteriores do Épico 12. Com esta story, o Épico 12 está code-complete; a validação visual completa das 5 stories fica registrada como o próximo passo antes de considerar o épico verdadeiramente "Done" (hoje: InReview em todas).
