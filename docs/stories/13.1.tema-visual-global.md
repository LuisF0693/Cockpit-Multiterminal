# Story 13.1: Tema visual global (design tokens + refresh do canvas)

## Status

Done

## Story

**As a** fundador que olha pro Cockpit o dia inteiro,
**I want** um visual coeso e bonito (fundo, tipografia, bordas, sombras, espaçamentos consistentes) no canvas e em todas as telas,
**so that** a ferramenta pareça um produto de referência, não um protótipo.

## Contexto

Feedback direto do fundador (2026-07-15): "Melhora também o visual do sistema
e o canvas, tô achando um pouco feio." Diagnóstico técnico: 264 cores hex
espalhadas em 19 arquivos do pacote UI + 32 declarações de estilo no App.tsx,
sem fonte única além de `STATUS_COLORS` (2.5) e `ADAPTER_COLORS` (12.4).
Paleta atual: dark cinza (#0B0F14/#111827/#1F2937), texto #E5E7EB, acento
ciano #22D3EE — funcional, mas chapada (sem camadas de profundidade), com
espaçamentos e raios inconsistentes entre superfícies.

Primeira story do Épico 13 DE PROPÓSITO: toolbar (13.2), status bar (13.3),
catálogo (13.4) e Configurações (13.5) devem nascer já usando os tokens.

## Acceptance Criteria

1. Módulo `theme.ts` novo no pacote `@cockpit/ui` com tokens nomeados (superfícies em camadas bg/painel/tile/header, texto primário/secundário/muted, borda default/sutil, acento, raios, sombras, tipografia, escala de espaçamento) — `STATUS_COLORS` e `ADAPTER_COLORS` continuam existindo e passam a ser referenciados/coordenados pelo tema, não substituídos (FR41).
2. Canvas refeito visualmente sobre os tokens: fundo com profundidade (não chapado), tiles com raio/sombra/borda consistentes, cabeçalho de tile mais limpo (identidade de adapter + status + nome sem poluição), tinta de projeto (12.6) integrada à nova paleta.
3. Todas as superfícies existentes (sidebar de sessões, sidebar unificada projetos+arquivos, master, TasksPanel, LifecycleBoard, LearningsView, TimelineView, SessionReport, ReviewPanel, PromptModal, Recovery Screen, minimapa, tiles de browser) migradas para os tokens — critério objetivo: nenhuma cor hex de superfície/texto/borda hardcoded fora de `theme.ts`/`status-colors.tsx`/`adapter-colors.ts` no pacote UI e no App.tsx.
4. Zero regressão funcional: nenhuma mudança de comportamento, só de apresentação — todos os testes existentes continuam passando sem alteração de lógica; gestures (drag/resize/Alt+link), zoom, pulso de waiting-input e anel de projeto continuam funcionando idênticos.

## Tasks / Subtasks

- [x] Task 1 — Criar `packages/ui/src/theme.ts`: objeto `theme` com `surface` (app/canvas/panel/tile/header/raised/overlay), `text` (primary/secondary/muted/faint/inverse), `border` (default/subtle/strong), `accent` (primary/soft/ring/danger/warn/ok/info), `radius` (sm/md/lg/pill), `shadow` (tile/tileFocused/overlay), `font` (ui/mono + tamanhos), `space` (escala 4px), `terminal` (xterm) + `canvasBackground()` + `PROJECT_PALETTE` — exportados no `index.ts` (AC1)
- [x] Task 2 — Teste de sanidade `theme.test.ts` (7 testes): cores hex/rgba válidas, terminal.background === surface.tile, escalas crescentes, canvasBackground com/sem cor de projeto (AC1)
- [x] Task 3 — Refresh do canvas: fundo em camadas (chão `surface.canvas` + grade de pontos via `radial-gradient`, sem imagem; lavagem translúcida da cor do projeto integrada), `TerminalTile`/`BrowserPreviewTile` com `radius.lg`, sombras do tema, header mais limpo (swatch de adapter com glow sutil) (AC2)
- [x] Task 4 — Migrados TODOS os componentes de superfície do pacote UI (sidebar, project-files-sidebar, master-dashboard, tasks-panel, lifecycle-board, learnings-view, timeline-view, session-report, review-panel, prompt-modal, recovery-screen, canvas-minimap, markdown-lite, terminal-view) (AC3)
- [x] Task 5 — App.tsx migrado (header, nav, badges, selects, canvas, linhas de vínculo SVG, estilos auxiliares); `PROJECT_COLORS` promovida a `PROJECT_PALETTE` no tema (AC3)
- [x] Task 6 — Verificação objetiva: grep de hex no pacote UI + App.tsx só encontra `theme.ts`, `status-colors.tsx`, `adapter-colors.ts` (AC3)
- [x] Task 7 — `pnpm verify` PASS (25/25 + NFR7) + `smoke:daemon` PASS; 236 testes (era 229; +7 do tema) (AC4)

## Dev Notes

- **Não inventar paleta do zero:** evoluir a existente (dark, acento ciano) para uma versão com mais profundidade — mudanças de HUE radicais mudariam a identidade que o fundador já conhece; o pedido é "menos feio/mais acabado", não "outra cara".
- `StatusPulseStyles` (keyframes do pulso) e o boxShadow de foco do tile são LÓGICA VISUAL DE ESTADO, não token — continuam onde estão, só consumindo cores do tema.
- `terminal-view.tsx` tem o theme do xterm (cores de terminal) — essas 4 cores fazem parte do contrato visual do xterm e devem ser coordenadas pelo `theme.ts` (token `terminal` dedicado), não deixadas de fora.
- Gotcha conhecido do canvas (12.6): o wrapper de zoom precisa de width/height explícitos — o refresh do fundo NÃO pode trocar a estrutura de DOM do canvas (section > wrapper escalado), só estilos.
- Tiles ficam SEMPRE montados (CSS-hide, gotcha xterm da 1.3/3.6) — nenhuma mudança de montagem permitida nesta story.

## Change Log

| 2026-07-15 | 0.1 | Story draftada pelo @sm a partir do Épico 13 (FR41) | River via Orion |
| 2026-07-15 | 0.2 | Validação @po GO (9/10) → Ready; @dev assume → InProgress | Pax/Dex via Orion |
| 2026-07-15 | 1.0 | Dev completo (theme.ts + migração de 15 componentes + App.tsx), verify/smoke PASS, 236 testes → InReview; gate @qa CONCERNS (aprovado) | Dex/Quinn via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List

- Paleta EVOLUÍDA, não substituída: mesma família (dark azul-acinzentado, acento ciano `#22D3EE`), mas com 7 camadas de superfície (canvas mais fundo → overlay) em vez de 3 valores repetidos — profundidade visual sem mudar a identidade que o fundador conhece.
- Fundo do canvas: grade de pontos sutil via `radial-gradient` (CSS puro, sem asset) sobre `surface.canvas`; a tinta de projeto da 12.6 virou uma camada `linear-gradient` translúcida combinada — sem cor de projeto, o visual é neutro (mesma garantia da 12.6).
- Cores DERIVADAS por alpha (`${token}14`, `${statusColor(...)}14`) substituíram superfícies tingidas hardcoded (`#1F1A0E`, `#0F1F17`, `#241111`) — o tom de "atenção"/"válido"/"inválido" agora acompanha automaticamente qualquer ajuste futuro nos tokens de status.
- `STATUS_DOT` do domínio de learning agora deriva do tema (`accent.info/ok/danger`, `text.muted`) — token `info` novo criado para o azul que era hardcoded.
- **Bug real encontrado durante a migração (NÃO corrigido nesta story — AC4 proíbe mudança de comportamento):** `master-dashboard.tsx` usa `window.prompt` para a justificativa de rejeição de tarefa — mesma limitação do Electron corrigida na 12.6 (retorna `null` sempre); a justificativa NUNCA pode ser preenchida hoje. Registrado para fix imediato pós-story.

### File List

- `packages/ui/src/theme.ts` (NOVO) — tokens + `canvasBackground()` + `PROJECT_PALETTE`
- `packages/ui/src/theme.test.ts` (NOVO) — 7 testes de sanidade
- `packages/ui/src/index.ts` — exports novos
- `packages/ui/src/{terminal-view,terminal-tile,browser-preview-tile,sidebar,project-files-sidebar,master-dashboard,tasks-panel,lifecycle-board,learnings-view,timeline-view,session-report,review-panel,prompt-modal,recovery-screen,canvas-minimap,markdown-lite}.tsx — migrados aos tokens
- `apps/desktop/src/renderer/src/App.tsx` — cromo+canvas migrados; `canvasBackground()` no chão do canvas

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:**
1. Code review — migração mecânica consistente (mesmos tokens pros mesmos papéis em todos os arquivos); derivações por alpha em vez de hexes tingidos novos; nenhuma mudança de estrutura de DOM no canvas (gotcha do wrapper de zoom preservado). OK.
2. Testes unitários — 7 testes novos do tema (validade de cor, coerência de escalas, invariante terminal.background===surface.tile, canvasBackground com/sem projeto); componentes de exibição seguem o precedente de proporcionalidade das stories anteriores.
3. Acceptance criteria — AC1-AC4 atendidos; AC3 verificado por grep objetivo (0 hex fora dos 3 arquivos de token).
4. Regressões — `pnpm verify` (lint+typecheck+test+NFR7) + `smoke:daemon` PASS; 236 testes; nenhum handler/gesture/lógica tocado — diffs são estritamente de estilo.
5. Performance — grade de pontos é background CSS estático (zero custo por frame); glow do swatch de adapter é box-shadow estático.
6. Segurança — nenhuma superfície nova de entrada; nada de HTML dinâmico.
7. Documentação — Dev Notes registram decisões e o bug de `window.prompt` achado (com plano de fix separado).

**Observação (CONCERNS, não bloqueante):** validação VISUAL manual pelo fundador pendente (é literalmente uma story de estética — o juiz final é o olho dele); mesma categoria de pendência de todo o Épico 12.
