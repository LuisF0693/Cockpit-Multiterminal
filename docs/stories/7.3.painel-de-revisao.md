# Story 7.3: Painel de revisão (saídas lado a lado)

## Status

InReview

## Story

**As a** desenvolvedor multi-agente,
**I want** ver a saída mais recente de cada revisor lado a lado com o resultado do escritor,
**so that** eu tenha contexto completo para decidir sem alternar entre terminais.

## Acceptance Criteria

1. Card da tarefa em modo three-brain expõe um painel de revisão com o escritor + cada revisor, mostrando adapter, status e um trecho recente do scrollback/transcript de cada um.
2. Painel acessível a partir do master, do TasksPanel e do LifecycleBoard (mesma tarefa, mesma vista).
3. Painel atualiza em tempo real conforme os terminais produzem output (mesmo padrão de push já usado em sessões/tarefas).
4. Painel some (ou informa "não é modo three-brain") para tarefas fora do modo three-brain (AC2 da 7.1).

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify testes PASS | [x] QA Gate (@qa) | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — IPC `sdc.transcriptTail {terminalId, maxBytes?}` → texto: reusa `readScrollbackTail` (já exportado por `@cockpit/pty-host` desde a 1.4, usado em `persist-smoke.ts`) sobre o MESMO `scrollbackDir` já configurado no boot — nenhuma infraestrutura nova de scrollback, só um wrapper de leitura fino no Main
- [x] Task 2 — `ReviewPanel` (packages/ui): recebe `task`+`sessions`+`getTranscript`; usa `classifyTaskRoles` (já centralizado desde a 7.1) para achar escritor/revisores; fora do modo three-brain mostra aviso (AC4); dentro, um card por agente (nome/adapter/status + trecho de transcript)
- [x] Task 3 — "tempo real" (AC3): MESMO padrão já usado em `TimelineView`/`SessionReportView` — poll a cada 5s enquanto o painel está aberto (não é push; scrollback não tem um canal de eventos, seria over-engineering criar um só para isto)
- [x] Task 4 — pontos de entrada (AC2): botão "revisão" na fila de decisões do master (ao lado de aprovar/rejeitar/redirecionar, só para tarefas `isThreeBrain`); botão no card do TasksPanel; botão no card do LifecycleBoard — todos abrem a MESMA view nova `'review'` no App.tsx com o `taskId` selecionado
- [x] Task 5 — testes: nenhuma lógica nova de domínio pura a testar (a decisão "é three-brain" já é testada pela `classifyTaskRoles` da 7.1) — cobertura fica em typecheck/verify + revisão de código, consistente com o padrão de outras UIs de leitura (SessionReportView, TimelineView) que também não têm teste unit próprio

## Dev Notes

- Scrollback é lido do ARQUIVO persistido (mesma fonte da 1.4/6.2), não da PTY ao vivo — evita abrir uma segunda MessagePort concorrente com o tile real (decisão crítica 4 preservada: dados de terminal continuam trafegando só pelo caminho já estabelecido; aqui é leitura passiva de um snapshot em disco).
- `maxBytes` pequeno por padrão (ex.: 4KB) — o painel mostra um TRECHO recente, não o scrollback inteiro (isso já existe na 1.4/restore).
- Painel é read-only nesta story — instruir a partir dele não é AC aqui (já existe via TasksPanel/master); ciclo de correção automático é a 7.4.

## Change Log

| 2026-07-14 | 0.1/0.2 | Draft + PO GO 10/10 (Ready) + dev start | River/Pax via Orion |
| 2026-07-14 | 0.3 | Dev completo — verify/smoke/testes PASS, status → InReview | Dex via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List

- Backend (Task 1): novo canal `sdc.transcriptTail` (`IpcChannels.sdcTranscriptTail` + `SdcTranscriptTailRequestSchema`, `maxBytes` com default 4096/máx 65536); handler no Main lê via `readScrollbackTail` (já existente em `@cockpit/pty-host`) sobre o `scrollbackDir` já configurado no boot — sem infra nova de scrollback; wrapper fino no preload valida a resposta com Zod na borda.
- `ReviewPanel` (Task 2): componente puro em `packages/ui`, usa `classifyTaskRoles` (7.1) para achar escritor/revisores; fora do modo three-brain mostra aviso textual (AC4); dentro, grid com um `AgentColumn` por agente (nome/adapter/status + `<pre>` do trecho de transcript). Não faz polling sozinho — App.tsx injeta `transcripts` (mesmo padrão de `TimelineView`/`SessionReportView`).
- Tempo real (Task 3): App.tsx faz poll a cada 5s enquanto `view === 'review'`, buscando `transcriptTail` para escritor + cada revisor via `classifyTaskRoles(sessions, taskId)`.
- Pontos de entrada (Task 4): botão "🧠 revisão" na fila de decisões pendentes do master (só quando `isThreeBrain`); botão "revisão" no card do TasksPanel (ao lado do badge 🧠); botão 🧠 clicável no card do LifecycleBoard — todos chamam `onOpenReview(taskId)` que seta `reviewTaskId` + `view = 'review'` no App.tsx.
- Gotcha corrigido em typecheck: `SdcTranscriptTailRequestSchema` usa `.default(4096)` em `maxBytes`, então o tipo inferido (`z.infer`, usado em `CockpitApi.sdc.transcriptTail`) exige `maxBytes` explícito no call-site — App.tsx passa `maxBytes: 4096` explicitamente em vez de confiar no default do schema (que só se aplica no lado do Main/parse).
- Verificação: `pnpm verify` (lint+typecheck+test+NFR7 isolation) PASS; `pnpm --filter @cockpit/desktop smoke:daemon` PASS (fase A + fase B, 0 órfãos); `npx turbo run test --force` → 149 testes / 27 arquivos, todos PASS (nenhum teste novo — Task 5 documenta a decisão de não criar testes unitários para esta UI de leitura, consistente com SessionReportView/TimelineView).

### File List

- `packages/shared/src/ipc.ts` — `IpcChannels.sdcTranscriptTail`, `SdcTranscriptTailRequestSchema`, `SdcTranscriptTailRequest`, `CockpitApi.sdc.transcriptTail`
- `apps/desktop/src/main/session-ipc.ts` — handler `sdc.transcriptTail` (lê `readScrollbackTail`), `registerSessionIpc` aceita `opts?.scrollbackDir`
- `apps/desktop/src/main/index.ts` — passa `scrollbackDir` para `registerSessionIpc`
- `apps/desktop/src/preload/index.ts` — `sdc.transcriptTail` (Zod na borda)
- `packages/ui/src/review-panel.tsx` (novo) — componente `ReviewPanel`
- `packages/ui/src/index.ts` — export de `ReviewPanel`/`ReviewPanelProps`
- `apps/desktop/src/renderer/src/App.tsx` — view `'review'`, estado `reviewTaskId`/`transcripts`, polling `refreshTranscripts`, `goToReview`, wiring do `ReviewPanel` e dos 3 `onOpenReview`
- `packages/ui/src/master-dashboard.tsx` — prop `onOpenReview`, botão "🧠 revisão" na fila de decisões (só `isThreeBrain`)
- `packages/ui/src/tasks-panel.tsx` — prop `onOpenReview`, botão "revisão" no card (só `isThreeBrain`)
- `packages/ui/src/lifecycle-board.tsx` — prop `onOpenReview`, badge 🧠 vira botão clicável

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:**
1. Code review — padrões consistentes com o resto do codebase (Zod na borda, decisão crítica 4 preservada — leitura passiva de scrollback em disco, sem segunda MessagePort); `ReviewPanel` segue o padrão estabelecido de componente puro alimentado por polling do App.tsx. OK.
2. Testes unitários — nenhum teste novo (decisão documentada na Task 5, consistente com o precedente de SessionReportView/TimelineView, que também são UIs de leitura sem lógica de domínio própria). A lógica de domínio reaproveitada (`classifyTaskRoles`) já tem cobertura própria da 7.1. OK.
3. Acceptance criteria — AC1 (painel com escritor+revisores, adapter/status/trecho) atendido pelo `ReviewPanel`; AC2 (acessível de master/TasksPanel/LifecycleBoard) atendido pelos 3 pontos de entrada; AC3 (tempo real) atendido pelo poll de 5s; AC4 (aviso fora do modo three-brain) atendido pelo branch condicional no `ReviewPanel`. Todos atendidos.
4. Regressões — `pnpm verify` + `smoke:daemon` + 149 testes/27 arquivos PASS, sem regressão detectada.
5. Performance — poll de 5s só ocorre com `view === 'review'` ativa (mesmo orçamento das telas de timeline/report já existentes); `maxBytes` limitado (4096, teto de 65536 no schema) evita payloads grandes.
6. Segurança — `SdcTranscriptTailRequestSchema` valida `terminalId`/`maxBytes` na borda (Main); preload valida a resposta com `z.string()`; nenhuma superfície nova de escrita (painel é read-only, por design da story).
7. Documentação — Dev Notes/Completion Notes/File List preenchidos nesta story; nenhuma doc externa exige atualização para esta mudança de UI.

**Observação (CONCERNS, não bloqueante):** o painel não expõe hoje um estado de "carregando" nem trata explicitamente uma resposta vazia do `transcriptTail` além do fallback "(sem saída recente)" já no `AgentColumn` — aceitável para uma primeira versão read-only, mas vale considerar um indicador de loading se o painel ganhar mais uso.
