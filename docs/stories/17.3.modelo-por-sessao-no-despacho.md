# Story 17.3: Modelo por sessão no despacho de workers

## Status

InReview

## Story

**As a** chefe que despacha workers pelo Cockpit,
**I want** que o worker já nasça com o MODELO escolhido (não só a CLI) — Haiku pra tarefa barata, Sonnet pra média, GPT-5.x no codex, Gemini na pesquisa,
**so that** o custo e a força de cada despacho sejam calibrados por tarefa, sem trocar modelo na mão depois que o terminal abre.

## Acceptance Criteria

1. Todos os adapters de IA (claude-code, codex, gemini-cli, grok, antigravity) repassam `config.args` ao spawn da CLI (o contrato e o daemon já propagavam desde a 12.6 — só o Ollama consumia).
2. A CLI `agent-dispatch` aceita `--model <nome>`; o worker nasce com `--model` na linha de comando da CLI alvo (Ollama: `run <modelo>`). `--model` exige `--adapter` explícito — a grafia do modelo é específica de cada CLI, então quem escolhe modelo escolhe a CLI.
3. A matriz de capacidades ganha `models` (sugestões por adapter, editáveis em `adapters-profile.json`) e o `--recommend` os exibe na justificativa.
4. Testes cobrem o repasse de args nos adapters e os modelos na justificativa.
5. Docs atualizadas (AGENTS.md) e gates verdes.

## Tasks / Subtasks

- [x] Task 1 — Adapters repassam `config.args` (AC: 1, 4).
- [x] Task 2 — CLI `--model` com validação e mapeamento por adapter (AC: 2).
- [x] Task 3 — `models` na matriz + `--recommend` (AC: 3, 4).
- [x] Task 4 — Docs + gates + smoke (AC: 5).

## Dev Notes

- Troca de modelo em sessão VIVA continua manual (digitar `/model` no claude/codex) — o chefe pode enviá-la via bridge/vínculo; automatizar isso é story futura se a dor aparecer.
- Os nomes em `models` do `adapters-profile.json` são dicas EDITÁVEIS do fundador, na grafia que cada CLI aceita; a CLI não valida o nome (erro de modelo aparece no terminal do worker).

## File List

- [x] `packages/adapters/claude-code/src/claude-code-adapter.ts` (+ test)
- [x] `packages/adapters/codex/src/codex-adapter.ts` (+ test)
- [x] `packages/adapters/gemini-cli/src/gemini-cli-adapter.ts` (+ test)
- [x] `packages/adapters/grok/src/grok-adapter.ts` (+ test)
- [x] `packages/adapters/antigravity/src/antigravity-adapter.ts`
- [x] `packages/core/src/adapter-profile.ts` (+ test)
- [x] `apps/desktop/src/main/agent-dispatch.ts`
- [x] `adapters-profile.json`
- [x] `AGENTS.md`

## Dev Agent Record

- Gates (2026-07-20): `pnpm lint` ✓ · `pnpm typecheck` ✓ (25 tasks) · testes 25/25 tasks ✓ (novos: repasse de args nos 5 adapters de IA — antigravity incluído pós-QA — e modelos na justificativa) · `electron-vite build` ✓.
- Smoke real: `--model haiku` sem `--adapter` → exit 1 com mensagem clara; despacho `@qa` com `--adapter claude-code --model sonnet` nasceu com `modelo=sonnet` e vínculo detectado (`vínculo→chefe=01KXSPNZ...`).
- Fix pós-QA gate (CONCERNS): teste do antigravity adicionado; comentário do gemini alinhado à matriz (`gemini-3.5-pro`); gates registrados aqui.

## QA Results

```yaml
storyId: '17.3'
gateDate: 2026-07-20
reviewer: 'Quinn (@qa)'
verdict: CONCERNS
scope: 'working tree da branch feature/17.2-vinculo-e-matriz (implementação AINDA NÃO COMMITADA no momento do gate)'
checks:
  code_review: PASS # repasse de config.args uniforme nos 5 adapters de IA (posição correta: após --settings no claude, após -c notify no codex); --model exige --adapter na CLI com exit 1 e mensagem clara; ollama mapeado pra `run <modelo>` (coerente com NON_DISPATCHABLE, que só vale pro despacho automático)
  unit_tests: CONCERNS # claude-code, codex, gemini-cli e grok têm teste do repasse de args; ANTIGRAVITY NÃO TEM (AC4 pede cobertura "nos adapters"). Testes existentes re-executados sem cache: todos verdes
  acceptance_criteria: PASS # AC1-AC3, AC5 verificados no diff; AC4 parcialmente (ver unit_tests)
  regressions: PASS # `config.args ?? []` preserva o comportamento sem --model; suíte completa 25/25 tasks verde
  performance: PASS # sem impacto — args passados direto ao spawn
  security: PASS # --model vai como argv ao spawn do PTY (sem shell interpolation); nome inválido de modelo falha visivelmente no terminal do worker (documentado em Dev Notes)
  documentation: CONCERNS # AGENTS.md atualizado ✓; mas o Dev Agent Record diz "gates na seção da execução — ver Change Log" e o Change Log NÃO registra os gates
issues:
  - severity: medium
    category: tests
    description: 'AntigravityAdapter passou a repassar config.args mas é o único adapter de IA sem teste desse repasse (AC4).'
    recommendation: 'Adicionar teste espelho dos demais adapters (3 linhas de harness) antes do push, ou registrar waiver explícito.'
  - severity: low
    category: docs
    description: 'Dev Agent Record aponta os gates pro Change Log, que não os contém; e a implementação estava não-commitada no momento do gate (story InReview sem commit próprio).'
    recommendation: '@dev: commitar a implementação da 17.3 com referência à story e registrar os gates no Dev Agent Record.'
  - severity: low
    category: code
    description: 'Comentário no gemini-cli-adapter exemplifica "gemini-2.5-pro" enquanto a matriz sugere "gemini-3.5-pro" — cosmético.'
    recommendation: 'Alinhar o exemplo do comentário quando tocar no arquivo de novo.'
evidence:
  - 'Testes forçados sem cache (turbo --force) nos 5 adapters + core + pty-host: 17/17 tasks verdes.'
  - 'pnpm lint ✓ e pnpm typecheck 25/25 ✓ nesta revisão (incluem o diff não-commitado).'
  - 'Verificado no planner: --adapter explícito ignora NON_DISPATCHABLE de propósito, então `--adapter ollama --model X` funciona como o AC2 descreve.'
```

Veredito: CONCERNS — aprovado com observações. Nenhum item bloqueia o merge; o teste do antigravity (medium) é a única pendência que recomendo resolver antes do push.

## Change Log

| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-07-20 | 0.1 | Story criada e implementada a partir do pedido do fundador ("já abra com o modelo: Haiku aqui, Sonnet ali, GPT-5.x no codex"). | Orion |
