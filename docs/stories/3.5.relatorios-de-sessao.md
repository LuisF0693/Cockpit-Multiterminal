# Story 3.5: Relatórios de sessão

## Status

InReview

## Story

**As a** desenvolvedor multi-agente,
**I want** relatórios por sessão (duração, eventos, instruções, transições de status),
**so that** eu entenda o que cada agente fez sem reler o terminal inteiro.

## Acceptance Criteria

1. Cada sessão acumula métricas: duração, contagem de transições de status, instruções recebidas via master, exit code.
2. Painel de detalhe da sessão exibe o relatório + últimos eventos da timeline.
3. Base extensível para tokens/tools por adapter (quando os CLIs expuserem — registrar como campos opcionais).

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 86 testes PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** — validação visual pendente | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — exitCode atravessa a cadeia (AC1): `PtyHostManager.onSessionExit` já entrega `exitCode`; `SessionRegistry.markExited(ptyId, exitCode?)` guarda no record (campo opcional em `SessionRecordSchema`); `PersistenceManager` grava `exitCode` no payload do evento `terminal.exited`
- [x] Task 2 — transições de status na trilha (AC1): `PersistenceManager.wire` case `'status'` passa a registrar evento `status.changed` (origin `agent`, payload `{status}`) — agentStatus segue transiente na tabela terminals (decisão 2.1 intacta; o delta é só a trilha, que também completa o AC1 da 3.3)
- [x] Task 3 — `StateStore.getTerminal(id)` + `StateStore.countEvents({terminalId?, type?})` (sqlite: SELECT/COUNT; memory: find/filter) + `PersistenceManager.sessionReport(id)` com flush prévio: duração (archivedAt/now − createdAt), transições, instruções, recuperações, exitCode (payload do último `terminal.exited`)
- [x] Task 4 — contrato: `SessionReportSchema` (Zod, com campos opcionais `tokens`/`toolCalls` — AC3), canal `session.report` (Zod na borda), preload + `CockpitApi.session.report`
- [x] Task 5 — UI (AC2): `SessionReportView` em packages/ui — métricas + últimos eventos da sessão (timeline filtrada, limit 20, refresh 5s); botão "relatório" na linha do master abre o painel
- [x] Task 6 — testes: persistence (status.changed na trilha; exitCode no payload; sessionReport com contagens) + verify suíte completa PASS (86)

## Dev Notes

- events já persiste desde a 1.4; relatório é PROJEÇÃO — nenhuma tabela nova, nenhuma migração de schema.
- Leitura não passa pela WriteQueue (padrão da 3.3): só `queue.flush()` prévio para frescor.
- `status.changed` é low-frequency: `markAgentStatus` só emite em mudança real (guard `!==` no registry).
- tokens/toolCalls ficam `optional()` no schema e "—" na UI até os adapters exporem (E-daemon/futuro).

## Change Log

| 2026-07-14 | 0.1/0.2 | Draft + PO GO 10/10 (Ready) + dev start | River/Pax via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List / File List

- Relatório é PROJEÇÃO da trilha — zero tabela nova, zero migração; sqlite/memory ganham `getTerminal`+`countEvents`.
- `endedAt` = archivedAt (closed); exited mantém sessão restaurável com duração correndo — semântica da 1.4 preservada.
- Gotcha de teste: eventos no mesmo ms têm ordem instável no sort por ts — asserção vira conjunto.
- **Criados:** `packages/ui/src/session-report.tsx`. **Modificados:** shared (ipc: SessionReport/exitCode/canal), core (session-registry, state-store types/sqlite/memory/persistence+tests), session-ipc, preload, ui (index/master-dashboard), App.

## QA Results

### Gate: **CONCERNS** (aprovado) — Quinn (@qa), 2026-07-14. 7 checks PASS (86 testes; AC1-3 com evidência unit; projeção sem migração preserva semântica 1.4/2.1). Achado médio CORRIGIDO no gate: índice `idx_events_terminal` (terminal_id, type) — countEvents sem full scan. Issues low: validação visual pendente do fundador (botão "relatório" no master com CLI real); CodeRabbit não executado (WSL indisponível — padrão das stories anteriores).
