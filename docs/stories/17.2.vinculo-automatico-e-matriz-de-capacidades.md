# Story 17.2: Vínculo automático chefe↔worker e matriz de capacidades

## Status

Done

## Story

**As a** chefe de departamento que despacha workers pelo Cockpit,
**I want** que o worker despachado nasça vinculado ao meu terminal e que a recomendação de modelo venha de uma matriz de capacidades editável,
**so that** o resultado do worker volte pra mim automaticamente (sem religar na mão) e a escolha de modelo seja informada por conhecimento real de cada CLI, não só palavras-chave.

## Acceptance Criteria

1. Quando o despacho parte de um terminal do Cockpit, a CLI detecta o terminal do CHEFE automaticamente (subindo a cadeia de PIDs do processo até casar com uma sessão viva do daemon) e o worker nasce com vínculo `worker → chefe` em modo `auto` — o término do worker (done/waiting-input) dispara a instrução automática no chefe (FR26/Épico 9).
2. `--no-link` desliga o vínculo; `--link-from <sessionId>` força a origem sem depender da detecção. Despacho fora de um terminal do Cockpit (detecção falha) segue funcionando sem vínculo, com aviso.
3. O protocolo do daemon propaga `dispatchedBy` (create → list-sessions), e a adoção no Main cria o vínculo via `TerminalLinkManager` quando chefe e worker pertencem ao mesmo projeto; projetos diferentes: loga e adota sem vínculo.
4. Uma matriz de capacidades editável (`adapters-profile.json` na raiz do repo, com defaults embutidos no core) descreve forças, custo e observações de cada adapter e pode sobrescrever a ordem de preferência por categoria; o merge é função pura testada.
5. `--recommend` passa a devolver justificativa por candidato a partir da matriz (e o caminho do arquivo editável), como insumo da decisão do chefe (AC7 da 17.1).
6. Testes cobrem: matching da cadeia de PIDs, merge/justificativas da matriz, override de preferências no planner, propagação de `dispatchedBy` no daemon e no plano de adoção.
7. Documentação atualizada (AGENTS.md deste repo e bloco replicado no SPFP) e gates verdes (lint, typecheck, testes, build) + smoke real contra o daemon.

## Tasks / Subtasks

- [x] Task 1 — Core: `findDispatcherSession` (matching PID chain → sessão) + override de preferências no `planAgentDispatch` (AC: 1, 4, 6).
- [x] Task 2 — Core: `adapter-profile.ts` com defaults, merge puro e justificativas (AC: 4, 5, 6).
- [x] Task 3 — Daemon: `dispatchedBy` no create/list-sessions + teste de integração (AC: 3, 6).
- [x] Task 4 — Core/Main: `planExternalAdoption` propaga `dispatchedBy`; adoção cria vínculo auto no mesmo projeto (AC: 1, 3, 6).
- [x] Task 5 — CLI: detecção por PIDs (PowerShell CIM), flags `--no-link`/`--link-from`/`--profile`, matriz no `--recommend` (AC: 1, 2, 5).
- [x] Task 6 — `adapters-profile.json` na raiz + docs (AGENTS.md aqui e no SPFP) (AC: 4, 7).
- [x] Task 7 — Gates + smoke real + atualizar esta story (AC: todos).

## Dev Notes

- Direção do vínculo: `planTerminalLinkRouting` dispara quando o SOURCE vai a done/waiting-input e instrui os TARGETS — logo o vínculo nasce `source=worker, target=chefe`. [Source: `packages/core/src/terminal-link-routing.ts`]
- O id de sessão do daemon É o id do tile no registry (tag = ULID), então `dispatchedBy` já é o id do tile do chefe. [Source: `packages/pty-host/src/daemon-protocol.ts`]
- A criação manual de vínculo via IPC exige mesmo projeto; a criação automática na adoção respeita a mesma regra. [Source: `apps/desktop/src/main/session-ipc.ts`]
- Detecção do chefe: uma chamada PowerShell (`Get-CimInstance Win32_Process`) monta o mapa pid→ppid; a cadeia é subida em memória e o matching é função pura testável. Falha da detecção NUNCA bloqueia o despacho (graceful, mesmo princípio do code-intel).

## File List

- [x] `packages/core/src/agent-dispatch.ts`
- [x] `packages/core/src/agent-dispatch.test.ts`
- [x] `packages/core/src/adapter-profile.ts`
- [x] `packages/core/src/adapter-profile.test.ts`
- [x] `packages/core/src/external-adoption.ts`
- [x] `packages/core/src/external-adoption.test.ts`
- [x] `packages/core/src/index.ts`
- [x] `packages/pty-host/src/daemon-protocol.ts`
- [x] `packages/pty-host/src/daemon-client.ts`
- [x] `packages/pty-host/src/daemon-server.ts`
- [x] `packages/pty-host/src/daemon.integration.test.ts`
- [x] `apps/desktop/src/main/agent-dispatch.ts`
- [x] `apps/desktop/src/main/session-ipc.ts`
- [x] `adapters-profile.json`
- [x] `AGENTS.md`

## Dev Agent Record

- Gates: `pnpm lint` ✓ · `pnpm typecheck` ✓ (25 tasks) · testes 25/25 tasks ✓ (core 151, +12 novos: findDispatcherSession, preferences override, adapter-profile, dispatchedBy na adoção; pty-host 29 com integração estendida) · `electron-vite build` ✓.
- Smoke real contra o daemon vivo: `--recommend` devolve candidatos justificados pela matriz (`matrixSource` apontando pro `adapters-profile.json` da raiz); despacho real com detecção da cadeia de PIDs encontrou o terminal do chefe (`vínculo→chefe=01KXSPNZ...`) a partir da sessão Claude rodando num tile do Cockpit.
- ATENÇÃO operacional: o vínculo só materializa com o Cockpit REINICIADO no build novo — o daemon antigo descarta `dispatchedBy` e o Main antigo não cria o vínculo na adoção. A CLI nova degrada graciosamente contra ambos.
- Decisões do fundador (2026-07-19): detecção automática + modo auto; matriz editável em arquivo; escopo draft+implement na mesma sessão.

## QA Results

```yaml
storyId: '17.2'
gateDate: 2026-07-20
reviewer: 'Quinn (@qa)'
verdict: PASS
scope: 'commit dd812b0 na branch feature/17.2-vinculo-e-matriz'
checks:
  code_review: PASS # core puro (findDispatcherSession, mergeAdapterMatrix, explainCandidates), I/O na CLI, graceful em toda falha de detecção — segue os padrões do repo
  unit_tests: PASS # re-executados SEM cache do turbo: core 17 files/152 tests ✓, pty-host 8 files ✓ (inclui integração dispatchedBy create→list-sessions em pipe real)
  acceptance_criteria: PASS # AC1-AC7 verificados no diff: detecção por cadeia de PIDs, --no-link/--link-from, dispatchedBy no protocolo + vínculo auto na adoção (mesmo projeto), matriz editável com merge puro, --recommend justificado, docs
  regressions: PASS # suíte completa 25/25 tasks verde; campos novos do protocolo são opcionais (sessões antigas seguem válidas)
  performance: PASS # detecção = 1 chamada CIM com limite defensivo de 64 níveis na cadeia; matriz lida uma vez por invocação da CLI
  security: PASS # sem input não confiável em shell (PowerShell com args fixos); dispatchedBy validado contra registry antes de criar vínculo
  documentation: PASS # AGENTS.md do repo atualizado no commit; replicação no SPFP declarada no Dev Agent Record (fora deste repo — não verificável aqui)
issues:
  - severity: low
    category: code
    description: 'loadAdapterMatrix engole silenciosamente JSON malformado do adapters-profile.json (cai nos defaults); fora do --recommend o usuário não vê aviso de que a edição dele foi ignorada.'
    recommendation: 'Emitir um console.error curto quando o arquivo existe mas não parseia (mantendo o graceful). Não bloqueia.'
  - severity: low
    category: requirements
    description: 'Vínculo na adoção compara chefe.projectId === plan.projectId — dois null passam e criam vínculo com projectId null (aceito pelo TerminalLinkManager por contrato). Borda improvável (chefe vivo no canvas normalmente tem projeto).'
    recommendation: 'Aceitável como está; se aparecer no uso real, exigir projectId não-nulo pro vínculo auto.'
evidence:
  - 'Testes forçados sem cache: turbo --force em core/pty-host/adapters → 17/17 tasks verdes (erros AttachConsole são ruído conhecido do node-pty no Windows).'
  - 'pnpm lint ✓ e pnpm typecheck 25/25 ✓ nesta revisão.'
  - 'Smoke real contra daemon vivo registrado no Dev Agent Record (detecção encontrou o chefe a partir de tile Claude vivo).'
```

## Change Log

| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-07-19 | 0.1 | Story criada a partir do pedido do fundador: vínculo automático chefe↔worker no despacho + matriz de capacidades editável para escolha de modelo. Decisões do fundador: auto-detecção + modo auto; matriz em arquivo editável; escopo draft+implement. | Orion |
| 2026-07-20 | 0.2 | Revisão pré-merge da PR #13 (8 frentes de análise) achou e corrigiu 5 bugs reais: (1) guarda do vínculo automático tratava `chefe.projectId === plan.projectId === null` como "mesmo projeto" — agora exige ambos não-nulos; (2) vínculo não checava se o "chefe" era um adapter de IA — worker podia ser linkado a um tile shell/cmd cru e receber instrução em linguagem natural no stdin; corrigido reusando `NON_DISPATCHABLE` (agora exportado do core); (3) `--no-link` não tinha efeito quando `--link-from` também era passado — agora `--no-link` sempre vence; (4) `loadAdapterMatrix` fazia cast sem validação do `adapters-profile.json` editado à mão — um campo mal formatado crashava `--recommend` (exit 2, mesmo código de "daemon inacessível"); adicionada validação de schema (zod) com diagnóstico claro no stderr; (5) falha silenciosa ao carregar o profile agora avisa no stderr fora do `--recommend` também. Débito registrado, não corrigido nesta leva (baixo risco, não bloqueia): `processPidChain()` faz uma varredura de processo do sistema inteiro (`Get-CimInstance`) a cada despacho automático sem cache; `REPO_ROOT_HOPS` é um caminho relativo de profundidade fixa (mesmo anti-padrão já corrigido em `daemon-manager.ts`); a regra "mesmo projeto" está duplicada entre o handler manual de vínculo e o automático. | Orion |
