# Story 6.3: App como cliente — attach/adoção na inicialização

## Status

InReview

## Story

**As a** desenvolvedor multi-agente,
**I want** o Cockpit conectando ao daemon existente no boot (ou subindo um) e adotando as sessões vivas,
**so that** fechar/reabrir o app não interrompa nenhum agente em execução.

## Acceptance Criteria

1. Main descobre daemon vivo no boot (pipe conhecido); se ausente, sobe o daemon (spawn detached) e conecta.
2. Sessões vivas do daemon são adotadas: aparecem no registry/UI com scrollback replayado — o restore do SQLite reconcilia (sessão viva no daemon = adotar; morta = relançar como hoje).
3. MessagePort do renderer preservada: o Main vira proxy pipe↔MessagePort; renderer e adapters não mudam (NFR7).
4. Fechar o app NÃO derruba as sessões (validável: reabrir e continuar digitando no mesmo shell).

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 99 testes PASS + smoke:daemon PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — DaemonClient: `autoAck` opcional (default true) + `ack(id, n)` — o proxy do Main repassa acks REAIS do renderer (backpressure fim-a-fim preservado)
- [x] Task 2 — `SessionRegistry.adopt(...)`: cria record de sessão JÁ VIVA no daemon (sem createPty), emite `created` (re-upsert + espelho da UI de graça); `PersistenceManager.restore` pula ids já no registry; trilha ganha `session.adopted`
- [x] Task 3 — `DaemonManager` (Main): mesma superfície do PtyHostManager + `listSessions`/`adoptPty`; start() = connect-or-spawn (detached, `ELECTRON_RUN_AS_NODE`, retry); proxy por sessão pipe↔MessageChannelMain (dados, input, acks); `disconnect()` NÃO derruba o daemon
- [x] Task 4 — build: entry `daemon.js` no electron-vite (rollup input, mesmo padrão do pty-host.js); spawn resolve por candidatos (gotcha do chunking — nunca __dirname puro)
- [x] Task 5 — session-ipc: interface `PtyBackend` (estrutural — host OU daemon); restore com passe de adoção antes do relaunch; index.ts escolhe backend (`COCKPIT_NO_DAEMON=1` = escape hatch; falha do daemon → fallback utilityProcess); before-quit em modo daemon DESCONECTA (sessões sobrevivem) e mantém clean_shutdown
- [x] Task 6 — smoke cross-processo `smoke:daemon` (AC4): fase A (processo 1) sobe daemon detached via node + cria sessão + marker; fase B (processo 2) reconecta, adota via list-sessions, replay contém marker, echo vivo, shutdown 0 órfãos — **PASS em 3 processos reais**
- [x] Task 7 — testes unit: registry.adopt (record correto + evento + trilha) e restore que pula adotadas; verify suíte completa PASS (99)

## Dev Notes

- Quit do app: `disconnect()` com flag shuttingDown — onClose NÃO dispara hostExit (que marcaria todas exited); sessões ficam `running` no SQLite e são adotadas no próximo boot.
- Se a sessão persistida estiver `exited` no SQLite mas VIVA no daemon, a adoção corrige: `created` re-upserta como running.
- Replay na adoção: attach usa restoreTailBytes da config — MessagePortMain bufferiza até o renderer reclamar a porta (mesmo fluxo do restore da 1.4).
- Daemon `create` com tag existente → create-error → restore arquiva (defensivo, não destrói).
- Dev e prod usam o pipe default único (`\\.\pipe\cockpit-daemon`); instâncias múltiplas do app compartilham o daemon — desejado (visão do fundador).

## Change Log

| 2026-07-14 | 0.1/0.2 | Draft + PO GO 10/10 (Ready) + dev start | River/Pax via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List / File List

- **Bug real achado pelo smoke:** primeira conexão falhando (daemon ainda subindo) → `helloAck` rejeitava sem awaiter → unhandled rejection derrubava o retry loop. Fix: marcar a promise como tratada antes do await do connect. Os testes de integração nunca pegariam (server sempre pronto) — o smoke cross-processo pagou o próprio custo.
- Dados agora atravessam o Main (proxy pipe↔MessagePort) — custo aceito da decisão crítica 5; backpressure fim-a-fim preservado via ack repassado.
- Quit em modo daemon: `disconnect()` com flag — sessões ficam running no SQLite e são adotadas no boot seguinte.
- **Criados:** `apps/desktop/src/main/daemon-manager.ts`, `daemon-smoke.ts`. **Modificados:** pty-host (daemon-client autoAck/ack + fix unhandled), core (session-registry.adopt, persistence skip+recordAdoption+test), session-ipc (PtyBackend + adoção), index.ts (backend + quit), electron.vite.config, package.json (smoke:daemon).

## QA Results

### Gate: **CONCERNS** (aprovado) — Quinn (@qa), 2026-07-14. 7 checks PASS (99 testes + smoke:daemon PASS cross-processo em artefatos BUILDADOS — AC1/AC2/AC4 provados fim-a-fim; AC3 por revisão: renderer/adapters intactos, NFR7 verde). Issues low: reconexão automática com backoff fica na 6.4 (planejado); validação do fundador pendente (fechar/reabrir o app real com CLI rodando); CodeRabbit não executado (WSL indisponível — padrão).
