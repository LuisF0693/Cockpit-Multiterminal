# Story 3.2: Envio de instruções a partir do master

## Status

InReview

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"

## Story

**As a** desenvolvedor multi-agente,
**I want** enviar instruções de texto a qualquer agente diretamente do painel master,
**so that** eu direcione o trabalho sem alternar entre terminais.

## Acceptance Criteria

1. Campo de instrução por agente no dashboard envia texto ao PTY correspondente (com confirmação visual de envio).
2. Instrução enviada aparece no terminal do agente como se digitada localmente.
3. Envio registrado na timeline (autor: humano via master).
4. Guarda de segurança: envio para agente em estado error/done exibe aviso antes de transmitir.

## Quality Gate Tasks

- [x] Pre-Commit (@dev) — verify 80 testes PASS
- [x] QA Gate (@qa) — **CONCERNS (aprovado)**: validação com agente real pendente do fundador
- [ ] Pre-PR (@devops): quando houver remote

## Tasks / Subtasks

- [x] Task 1 — Envio pela porta binária (AC: 1, 2) — campo por linha + Enter; ✓ verde com fade 2.5s; mesma porta do tile (aparece como digitado)
- [x] Task 2 — Guarda + timeline (AC: 3, 4) — confirm() em error/done; `session.instructed` → `PersistenceManager.recordInstruction` (events: origem human, type instruction.sent, payload truncado 500)
- [x] Task 3 — Testes + verify — 80 testes PASS (cadeia coberta por unit existentes + persistência de evento via queue)

## Dev Notes

O renderer JÁ possui a MessagePort de cada sessão (store) — o envio não precisa de novo caminho de dados; apenas o REGISTRO do evento vai por IPC de controle (`session.instructed`, fire-and-forget) para a trilha (3.3 consumirá).

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-14 | 0.1/0.2 | Draft + Validação PO GO 10/10 — Approved | River/Pax via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 (claude-fable-5) — Dex (@dev) via Orion

### Debug Log References

- Sem bugs.

### Completion Notes List

- Envio reusa a MessagePort existente do renderer (zero caminho novo de dados); só o REGISTRO vai por IPC de controle. Instrução chega ao agente como digitação local (AC2 por construção — mesmo canal do teclado).

### File List

**Modificados:** `packages/shared/src/ipc.ts` (session.instructed), `packages/core/src/state-store/persistence.ts` (recordInstruction), `apps/desktop/src/main/session-ipc.ts`, `apps/desktop/src/preload/index.ts`, `packages/ui/src/master-dashboard.tsx` (campo+guarda+✓), `apps/desktop/src/renderer/src/App.tsx` (instructAgent)

## QA Results

### Gate: **CONCERNS** (aprovado) — Quinn (@qa) via Orion, 2026-07-14

7 checks PASS. Issue única (medium): validar com agente real (enviar instrução do master ao Claude Code e vê-la executar no tile) — roteiro do fundador.
