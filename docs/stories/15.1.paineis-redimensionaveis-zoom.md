# Story 15.1: Painéis redimensionáveis + zoom out maior

## Status

InReview

## Story

**As a** fundador,
**I want** arrastar a borda da sidebar, da telemetria e do preview pra dimensioná-los, e afastar mais o zoom do canvas,
**so that** eu controle quanto espaço cada área ocupa — e veja topologias grandes inteiras.

## Acceptance Criteria

1. Alça de arraste na borda interna dos 3 painéis: sidebar (200-400px), telemetria (200-400px), preview (380-800px) — mesmo padrão de drag global dos tiles (FR52).
2. Larguras persistem nas settings (FR46), defaults atuais (240/230/520); quem nunca arrastar não muda nada.
3. Zoom mínimo do canvas vira 0.15 (wheel + pill do header); schema `canvasDefaultZoom` alinhado (0.15-2 → clamp efetivo 0.15-1.6) (FR53).

## Tasks / Subtasks

- [x] Task 1 — `PanelResizeHandle` (NOVO, componente único direção-aware) + fiação nos 3 painéis com clamps (200-400/200-400/380-800); alça com `data-no-pan` (não colide com o pan do canvas)
- [x] Task 2 — `AppSettingsSchema`: `sidebarWidth`/`telemetryWidth`/`previewWidth` (catch+default 240/230/520); App aplica no boot e persiste no pointerup (`persistPanelWidth`, merge parcial no Main); testes do schema atualizados; `settings-view.save` faz spread pra não descartar campos que a tela não edita (bug de compilação pego pelo verify)
- [x] Task 3 — `clampZoom` min 0.15 (FR53); schema `canvasDefaultZoom` min 0.15
- [x] Task 4 — `pnpm verify` PASS (25/25 + NFR7) + `smoke:daemon` PASS

## Change Log

| 2026-07-16 | 0.1 | Draft @sm (FR52-53) → @po GO → @dev → InReview; gate @qa CONCERNS (aprovado) | River/Pax/Dex/Quinn via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List

- Persistência no POINTERUP (não durante o arraste) — zero I/O por pixel, mesmo espírito do debounce de layout da 1.4.
- Gotcha desta leva: `pnpm verify` NÃO pode rodar em paralelo com `pnpm dist` (turbo compartilha out/ — falhas fantasma de "no output files").

### File List

- `packages/ui/src/panel-resize-handle.tsx` (NOVO) + `index.ts`
- `packages/ui/src/app-sidebar.tsx`, `telemetry-panel.tsx`, `file-preview-panel.tsx` — prop width + alça
- `packages/ui/src/settings-view.tsx` — spread no save
- `packages/shared/src/ipc.ts` + `ipc.test.ts` — campos/testes novos
- `apps/desktop/src/renderer/src/App.tsx` — estados/persistência/clamp 0.15

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:** 1. Code review — componente único reutilizado 3x, clamps por painel, persistência barata. OK. 2. Testes — schema coberto (defaults+preservação com os campos novos). 3. ACs 1-3 atendidos. 4. Regressões — verify+smoke PASS. 5-6. n/a. 7. Docs ok.

**Observação (CONCERNS):** validação visual pendente (arrastar as 3 alças, fechar/reabrir e conferir persistência, zoom a 15%).
