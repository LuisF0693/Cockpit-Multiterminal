# Story 15.1: PainÃ©is redimensionÃ¡veis + zoom out maior

## Status

Done

## Story

**As a** fundador,
**I want** arrastar a borda da sidebar, da telemetria e do preview pra dimensionÃ¡-los, e afastar mais o zoom do canvas,
**so that** eu controle quanto espaÃ§o cada Ã¡rea ocupa â€” e veja topologias grandes inteiras.

## Acceptance Criteria

1. AlÃ§a de arraste na borda interna dos 3 painÃ©is: sidebar (200-400px), telemetria (200-400px), preview (380-800px) â€” mesmo padrÃ£o de drag global dos tiles (FR52).
2. Larguras persistem nas settings (FR46), defaults atuais (240/230/520); quem nunca arrastar nÃ£o muda nada.
3. Zoom mÃ­nimo do canvas vira 0.15 (wheel + pill do header); schema `canvasDefaultZoom` alinhado (0.15-2 â†’ clamp efetivo 0.15-1.6) (FR53).

## Tasks / Subtasks

- [x] Task 1 â€” `PanelResizeHandle` (NOVO, componente Ãºnico direÃ§Ã£o-aware) + fiaÃ§Ã£o nos 3 painÃ©is com clamps (200-400/200-400/380-800); alÃ§a com `data-no-pan` (nÃ£o colide com o pan do canvas)
- [x] Task 2 â€” `AppSettingsSchema`: `sidebarWidth`/`telemetryWidth`/`previewWidth` (catch+default 240/230/520); App aplica no boot e persiste no pointerup (`persistPanelWidth`, merge parcial no Main); testes do schema atualizados; `settings-view.save` faz spread pra nÃ£o descartar campos que a tela nÃ£o edita (bug de compilaÃ§Ã£o pego pelo verify)
- [x] Task 3 â€” `clampZoom` min 0.15 (FR53); schema `canvasDefaultZoom` min 0.15
- [x] Task 4 â€” `pnpm verify` PASS (25/25 + NFR7) + `smoke:daemon` PASS

## Change Log

| 2026-07-16 | 0.1 | Draft @sm (FR52-53) â†’ @po GO â†’ @dev â†’ InReview; gate @qa CONCERNS (aprovado) | River/Pax/Dex/Quinn via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 â€” Dex (@dev) via Orion

### Completion Notes List

- PersistÃªncia no POINTERUP (nÃ£o durante o arraste) â€” zero I/O por pixel, mesmo espÃ­rito do debounce de layout da 1.4.
- Gotcha desta leva: `pnpm verify` NÃƒO pode rodar em paralelo com `pnpm dist` (turbo compartilha out/ â€” falhas fantasma de "no output files").

### File List

- `packages/ui/src/panel-resize-handle.tsx` (NOVO) + `index.ts`
- `packages/ui/src/app-sidebar.tsx`, `telemetry-panel.tsx`, `file-preview-panel.tsx` â€” prop width + alÃ§a
- `packages/ui/src/settings-view.tsx` â€” spread no save
- `packages/shared/src/ipc.ts` + `ipc.test.ts` â€” campos/testes novos
- `apps/desktop/src/renderer/src/App.tsx` â€” estados/persistÃªncia/clamp 0.15

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:** 1. Code review â€” componente Ãºnico reutilizado 3x, clamps por painel, persistÃªncia barata. OK. 2. Testes â€” schema coberto (defaults+preservaÃ§Ã£o com os campos novos). 3. ACs 1-3 atendidos. 4. RegressÃµes â€” verify+smoke PASS. 5-6. n/a. 7. Docs ok.

**ObservaÃ§Ã£o (CONCERNS):** validaÃ§Ã£o visual pendente (arrastar as 3 alÃ§as, fechar/reabrir e conferir persistÃªncia, zoom a 15%).
