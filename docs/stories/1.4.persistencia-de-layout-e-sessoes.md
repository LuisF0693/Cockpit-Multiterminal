# Story 1.4: Persistência de layout e sessões (fundação)

## Status

InReview

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["coderabbit", "qa-gate-checklist", "persist-restore-cycle"]

## Story

**As a** desenvolvedor multi-agente,
**I want** que o app reabra com o mesmo layout e terminais da última sessão,
**so that** eu não reconstrua meu ambiente a cada restart.

## Acceptance Criteria

1. Layout do grid (quantidade, nomes, posições, tamanhos dos terminais) persiste localmente de forma contínua e assíncrona (NFR8).
2. Ao reabrir o app, o layout é restaurado e os terminais são relançados nos mesmos diretórios de trabalho.
3. Scrollback de cada terminal é persistido e restaurado (limite configurável).
4. Teste de integração automatizado cobre o ciclo persistir → fechar → restaurar.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Feature (persistência + restore)
**Secondary Type(s)**: Database (SQLite WAL, migrations), Infrastructure (módulo nativo sob Electron)
**Complexity**: High

### Specialized Agent Assignment

**Primary Agents**:
- @dev (implementação)

**Supporting Agents**:
- @qa (gate + ciclo persist/restore)
- @data-engineer (consulta se o schema evoluir além da visão lógica de data-models.md)

### Quality Gate Tasks

- [x] Pre-Commit (@dev): Run before marking story complete — `pnpm verify` PASS (45 testes) + `smoke:store` + `smoke:persist` PASS em 2026-07-13
- [x] QA Gate (@qa): 7 quality checks + ciclo persistir → fechar → restaurar — **CONCERNS (aprovado c/ observações)**, ver QA Results
- [ ] Pre-PR (@devops): Run before creating pull request (quando houver remote)

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 30 minutes
- Severity Filter: [CRITICAL, HIGH]

### CodeRabbit Focus Areas

**Primary Focus**:
- Atomicidade das transações (NFR5: gravação parcial jamais corrompe estado)
- Write queue não-bloqueante (NFR8: input do usuário nunca espera I/O)

**Secondary Focus**:
- Rotação/limite dos arquivos de scrollback (não crescer sem teto)
- Migração de schema versionada (app_meta.schema_version)

## Tasks / Subtasks

- [x] Task 0 — better-sqlite3 sob ABI do Electron **[gotcha de módulo nativo — padrão da 1.2]** (AC: 1)
  - [x] Adicionar `better-sqlite3` a `apps/desktop`; `rebuild:native` reescrito como script (prebuild-install com runtime electron — node-gyp exigiria VS Build Tools)
  - [x] Smoke `smoke:store`: DB WAL + transação de 100 escritas/leitura sob Electron — PASS (ABI 125)
  - [x] ⚠️ Dual-ABI confirmado: testes vitest usam `MemoryStateStore`; ciclo real no smoke Electron
  - [x] Documentado no README (seção módulos nativos)
- [x] Task 1 — State store no Main (AC: 1)
  - [x] `packages/core/src/state-store/`: interface `StateStore` (pura) + `SqliteStateStore` (driver better-sqlite3 INJETADO — core nunca importa o nativo)
  - [x] Schema v1: `terminals` (com tile_json), `app_meta` (schema_version, clean_shutdown), `settings`, `events` (append-only) + índices
  - [x] WriteQueue: flush 250ms ou 100 ops numa transação única (`applyBatch`)
  - [x] DB em `userData/state/cockpit.db`
- [x] Task 2 — Persistência contínua (AC: 1)
  - [x] PersistenceManager.wire: eventos do registry → queue → `terminals` + trilha em `events`
  - [x] Canal `layout.update` (Zod) com debounce de 300ms no renderer
  - [x] `clean_shutdown`: 0 no boot, 1 no exit gracioso (com flush)
- [x] Task 3 — Restore no boot (AC: 2)
  - [x] `PersistenceManager.restore`: recria sessões ativas com MESMO id/nome/cwd antes da janela abrir
  - [x] Falha individual → `archived_at` (nunca destrói), demais seguem
  - [x] Renderer inalterado: `session.list` entrega sessões restauradas + portas estacionadas; `layout.get` entrega tiles salvos
- [x] Task 4 — Scrollback persistido (AC: 3)
  - [x] `ScrollbackWriter` no PTY Host: batch ~500ms por sessão (chunks em memória entre flushes)
  - [x] `userData/state/scrollback/{sessionId}.log` com rotação por tamanho (2 gerações, 1MB cada)
  - [x] Restore: `readScrollbackTail` (256KB) injetado na porta ANTES do stream vivo
- [x] Task 5 — Testes e validação (AC: 4)
  - [x] Unit: WriteQueue (4), PersistenceManager (6), ScrollbackWriter (4) — todos com fakes, sem nativo
  - [x] Integração Electron-run `smoke:persist`: 2 sessões + layout + scrollback → shutdown → reboot → restore íntegro — **PASS**
  - [ ] Validação manual: fechar app com 3+ terminais nomeados/posicionados → reabrir → conferir — **pendente do fundador**
  - [x] `pnpm verify` completo passando (45 testes, 9/9 tasks)

## Dev Notes

> Contexto dos shards — não reler arquitetura.

**Decisão crítica 2 (persistência):** SQLite better-sqlite3 WAL como verdade estrutural + event-log; scrollback FORA do DB em arquivos append por terminal. Write queue: input nunca espera I/O; no pior crash perde-se ≤ 250ms de batch. Boot lê estado direto (sem replay de eventos — NFR4 < 10s).

**Gotcha de módulo nativo (herdado da 1.2):** node-pty escapou do rebuild por ter prebuilds N-API; better-sqlite3 usa V8 API — EXIGE rebuild para a ABI do Electron (`@electron/rebuild`, já devDependency). Após o rebuild, better-sqlite3 não carrega mais sob Node puro: NUNCA importar better-sqlite3 em testes vitest — usar a interface `StateStore` com fake. O ciclo real é validado por script Electron-run (padrão `smoke:abi`).

**Onde vive o quê:** SqliteStateStore + write queue no Main (via `packages/core` com driver injetado); ring buffer de scrollback no PTY Host (já recebe todos os chunks — zero custo extra de IPC); arquivos em `userData/state/`.

**Restore reusa o fluxo da 1.3:** recriar sessão = `SessionRegistry.create` com `cwd`/`name` salvos; o renderer recebe tudo por `session.list`/eventos — NÃO criar caminho paralelo de restore na UI. Layout restaurado entra como estado inicial do store Zustand.

**Schema (visão lógica de data-models.md):** `terminals` ganha as colunas do tile (x, y, width, height, zIndex) — o `gridPosition` do modelo conceitual. `adapterId` fica `'shell'` fixo até o E2. `events` append-only com payload JSON validado por Zod.

**Fora desta story:** Recovery Screen de crash (E5/FR12 — aqui só a flag), tasks/decisions (E3), múltiplos perfis/settings UI.

### Testing

- **Unit:** `packages/core/src/state-store/**/*.test.ts` — write queue, ring buffer, restore planner com driver fake.
- **Integração:** `smoke:persist` Electron-run — ciclo completo persistir → fechar → restaurar (AC4).
- **Manual:** 3+ terminais nomeados/posicionados → fechar → reabrir → conferir.
- **Comando:** `pnpm verify` integral (unit) + `pnpm --filter @cockpit/desktop smoke:persist`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-13 | 0.1 | Draft criado a partir do Epic 1 + decisão crítica 2 + data-models, com gotcha dual-ABI do better-sqlite3 mapeado | River (@sm) via Orion |
| 2026-07-13 | 0.2 | Validação PO: GO 10/10 (título ✓, descrição ✓, ACs testáveis ✓, escopo IN/OUT ✓, dependências 1.3 ✓, complexidade High ✓, valor ✓, riscos dual-ABI ✓, DoD ✓, alinhado ao Epic 1 ✓) — Status → Approved | Pax (@po) via Orion |
| 2026-07-13 | 0.3 | Implementação completa (Tasks 0-5): state store WAL + write queue + restore + scrollback; verify 45 testes + smoke:store + smoke:persist PASS. Status → InReview | Dex (@dev) via Orion |
| 2026-07-13 | 1.0 | QA Gate: CONCERNS (aprovado c/ observações) — validação manual do fundador pendente | Quinn (@qa) via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 (claude-fable-5) — Dex (@dev) via Orion

### Debug Log References

- pnpm 10 ignora `package.json#pnpm.onlyBuiltDependencies` → movido para `pnpm-workspace.yaml`; ainda assim o install script não rodou — prebuilt baixado direto via `prebuild-install --runtime electron` (electron-rebuild via node-gyp exigiria VS Build Tools ausentes).
- ESLint parsing error: `session.*/layout.*` dentro de JSDoc fecha o comentário — reescrito.
- `@types/better-sqlite3` necessário para o import tipado no Main.

### Completion Notes List

- **Decisão crítica 2 implementada:** SQLite WAL (schema v1: terminals/app_meta/settings/events) + WriteQueue (250ms/100 ops, transação única via `applyBatch`) + scrollback em arquivos com rotação. Input nunca espera I/O (NFR8); atomicidade por transação (NFR5).
- **Injeção de driver:** `SqliteStateStore` recebe o `Database` pronto do Main — `@cockpit/core` nunca importa better-sqlite3 (gotcha dual-ABI); testes usam `MemoryStateStore` com o mesmo contrato.
- **Restore (AC2):** roda ANTES de criar a janela; ids/nomes/cwd preservados (scrollback casa pela tag = session id); falha individual arquiva e segue; portas estacionadas são entregues no primeiro `session.list` do renderer.
- **Scrollback (AC3):** ScrollbackWriter no PTY Host (batch 500ms, rotação 1MB×2 gerações); restore injeta tail de 256KB na porta antes do stream vivo — o xterm recebe o histórico como primeiro chunk.
- **clean_shutdown (FR12 fundação):** 0 no boot / 1 no exit gracioso com flush da queue; Recovery Screen completa fica para o E5.
- **Validações:** verify 45 testes PASS; `smoke:store` PASS (WAL sob ABI 125); `smoke:persist` PASS (ciclo completo com código real sob Electron). Validação manual do fundador pendente (roteiro no QA Gate).

### File List

**Criados (packages/core):** `src/state-store/types.ts`, `src/state-store/write-queue.ts`, `src/state-store/write-queue.test.ts`, `src/state-store/sqlite-state-store.ts`, `src/state-store/memory-state-store.ts`, `src/state-store/persistence.ts`, `src/state-store/persistence.test.ts`
**Criados (packages/pty-host):** `src/scrollback-writer.ts`, `src/scrollback-writer.test.ts`
**Criados (apps/desktop):** `scripts/rebuild-native.cjs`, `smoke/store-smoke.cjs`, `src/main/persist-smoke.ts`
**Modificados:** `packages/core/src/{index.ts,session-registry.ts}`, `packages/pty-host/src/{index.ts,protocol.ts,host-entry.ts}`, `packages/shared/src/ipc.ts` (LayoutTile + layout.*), `apps/desktop/{package.json,electron.vite.config.ts}`, `apps/desktop/src/main/{index.ts,pty-host-manager.ts,session-ipc.ts}`, `apps/desktop/src/preload/index.ts`, `apps/desktop/src/renderer/src/{App.tsx,cockpit-store.ts}`, `pnpm-workspace.yaml`, `README.md`, `pnpm-lock.yaml`

## QA Results

### Gate Decision: **CONCERNS** (aprovado com observações) — Quinn (@qa) via Orion, 2026-07-13

| # | Check | Resultado | Evidência |
|---|-------|-----------|-----------|
| 1 | Code review | PASS | Driver injetado isola o nativo do core; write queue única no Main; restore reusa fluxo da 1.3 sem caminho paralelo |
| 2 | Unit tests | PASS | 45 testes: +4 WriteQueue, +6 PersistenceManager, +4 ScrollbackWriter (fakes, sem nativo) |
| 3 | Acceptance criteria | PASS | AC1 ✓ queue 250ms/100 ops + layout debounced; AC2 ✓ restore ids/nomes/cwd antes da janela; AC3 ✓ scrollback com rotação + tail injetado; AC4 ✓ smoke:persist Electron-run |
| 4 | No regressions | PASS | verify integral verde; fluxo 1.3 inalterado (session.list entrega restauradas) |
| 5 | Performance | PASS | I/O 100% assíncrono via batch; scrollback fora do DB; boot lê estado direto (sem replay) |
| 6 | Security | PASS | Zod nos canais layout.*; DB em userData; sem dados sensíveis novos |
| 7 | Documentation | PASS | README (módulos nativos + dual-ABI), story completa |

**Issues (CONCERNS):**

```yaml
storyId: '1.4'
verdict: CONCERNS
issues:
  - severity: medium
    category: requirements
    description: 'Validação manual do ciclo real no app (3+ terminais nomeados/posicionados → fechar → reabrir) não executada por humano'
    recommendation: 'Roteiro: abrir 3 terminais, renomear, arranjar, rodar comandos, fechar o app, reabrir — conferir layout, nomes, cwd e histórico'
  - severity: low
    category: code
    description: 'Restore relança PTYs com cols/rows fixos 80x24 — o primeiro fit do renderer corrige, mas pode piscar reflow'
    recommendation: 'Persistir cols/rows do último resize numa iteração futura'
  - severity: low
    category: tests
    description: 'CodeRabbit não executado (carryover 1.2/1.3)'
    recommendation: 'Rodar nas três stories quando o ambiente WSL estiver disponível'
```

**Conclusão:** aprovado; pendência é percepção humana do ciclo completo no app real.

**Update 2026-07-13 20:10 — validação do fundador CONCLUÍDA:** app reaberto restaurou as 3 sessões da execução anterior com scrollback injetado (logs: tails de 3-12KB entregues como primeiro chunk) — confirmado visualmente ("deu certo!"). Issue #1 RESOLVIDA — gate efetivamente PASS.

**Bugs encontrados e corrigidos durante a validação (registrados p/ trilha):**
1. Fork do PTY Host quebrado por chunking do Rollup (commit `d83ccff`) — path por candidatos + smoke:dataflow novo.
2. `port.close()` no cleanup do TerminalView matava o canal no duplo-mount do StrictMode (dev) — posse da porta movida para o store (removeSession fecha).
