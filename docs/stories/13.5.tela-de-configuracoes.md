# Story 13.5: Tela de Configurações

## Status

InReview

## Story

**As a** fundador,
**I want** uma tela de Configurações com as preferências do app,
**so that** eu ajuste comportamentos padrão sem depender de valores chumbados no código.

## Acceptance Criteria

1. Tela/painel de Configurações (nova entrada de navegação, ícone de engrenagem) com no mínimo: modelo default do Ollama, intervalo de refresh do preview de browser (10.1) e zoom padrão do canvas (12.6) (FR46).
2. Preferências persistem via `app_meta` (mesma infraestrutura `getMeta`/`setMeta` do FR10/8.1) — chave única `settings`, JSON; sem tabela nova.
3. Valores aplicados de verdade nos consumidores (novo terminal Ollama usa o default configurado; poll do preview usa o intervalo; canvas abre no zoom configurado) — não é uma tela cosmética.
4. Defaults atuais preservados quando nada foi configurado (zero regressão pra quem nunca abrir a tela).

## Tasks / Subtasks

- [x] Task 1 — `@cockpit/shared`: `AppSettingsSchema` com `catch`+`default` POR CAMPO (`ollamaDefaultModel` 'llama3', `browserPreviewIntervalMs` 1500 [500-60000], `canvasDefaultZoom` 1 [0.4-2]) — valor ausente OU inválido degrada pro default do campo, nunca erro; `SettingsUpdateRequestSchema = partial()`; canais `settings.get`/`settings.update`; 3 testes novos em `ipc.test.ts` (AC2, AC4)
- [x] Task 2 — `PersistenceManager.appSettingsRaw()`/`setAppSettingsRaw()` — transporte cru do JSON em `app_meta.settings`, mesmo padrão de projects/workspaces (AC2)
- [x] Task 3 — Main: `readSettings()` tolerante (JSON quebrado → defaults) + handler de update com merge parcial (AC2, AC4)
- [x] Task 4 — preload: `settings.get`/`update` com Zod na borda (AC2)
- [x] Task 5 — `packages/ui/src/settings-view.tsx` (NOVO): 3 campos com hint de limites/padrão, botão salvar com feedback "✓ salvo", drafts locais re-sincronizados se as settings chegarem depois (boot) (AC1)
- [x] Task 6 — App.tsx: view `'settings'` + nav `⚙`; boot aplica `ollamaDefaultModel` (estado do campo Ollama) e `canvasDefaultZoom` (via `clampZoom`); poll do preview usa `settings.browserPreviewIntervalMs` (era 1500 hardcoded); `saveSettings` re-aplica o modelo default (AC1, AC3)
- [x] Task 7 — `pnpm verify` PASS (25/25 + NFR7) + `smoke:daemon` PASS; 247 testes (era 244; +3)

## Dev Notes

- **Gotcha real de TDZ evitado:** o estado `settings` precisa ser declarado ANTES do efeito do preview de browser no corpo do componente — o array de dependências (`settings?.browserPreviewIntervalMs`) avalia DURANTE o render; declarado depois, seria ReferenceError no boot. Movido pro topo junto do `ollamaModel`, com comentário no código.
- `catch` por campo (não no objeto) é deliberado: um campo corrompido não arrasta os outros pro default.
- Zoom default aplica só no BOOT (é "zoom padrão ao abrir", não força o zoom da sessão corrente ao salvar) — o modelo do Ollama, ao contrário, re-aplica ao salvar (é um default de criação, sem estado de sessão a preservar).

## Change Log

| 2026-07-15 | 0.1 | Draft @sm (FR46) → @po GO (9/10) → @dev → InReview; gate @qa CONCERNS (aprovado). ÉPICO 13 CÓDIGO-COMPLETO (5/5) | River/Pax/Dex/Quinn via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### File List

- `packages/shared/src/ipc.ts` — schema/canais/CockpitApi; `ipc.test.ts` +3 testes
- `packages/core/src/state-store/persistence.ts` — transporte cru
- `apps/desktop/src/main/session-ipc.ts` — handlers get/update
- `apps/desktop/src/preload/index.ts` — ponte
- `packages/ui/src/settings-view.tsx` (NOVO) + `index.ts`
- `apps/desktop/src/renderer/src/App.tsx` — view/nav, boot-apply, poll configurável

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:** 1. Code review — merge no Main (fonte única), Zod nas duas bordas, parsing tolerante; TDZ do array de deps identificado e resolvido com comentário. OK. 2. Testes — 3 novos no schema (defaults, preservação, degradação por campo). 3. ACs 1-4 atendidos — AC3 verificado nos 3 consumidores reais. 4. Regressões — verify+smoke PASS; defaults idênticos ao comportamento anterior. 5. Performance — leitura única no boot; nenhum poll novo. 6. Segurança — valores clampados por schema; nada interpolado em comando (modelo Ollama continua indo como arg posicional, garantia da 12.6). 7. Docs — decisões e gotcha documentados.

**Observação (CONCERNS):** validação visual manual pendente (salvar, reabrir o app, conferir zoom/modelo/intervalo aplicados) — padrão do épico.
