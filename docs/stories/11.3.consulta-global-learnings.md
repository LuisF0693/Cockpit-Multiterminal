# Story 11.3: Consulta global de learnings

## Status

InReview

## Story

**As a** desenvolvedor multi-projeto,
**I want** buscar/filtrar learnings por categoria, projeto de origem ou texto,
**so that** eu reaproveite um aprendizado anterior sem depender de lembrar onde/quando o registrei.

## Acceptance Criteria

1. Tela dedicada (nova view, acessível pelo nav do header) lista todos os learnings, com filtro por categoria, projeto de origem e busca textual simples.
2. Tela é INDEPENDENTE do projeto ativo (Épico 8) — ao contrário de sessões/tarefas/vínculos, learnings NÃO são escopados ao projeto ativo por padrão (a busca por projeto é um FILTRO opcional, não um escopo automático) — é exatamente o ponto da visão do fundador ("banco separado dos projetos", reutilizável entre eles).
3. Cada learning mostra categoria, status de qualificação, projeto de origem (se houver) e texto completo.
4. Nenhuma reformulação do lifecycle de tarefa (FR13) — learnings são uma entidade paralela, sem relação obrigatória com tarefas (FR20).

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 200 testes PASS + smoke:daemon PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — `LearningsView` (packages/ui, NOVO): busca textual + filtro de categoria (derivado dinamicamente dos learnings existentes, sem lista hardcoded) + filtro de projeto de origem; cada card mostra status (cor+label), categoria, projeto de origem e texto completo (AC1/AC3)
- [x] Task 2 — AC2 é a decisão de design mais importante desta story: `LearningsView` recebe `learnings` (lista COMPLETA, sem filtro por projeto ativo aplicado ANTES de chegar no componente) — diferente de `projectSessions`/`projectTasks`/`projectTerminalLinks`/`projectBrowserTiles` (Épico 8/9/10, todos filtrados por `activeProjectId` ANTES de chegar na UI), aqui o filtro de projeto é só mais um filtro INTERNO da própria tela, com o mesmo peso de categoria/busca textual — nunca um escopo automático
- [x] Task 3 — nova view `'learnings'` no App.tsx, entrada própria no nav do header (mesmo padrão de tasks/board/files) — acessível de qualquer lugar, sem depender de estar num projeto específico
- [x] Task 4 — testes: nenhum teste novo (mesmo precedente de `FileExplorer`/`ReviewPanel` — UI de leitura/busca pura sem lógica de domínio própria a isolar; a lógica que IMPORTA — `canTransitionLearning`, persistência — já está coberta desde a 11.1/11.2)

## Dev Notes

- Esta story fecha o Épico 11 e, com ele, TODOS os 4 épicos formalizados a partir da visão do fundador nesta rodada (7, 8, 9, 10 já fechados; 11 formalizado à parte, a partir do ponto 6 da visão original, depois que 8-10 passaram na frente).
- A decisão do Task 2 é sutil mas é o CORAÇÃO da AC2: bastaria copiar o padrão de `projectSessions` (filtrar ANTES de passar pro componente) para quebrar silenciosamente a promessa de "banco separado dos projetos" — um aprendizado registrado num projeto que depois é excluído (Épico 8, `removeProject`) precisa continuar aparecendo aqui, e um aprendizado qualquer precisa ser encontrável mesmo trabalhando noutro projeto agora.
- Filtro de categoria é POPULADO DINAMICAMENTE a partir dos learnings existentes (`[...new Set(learnings.map(l => l.category))]`) em vez de uma lista fixa — como a 11.1 permite categoria livre (`datalist`, não enum fechado), uma lista hardcoded ficaria desatualizada rapidamente.

## Change Log

| 2026-07-14 | 0.1 | Draft + PO GO (Ready) + dev completo — verify/smoke PASS, status → InReview. **ÉPICO 11 FECHADO (3/3)** | River/Pax/Dex via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List

- **ÉPICO 11 (Learning Logs Globais) FECHADO — 3/3 stories.** Com isso, os 4 épicos desta rodada de extensão da visão do fundador (7 já estava fechado antes; 8, 9, 10 fechados nesta sessão; 11 formalizado e fechado agora) estão todos código-completos.
- Cores de status do learning (`STATUS_DOT`) foram definidas como uma paleta PRÓPRIA do domínio, não reaproveitando `STATUS_COLORS` (que é especificamente sobre status de AGENTE — idle/working/waiting-input/done/error, Story 2.5) — evita confundir dois conceitos de "status" completamente diferentes só porque ambos usam cores.
- Próximo passo (fora desta story): PR do Épico 11 pra main.
- Verificação: `pnpm verify` (lint+typecheck+test+NFR7) PASS; `pnpm --filter @cockpit/desktop smoke:daemon` PASS; `npx turbo run test --force` → **200 testes / 32 arquivos** (inalterado desde a 11.2 — story é UI pura de leitura/busca, sem lógica de domínio nova a isolar em teste unit).

### File List

- `packages/ui/src/learnings-view.tsx` (novo) — componente `LearningsView`
- `packages/ui/src/index.ts` — export
- `apps/desktop/src/renderer/src/App.tsx` — view `'learnings'`, entrada no nav do header, wiring do `LearningsView` com a lista COMPLETA (sem filtro de projeto ativo)

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:**
1. Code review — a decisão de NÃO filtrar por projeto ativo antes de passar a lista pro componente é exatamente correta e foi verificada linha a linha contra o padrão (incorreto para este caso) de `projectSessions`/etc.; paleta de cores própria evita confusão conceitual com status de agente. OK.
2. Testes unitários — nenhum teste novo; mesmo precedente de UI de leitura pura já estabelecido (`FileExplorer` 8.4, `ReviewPanel` 7.3) — a lógica de domínio que sustenta esta tela já está 100% coberta desde a 11.1/11.2.
3. Acceptance criteria — AC1 (tela dedicada com 3 filtros) atendido; AC2 (independente do projeto ativo, filtro opcional não automático) atendido e é o ponto mais escrutinado desta review — CONFIRMADO correto; AC3 (campos mostrados por card) atendido; AC4 (nenhuma mudança no lifecycle de tarefa) atendido — `LearningsView` não toca em `TaskManager`/`task-lifecycle.ts` de forma alguma. Todos atendidos.
4. Regressões — `pnpm verify` + `smoke:daemon` + 200 testes/32 arquivos PASS; nenhuma view existente foi alterada além da adição da nova entrada de nav.
5. Performance — filtros são `.filter()`/`.map()` sobre um array tipicamente pequeno (learnings não crescem na mesma escala de eventos/timeline), sem impacto perceptível.
6. Segurança — nenhuma superfície nova; tela é só leitura (nenhuma mutação nesta story).
7. Documentação — Dev Notes/Completion Notes/File List preenchidos; decisão do AC2 (não escopar) documentada com destaque por ser a mais fácil de errar por analogia com o resto do app; Épico 11 marcado como fechado.

**Observação (CONCERNS, não bloqueante):** validação visual manual (registrar learnings reais, filtrar, confirmar que sobrevivem à exclusão de um projeto) pendente do fundador — mesmo padrão de toda a sessão.
