# Story 2.5: Painel de status dos agentes no grid

## Status

InReview

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"

## Story

**As a** desenvolvedor multi-agente,
**I want** ver em um relance o status de todos os agentes no grid,
**so that** eu identifique imediatamente quem precisa de mim.

## Acceptance Criteria

1. Cada tile exibe status com código de cor consistente (working/waiting/done/error/idle).
2. Agentes em waiting-input recebem destaque visual proeminente (pré-requisito do FR9).
3. Transições de status aparecem em < 2s após o evento do adapter.

## Quality Gate Tasks

- [x] Pre-Commit (@dev) — verify 77 testes PASS
- [x] QA Gate (@qa): 7 checks — **CONCERNS (aprovado)**; percepção visual pendente do fundador
- [ ] Pre-PR (@devops): quando houver remote

## Tasks / Subtasks

- [x] Task 1 — Código de cores centralizado (AC: 1)
  - [x] `packages/ui/src/status-colors.tsx`: STATUS_COLORS/LABELS + helpers (fonte única; tile/sidebar/header consomem)
  - [x] Unit test cobrindo todas as chaves do AgentStatusSchema
- [x] Task 2 — Realce de waiting-input (AC: 2)
  - [x] Tile: borda âmbar + pulso (keyframes via StatusPulseStyles, injetado 1x no App) + dot maior — vence o focus ring
  - [x] Sidebar: dot por agentStatus + badge "!" âmbar
  - [x] Header: contador "N aguardando você" — embrião da fila de decisões (visão/E5)
- [x] Task 3 — Latência e validação (AC: 3)
  - [x] Cadeia 100% push (único delay: polling de segurança 500ms dos watchers) — orçamento < 2s ok por construção
  - [ ] Manual (fundador): permissão do Claude → tile pulsa + contador — **pendente**
  - [x] `pnpm verify` integral (77 testes)

## Dev Notes

Toda a fiação de eventos veio das 2.1-2.3 — esta story é a CAMADA VISUAL. O realce de waiting é o pré-requisito do FR9 (decisões pendentes, E5) e conecta com a "fila de decisões" da visão do fundador. Latência: cadeia é 100% event-driven; o único delay configurável é o polling de segurança dos watchers (500ms) — dentro do orçamento de 2s.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-14 | 0.1 | Draft (camada visual sobre eventos das 2.1-2.3) | River (@sm) via Orion |
| 2026-07-14 | 0.2 | Validação PO: GO 10/10 — Approved | Pax (@po) via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 (claude-fable-5) — Dex (@dev) via Orion

### Debug Log References

- `status-colors` precisa ser .tsx (JSX do StatusPulseStyles).

### Completion Notes List

- Cores movidas do tile p/ fonte única em `status-colors.tsx`; contador do header e badge da sidebar preparam o FR9 (fila de decisões, E5) e a "coluna de decisões pendentes" da visão do fundador.

### File List

**Criados:** `packages/ui/src/{status-colors.tsx,status-colors.test.ts}`
**Modificados:** `packages/ui/src/{index.ts,terminal-tile.tsx,sidebar.tsx}`, `apps/desktop/src/renderer/src/App.tsx`

## QA Results

### Gate: **CONCERNS** (aprovado) — Quinn (@qa) via Orion, 2026-07-14

7 checks PASS (fonte única de cores testada contra o schema; realce em 3 camadas; cadeia push < 2s por construção). Issue única (low): percepção visual do pulso/contador pendente do roteiro do fundador.
