# Story 15.3: Janela de ConfiguraÃ§Ãµes OmniRift

## Status

Done

## Story

**As a** fundador,
**I want** uma janela de ConfiguraÃ§Ãµes em overlay com navegaÃ§Ã£o lateral (Geral/Privacidade/AparÃªncia), fiel Ã s capturas do OmniRift,
**so that** eu configure o app â€” inclusive o tema AO VIVO â€” numa superfÃ­cie bonita e organizada.

## Acceptance Criteria

1. Overlay modal (760Ã—540, scrim, fecha no Ã— ou clicando fora) com nav lateral: Geral, Privacidade e (sob "MAIS") AparÃªncia; substitui a view `settings` â€” a entrada da sidebar abre a janela sobre QUALQUER tela (FR54).
2. Geral: IDIOMA (PortuguÃªs ativo, English desabilitado "em breve" â€” decisÃ£o i18n-depois) + preferÃªncias reais do FR46 (modelo Ollama, intervalo do preview, zoom padrÃ£o; commit no blur) + card-atalho pra AparÃªncia.
3. Privacidade: compromissos REAIS (NFR1 local-first/zero telemetria, NFR6 credenciais nos CLIs, chaves da Central de API no keychain) â€” nenhuma seÃ§Ã£o inventada (sem Conta/Feature flags/etc).
4. AparÃªncia (fiel Ã  foto): MODO Escuro/Claro, TEMAS PRONTOS (3 presets), COR DE DESTAQUE (7 swatches, selecionado vira quadrado com anel), FONTE (TEXTO)/(MONO), TODAS AS CORES (10 pares label+hex do tema COMPOSTO atual) e rodapÃ© "Preview ao vivo â€” salvo automÃ¡tico" + â†º padrÃ£o â€” cada mudanÃ§a persiste (FR46) E re-aplica o tema na hora (15.2).

## Tasks / Subtasks

- [x] Task 1 â€” `settings-window.tsx` (NOVO): overlay + nav + 3 seÃ§Ãµes (Geral/Privacidade/AparÃªncia), pills/inputs/swatches sobre os tokens; `TODAS AS CORES` derivada de `composeTheme`+`themeToCssVars` (sempre espelha o tema composto REAL)
- [x] Task 2 â€” App.tsx: estado `settingsOpen` (view `'settings'` REMOVIDA da uniÃ£o â€” a janela flutua sobre qualquer view, como a referÃªncia); entrada da sidebar abre a janela; `saveSettings` â†’ `updateSettings(patch)` (merge parcial + `applyTheme(composeTheme(...))` no retorno)
- [x] Task 3 â€” `settings-view.tsx` (13.5) REMOVIDA (superseded â€” todos os campos re-hospedados em Geral); exports atualizados
- [x] Task 4 â€” `pnpm verify` PASS (25/25 + NFR7) + `smoke:daemon` PASS

## Dev Notes

- "Salvo automÃ¡tico" da referÃªncia implementado de verdade: AparÃªncia persiste POR MUDANÃ‡A (cada clique = 1 `settings.update` parcial + re-apply); campos textuais do Geral commitam no blur (evita persistir digitaÃ§Ã£o parcial).
- A lista TODAS AS CORES Ã© READ-ONLY (como a foto) â€” ediÃ§Ã£o de cor individual criaria um 4Âº eixo de tema (custom themes); fica como extensÃ£o futura se o fundador pedir.
- SeÃ§Ã£o "Central de API" entra nesta MESMA janela na 15.4 (nav ganha a entrada).

## Change Log

| 2026-07-16 | 0.1 | Draft @sm (FR54) â†’ @po GO (9/10) â†’ @dev â†’ InReview; gate @qa CONCERNS (aprovado) | River/Pax/Dex/Quinn via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 â€” Dex (@dev) via Orion

### File List

- `packages/ui/src/settings-window.tsx` (NOVO)
- `packages/ui/src/settings-view.tsx` (REMOVIDO)
- `packages/ui/src/index.ts` â€” exports
- `apps/desktop/src/renderer/src/App.tsx` â€” overlay + updateSettings + view removida

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:** 1. Code review â€” overlay isolado (stopPropagation no frame), seÃ§Ãµes puras, persistÃªncia parcial por mudanÃ§a. OK. 2. Testes â€” componente de exibiÃ§Ã£o (precedente); a lÃ³gica de composiÃ§Ã£o JÃ Ã© testada na 15.2. 3. ACs 1-4 atendidos. 4. RegressÃµes â€” verify+smoke PASS; campos do FR46 preservados na nova casa. 5. Performance â€” TODAS AS CORES recompÃµe o tema por render da janela (barato, sÃ³ com a janela aberta). 6. n/a. 7. Docs ok.

**ObservaÃ§Ã£o (CONCERNS):** validaÃ§Ã£o visual pendente (trocar tema/cor/fonte ao vivo com terminais abertos, tema claro, contraste).
