# Story 15.3: Janela de Configurações OmniRift

## Status

InReview

## Story

**As a** fundador,
**I want** uma janela de Configurações em overlay com navegação lateral (Geral/Privacidade/Aparência), fiel às capturas do OmniRift,
**so that** eu configure o app — inclusive o tema AO VIVO — numa superfície bonita e organizada.

## Acceptance Criteria

1. Overlay modal (760×540, scrim, fecha no × ou clicando fora) com nav lateral: Geral, Privacidade e (sob "MAIS") Aparência; substitui a view `settings` — a entrada da sidebar abre a janela sobre QUALQUER tela (FR54).
2. Geral: IDIOMA (Português ativo, English desabilitado "em breve" — decisão i18n-depois) + preferências reais do FR46 (modelo Ollama, intervalo do preview, zoom padrão; commit no blur) + card-atalho pra Aparência.
3. Privacidade: compromissos REAIS (NFR1 local-first/zero telemetria, NFR6 credenciais nos CLIs, chaves da Central de API no keychain) — nenhuma seção inventada (sem Conta/Feature flags/etc).
4. Aparência (fiel à foto): MODO Escuro/Claro, TEMAS PRONTOS (3 presets), COR DE DESTAQUE (7 swatches, selecionado vira quadrado com anel), FONTE (TEXTO)/(MONO), TODAS AS CORES (10 pares label+hex do tema COMPOSTO atual) e rodapé "Preview ao vivo — salvo automático" + ↺ padrão — cada mudança persiste (FR46) E re-aplica o tema na hora (15.2).

## Tasks / Subtasks

- [x] Task 1 — `settings-window.tsx` (NOVO): overlay + nav + 3 seções (Geral/Privacidade/Aparência), pills/inputs/swatches sobre os tokens; `TODAS AS CORES` derivada de `composeTheme`+`themeToCssVars` (sempre espelha o tema composto REAL)
- [x] Task 2 — App.tsx: estado `settingsOpen` (view `'settings'` REMOVIDA da união — a janela flutua sobre qualquer view, como a referência); entrada da sidebar abre a janela; `saveSettings` → `updateSettings(patch)` (merge parcial + `applyTheme(composeTheme(...))` no retorno)
- [x] Task 3 — `settings-view.tsx` (13.5) REMOVIDA (superseded — todos os campos re-hospedados em Geral); exports atualizados
- [x] Task 4 — `pnpm verify` PASS (25/25 + NFR7) + `smoke:daemon` PASS

## Dev Notes

- "Salvo automático" da referência implementado de verdade: Aparência persiste POR MUDANÇA (cada clique = 1 `settings.update` parcial + re-apply); campos textuais do Geral commitam no blur (evita persistir digitação parcial).
- A lista TODAS AS CORES é READ-ONLY (como a foto) — edição de cor individual criaria um 4º eixo de tema (custom themes); fica como extensão futura se o fundador pedir.
- Seção "Central de API" entra nesta MESMA janela na 15.4 (nav ganha a entrada).

## Change Log

| 2026-07-16 | 0.1 | Draft @sm (FR54) → @po GO (9/10) → @dev → InReview; gate @qa CONCERNS (aprovado) | River/Pax/Dex/Quinn via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### File List

- `packages/ui/src/settings-window.tsx` (NOVO)
- `packages/ui/src/settings-view.tsx` (REMOVIDO)
- `packages/ui/src/index.ts` — exports
- `apps/desktop/src/renderer/src/App.tsx` — overlay + updateSettings + view removida

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:** 1. Code review — overlay isolado (stopPropagation no frame), seções puras, persistência parcial por mudança. OK. 2. Testes — componente de exibição (precedente); a lógica de composição JÁ é testada na 15.2. 3. ACs 1-4 atendidos. 4. Regressões — verify+smoke PASS; campos do FR46 preservados na nova casa. 5. Performance — TODAS AS CORES recompõe o tema por render da janela (barato, só com a janela aberta). 6. n/a. 7. Docs ok.

**Observação (CONCERNS):** validação visual pendente (trocar tema/cor/fonte ao vivo com terminais abertos, tema claro, contraste).
