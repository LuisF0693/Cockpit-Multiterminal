# Story 7.1: Papéis de agente na tarefa (escritor/revisor)

## Status

InReview

## Story

**As a** desenvolvedor multi-agente,
**I want** atribuir um papel (escritor ou revisor) a cada terminal vinculado a uma tarefa,
**so that** o sistema saiba quem escreve e quem revisa antes de orquestrar o ciclo.

## Acceptance Criteria

1. Vínculo tarefa↔terminal (5.2) ganha um papel opcional: `writer` ou `reviewer`; ausência de papel = vínculo "neutro" (comportamento atual, sem three-brain).
2. Uma tarefa é considerada "modo three-brain" quando tem exatamente 1 terminal com papel `writer` e 2+ com papel `reviewer`.
3. Papel é definido no mesmo dropdown de vínculo do master (3.1/5.2) e visível no card da tarefa (TasksPanel/LifecycleBoard).
4. Papel persiste e sobrevive a restart/adoção (mesma garantia do vínculo em si, 5.2).

## Quality Gate Tasks

- [x] Pre-Commit (@dev): verify 140 testes PASS + smoke:daemon PASS | [x] QA Gate (@qa): **CONCERNS (aprovado)** | [ ] Pre-PR (@devops, com remote)

## Tasks / Subtasks

- [x] Task 1 — `classifyTaskRoles(sessions, taskId)` em `@cockpit/shared` (NOVO arquivo `task-roles.ts`): computa `{writer, reviewers, isThreeBrain}` a partir de `SessionRecord[]` — função pura, ÚNICA implementação consumida por Main (7.2) e UI (7.3), evita duplicar a regra dos dois lados da fronteira core/ui (lição da 5.4)
- [x] Task 2 — modelo (AC1): `taskRole: 'writer'|'reviewer'|null` no `SessionRecordSchema` (shared) e `PersistedTerminal` (core); schema v6 (coluna `task_role`, ALTER idempotente igual v2/v3/v5)
- [x] Task 3 — `StateStore.setTerminalTask` ganha parâmetro `role` opcional (sqlite+memory); `SessionRegistry.linkTask(id, taskId, role?)` estende a assinatura da 5.2 sem quebrar chamadores existentes (role default undefined = sem papel)
- [x] Task 4 — contrato: `taskRole` opcional em `TaskLinkRequestSchema`; `CockpitApi.session.linkTask` já existia, só o payload cresceu
- [x] Task 5 — IPC (Main) + preload: handler `session.linkTask` repassa `role`; `registry.adopt`/`restore()` propagam `taskRole` (mesmo cuidado da 5.2 com `taskId`)
- [x] Task 6 — UI: dropdown de vínculo do master (5.2) ganha um segundo `<select>` adjacente (✍ escritor/👁 revisor/—) desabilitado sem tarefa; badge 🧠 de "modo three-brain" no card (TasksPanel) e no LifecycleBoard usando `classifyTaskRoles`; ícone de papel nos chips de terminal vinculado
- [x] Task 7 — testes: `classifyTaskRoles` (7 casos — 0 writer, 1 writer só, 1+1, 1+2 três-brain, 2 writers ambíguo, cross-task, 1+3); registry.linkTask com role persiste e sobrevive a "restart"; desvincular limpa o papel; verify suíte completa PASS (140) + smoke:daemon PASS

## Dev Notes

- `classifyTaskRoles` em `@cockpit/shared` (não em core nem em ui) é a decisão arquitetural chave desta story — Main (7.2, decidir quando rotear) e UI (7.3, decidir o que mostrar no painel) precisam da MESMA regra; shared é o único pacote que ambos já importam.
- Papel é armazenado SEPARADO do `taskId` (não um enum combinado) — desvincular (`taskId: null`) já limpa o papel implicitamente (papel sem tarefa não faz sentido); a Task 3 garante isso na store.
- Sem papel = comportamento atual (5.2/5.3) inalterado — three-brain é aditivo, nunca obrigatório.

## Change Log

| 2026-07-14 | 0.1/0.2 | Draft + PO GO 10/10 (Ready) + dev start | River/Pax via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### Completion Notes List / File List

- `classifyTaskRoles` em `@cockpit/shared` (não core, não ui) foi a decisão arquitetural central — evita repetir o erro de duplicação que só foi corrigido DEPOIS na 5.4; aqui centralizamos DESDE a primeira story que precisa da regra.
- Mesmo cuidado da 5.2 com `taskId` replicado para `taskRole`: `create()`, `adopt()` e `restore()` precisam propagar o papel explicitamente, senão ele se perderia silenciosamente no primeiro relaunch/adoção — coberto por teste dedicado.
- Desvincular (`taskId: null`) limpa o papel em TODAS as camadas (registry, sqlite, memory) — nunca fica um `taskRole` órfão sem `taskId`.
- **Criados:** shared (`task-roles.ts`+test). **Modificados:** shared (`ipc.ts`: taskRole/TaskLinkRequest, `index.ts`), core (state-store types/sqlite v6/memory/persistence+tests, session-registry), Main (`session-ipc.ts`), ui (`master-dashboard.tsx` seletor de papel, `tasks-panel.tsx`/`lifecycle-board.tsx` badges), App.tsx (linkTask com role).

## QA Results

### Gate: **CONCERNS** (aprovado) — Quinn (@qa), 2026-07-14. 7 checks PASS (140 testes + smoke:daemon PASS; AC1/AC2/AC4 com evidência unit completa — classifyTaskRoles em 7 casos de contorno, persistência+sobrevivência a restart, limpeza de papel no desvínculo; AC3 por revisão de código). Issues low: modo three-brain ainda não tem NENHUM comportamento automático (roteamento é 7.2) — puramente um rótulo visual nesta story, como previsto no escopo; validação visual do fundador pendente; CodeRabbit não executado (WSL indisponível — padrão).
