# Story 14.1: Paleta e tipografia Multerminal (re-tune dos tokens)

## Status

InReview

## Story

**As a** fundador com o mockup aprovado,
**I want** a paleta quase-preta neutra e fonte mono do mockup aplicadas via tokens,
**so that** o app inteiro ganhe a cara nova de uma vez, sem tocar componente por componente.

## Acceptance Criteria

1. `theme.ts` re-tunado com os valores EXATOS do mockup (`docs/prd/referencia-visual-multerminal.dc.html`): superfícies `#0a0a0c…#19191c` neutras, bordas `#232326/#1c1c1f/#2a2a2e`, texto `#f4f4f5/#e4e4e7/#a1a1aa/#71717a/#52525b`, acento ciano `#22d3ee`+`#67e8f9`, ok `#4ade80` (FR47).
2. `font.ui` monoespaçada (JetBrains Mono com fallbacks) em TODA a interface; escala tipográfica do mock (10/11/12/13/16).
3. Grade do canvas com os valores do mock (dots `#1c1c1f` 1.5px, célula 22px, chão `#0c0c0e`); `PROJECT_PALETTE` do mock; raio de tile 9 (`radius.lg`), `radius.xl` 16 novo (zonas, 14.3).
4. Testes verdes, `pnpm verify` + `smoke:daemon` PASS — zero mudança de comportamento.

## Tasks / Subtasks

- [x] Task 1 — `theme.ts`: superfícies/texto/bordas/acentos/raios/sombras/fontes re-tunados; tokens NOVOS `text.bright` (#F4F4F5) e `accent.bright` (#67E8F9) pros destaques do mock; `radius.xl` 16 (AC1, AC2)
- [x] Task 2 — `canvasBackground()`: grade exata do mock (`circle at 1px 1px`, 1.5px, 22px); `PROJECT_PALETTE` → `#22D3EE #4ADE80 #A78BFA #F472B6 #FBBF24 #FB923C` (AC3)
- [x] Task 3 — `STATUS_COLORS` alinhado (idle `#71717A`, working `#4ADE80`) — âmbar do waiting-input PRESERVADO (contrato FR9/teste); `terminal` (xterm) → fundo `#0F0F11`, seleção `#2A2A2E`, texto `#D4D4D8` (AC1)
- [x] Task 4 — `pnpm verify` PASS (25/25 + NFR7) + `smoke:daemon` PASS; 247 testes inalterados (testes de tema validam formato/invariantes, não valores — passaram sem edição) (AC4)

## Dev Notes

- A 13.1 é o que torna esta story de ~3 arquivos em vez de ~20: todos os componentes já consomem tokens, então trocar VALORES re-veste tudo — inclusive o xterm (token `terminal`).
- `shadow.tileFocused` perdeu o anel ciano (o mock foca só com sombra mais forte + alças); a borda ciana de foco do componente continua até a 14.4 redesenhar o tile.
- Fontes: JetBrains Mono precisa estar instalada na máquina (o mock usa `local()`); fallbacks Cascadia Mono/Consolas cobrem Windows por padrão.

## Change Log

| 2026-07-16 | 0.1 | Draft @sm (FR47) → @po GO (9/10) → @dev → InReview; gate @qa CONCERNS (aprovado) | River/Pax/Dex/Quinn via Orion |

## Dev Agent Record

### Agent Model Used

Claude Fable 5 — Dex (@dev) via Orion

### File List

- `packages/ui/src/theme.ts` — valores Multerminal + `text.bright`/`accent.bright`/`radius.xl`
- `packages/ui/src/status-colors.tsx` — idle/working alinhados

## QA Results

**Verdict:** CONCERNS (aprovado)

**Checklist:** 1. Code review — só valores de token; arquitetura intocada; invariantes preservadas (terminal.background===surface.tile; âmbar FR9). OK. 2. Testes — suíte existente valida formato/escalas; 44 UI PASS sem edição. 3. ACs 1-4 atendidos. 4. Regressões — verify+smoke PASS. 5-6. n/a (sem lógica/superfície nova). 7. Docs ok.

**Observação (CONCERNS):** validação visual do fundador pendente — e o restante do épico (14.2-14.5) é que entrega a ANATOMIA do mock; esta story é só a pele.
